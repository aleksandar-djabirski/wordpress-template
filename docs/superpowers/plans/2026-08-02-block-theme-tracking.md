# Block-theme migration — execution tracking

Master specification: `BLOCK_THEME_PROPOSAL.md`.

The detailed implementation remains in the four task-plan files in this directory. This file defines the authoritative execution order, branch boundaries, shared-file ownership, review policy, and final merge policy. When a detailed plan conflicts with this file, this file controls execution and the orchestrator must correct the detailed plan before continuing.

The `cinq-wp` back-port remains outside this engagement. Do not create or change `cinq-wp` files.

## Status table

| Unit | Plan file and scope | Gate | Depends on | Branch | Status |
|---|---|---|---|---|---|
| 0 | Planning pack, clean base, integration branch | Execution ready | — | `feat/block-theme-fse-migration` | merged |
| 1 | `2026-08-02-block-theme-task-1-theme-and-editing.md` | Release 1 | 0 | `feat/bt-task-1-theme-and-editing` | merged |
| 2 | `2026-08-02-block-theme-task-2-state-export-diff.md` | Release 2 | 1 | `feat/bt-task-2-state-export-diff` | merged |
| 3A | `2026-08-02-block-theme-task-3-promotion-lifecycle.md`, Tasks 1–21 | Release 3 | 2 | `feat/bt-task-3-promotion-lifecycle` | merged |
| 3B | `2026-08-02-block-theme-task-3-promotion-lifecycle.md`, Tasks 22–23 | Release 4 | 3A | `feat/bt-task-3-global-styles` | planned |
| 4A | `2026-08-02-block-theme-task-4-commerce-hardening.md`, Phase A | Commerce profile | 3B | `feat/bt-task-4-commerce-hardening` | planned |
| 4B | `2026-08-02-block-theme-task-4-commerce-hardening.md`, Phase B | Final closure | 4A | `feat/bt-task-4-final-hardening` | planned |

Statuses: `planned` → `in-progress` → `in-review` → `merged`. Use `blocked` only with the exact blocker and evidence.

## Unit 0 — execution readiness

The orchestrator completes every item before Unit 1 starts:

1. Record the current local `main` SHA and `origin/main` SHA.
2. Review the five local commits that are ahead of `origin/main`. Preserve them. Do not reset or discard them.
3. Fix the accidental `README.md` text insertion. Commit it separately only when the corrected line differs from `HEAD`. When the repair restores the tracked line exactly, record the no-op and do not create an empty commit.
4. Commit `BLOCK_THEME_PROPOSAL.md`, the four task plans, this tracking file, and the execution handoff prompt.
5. Confirm `git status --porcelain` is empty.
6. Create `feat/block-theme-fse-migration` from the recorded local `main` SHA.
7. Push the integration branch because the migration-baseline workflow must run from GitHub.
8. Confirm DDEV, Docker, Node 22 or newer, npm 10 for lock generation, Composer through DDEV, authenticated Windows Git access through Git Credential Manager, and GitHub Actions access through the connected GitHub app. Do not require a second GitHub CLI token when the existing Windows and app connections provide the required operations.
9. Confirm the task runner can open and inspect downloaded PNG artifacts.
10. Confirm the Claude Code command and the requested Opus 5 model are callable before the final-review gate. If they are unavailable, report the blocker before Unit 1 starts. Do not claim that another model performed the requested independent review.

## Execution order

Run the units in the status-table order. Do not run implementation units in parallel.

This repository uses the fixed DDEV project name `agency-starter`. Parallel worktrees would otherwise share or replace one DDEV project and database. Only one task worktree may own the DDEV project at a time.

DDEV refuses to move a listed project root. `ddev start` from a new worktree fails with `project root is already set to <old path>, refusing to change it`. The documented and non-destructive fix is `ddev stop --unlist agency-starter`, then `ddev start` from the new worktree. Proven on 2026-08-03: the `agency-starter-mariadb` volume survives `--unlist`, so the installed WordPress database follows the project name into the new worktree and no re-install is needed. Only `ddev delete` would drop that volume, and this plan forbids `ddev delete` against the main checkout.

Before a new unit starts:

1. Stop the prior worktree's DDEV project with `ddev stop`, then release the project root with `ddev stop --unlist agency-starter`.
2. Merge the prior unit into `feat/block-theme-fse-migration`.
3. Run the prior unit's integration-branch gate.
4. Create the next branch and worktree from the updated integration branch.
5. Start DDEV from that worktree only.

Do not use `ddev delete` against the main checkout. A plan step that needs a destructive fresh-install proof must use its named disposable proof project or a verified disposable task worktree.

## Shared-file ownership

Only the orchestrator edits this tracking file. Workers return status, commit SHAs, tests, and blockers to the orchestrator. Workers do not commit this file from task branches.

The existing cross-task ownership grants remain:

- Unit 1 owns the role and editor policy, all Release 1 behavior in `web/app/mu-plugins/agency-platform/src/Plugin.php` and `src/Cli/AgencyCommands.php`, `scripts/setup`, the base `.github/workflows/ci.yml` conversion, `tests/support/BlockIndexGenerator.php`, the `WooCommerceIsolationTest` wording, and the narrow base capability-matrix change in `tests/commerce/Integration/Permissions/ShopManagerCapabilitiesTest.php`.
- Unit 2 owns `.env.example` entries for state export, `Health/SanitizeSteps.php`, `EnvironmentSafetyTest.php`, deletion of `DatabaseOverrideCheck.php`, and its sequenced one-line `Plugin.php` registration.
- **Ownership transfer to Unit 2, decided 2026-08-05 — `src/Cli/AgencyCommands.php`, narrowly.** The two grants above cannot both hold as written: `AgencyCommands.php:7` imports `DatabaseOverrideCheck` and line 53 constructs it, so Unit 2 cannot delete that class while Unit 1 owns the only file that uses it. Deleting it without editing the file makes every `wp agency check-overrides` invocation fatal. Unit 2 therefore owns, in the single Task 13 commit that removes the class: `check_overrides()`'s body, docblock and imports; the deletion of `drift_is_failure()` and `drift_summary_lines()`; and the deletion of `tests/Unit/AgencyPlatform/CheckOverridesReportTest.php`, whose four tests call only those two helpers over the report shape that is being removed. `check_overrides()`'s signature is already correct from Unit 1's `d0da3f3` and does not change. `register()`, `sanitize()` and `verify_env()` stay Unit 1's and must not change. Deferring was considered and rejected: deferring the rewrite ships a knowingly fatal command, and deferring the deletion leaves §11.10 drift reporting with two competing implementations across a unit boundary. Unit 1 is merged, so the transfer creates no live conflict.
- Unit 3A owns the promotion Playwright script, promotion CI steps, and its sequenced one-line `Plugin.php` registration.
- Unit 3B owns only the Theme JSON adapter, `PromotionStrategyRegistrarTest.php`, and the Global Styles promotion surface named in Tasks 22–23.
- Unit 4A owns commerce templates, commerce patterns, commerce tests, `scripts/enable-commerce`, `CommerceBoundaryTest.php`, and the commerce routing documentation. Unit 1 owns the PHP-symbol wording in `WooCommerceIsolationTest.php`; Unit 4A owns the HTML block-markup boundary. Add no WooCommerce allow-list entry unless the scanner proves that a new scanned PHP symbol needs one.
- Unit 4B owns the state runbook, `README.md`, `ops/**`, the remaining documentation and operations sweep, the proof records, and the final report. Unit 1 documentation is limited to `AGENTS.md`, `docs/architecture.md`, `docs/editing-strictness.md`, `docs/ownership-rules.md`, `docs/adding-a-block.md`, `docs/validation-scenarios.md`, `docs/generated-block-index.md`, and `docs/block-theme-migration-baseline.md`.

`PromotionStrategy`, `PromotionStrategies`, and `SchemaValidator::SCHEMA_PROMOTION_MANIFEST` are Unit 2 interfaces. Unit 3 implements and registers the strategies.

## Model and review policy

The primary session uses Claude Code Opus 5 as the orchestrator. The user made this change on 2026-08-03 after the earlier `gpt-5.6-sol` session stopped. Opus 5 owns orchestration, integration decisions, plan and tracking corrections, final verification, and the single final independent review. Opus 5 does not perform bounded implementation work while a Codex worker is callable.

Record the model and the reasoning level for every delegated task in the log below. Never record a model that did not run.

When `gpt-5.6-luna` is callable, use it with max reasoning for bounded implementation steps. Give it one plan task, its owned files, the exact acceptance checks, and the required report. Run code-writing workers in the exact task worktree with `codex exec -m gpt-5.6-luna -c 'model_reasoning_effort="max"' -C <worktree>`. Keep the returned thread identifier so a bounded fix can resume the same task context. If any runner reports a lower reasoning level, stop that code-writing task and replace it with the exact CLI invocation. The Sol orchestrator reviews every returned diff and runs the required gates. If Luna is not callable, the Sol session performs the work itself and records that fallback. Never claim that Luna ran when it did not.

Use Terra high for each bounded task review and for every accepted review-fix wave, including fixes from the final Opus review. Keep the Terra input limited to the task brief, the review findings, the affected diff, and the required tests. The Sol orchestrator reviews every Terra fix diff and runs the affected gates. This replaces the earlier instruction to use Luna for accepted review fixes. Read-only plan audits that produced no code do not need a second run when the app displayed a lower reasoning level.

Run a Sol high-reasoning review at the end of each execution unit. Do not merge a unit with unresolved critical or important findings.

After Unit 4B is merged into the integration branch, run the final review exactly once over the complete diff from the recorded base SHA to `feat/block-theme-fse-migration`. Save its report outside tracked customer-state paths. Classify every finding as accepted or rejected with evidence.

The final review runs in a **cold Claude Opus 5 subagent**, not in the orchestrator session. The orchestrator gives it the master proposal, this tracking file, `AGENTS.md`, all four task plans, the complete diff, and the verification evidence. It asks for actionable findings only, each with severity, file and line, the violated requirement, the evidence, and a proposed correction.

The reason is independence. The orchestrator session directed the work, so it carries anchoring bias into any review of that work. A cold subagent has seen none of the execution, which is what the original plan required. A second reason is context quality: by Unit 4B the orchestrator context has been summarized several times, and a large final diff must not be judged from a compacted context.

The same rule applies through the whole engagement. Large diffs stay out of the orchestrator context. Terra and Sol read each diff and return short reports. The orchestrator pulls only the specific hunks it must judge itself.

Use Terra high for accepted fixes. Use Sol high as the fallback. Then run a Sol high-reasoning review of the fix diff. The final Opus review is not a substitute for this post-fix review.

## Required CI inputs

Task 1's two image workflows are orchestrator gates. The workflow files must provide an agent-runnable push trigger in addition to `workflow_dispatch` when no authenticated workflow-dispatch tool is available. The orchestrator uses Windows Git Credential Manager to push the exact trigger ref and the connected GitHub app to inspect runs and download artifacts. It inspects every PNG, updates the metadata, and commits the accepted files. No second GitHub CLI token or separate human action is required.

Promotion CI uses a fixed or generated test-only HMAC key inside the isolated CI job. It does not depend on a repository secret. Production still requires `AGENCY_PROMOTION_HMAC_KEYS` and `AGENCY_PROMOTION_HMAC_SIGNING_KEY_ID` from the host secret store.

A required test must fail when its environment is unavailable. It must not skip and leave a green release gate. Profile-selection skips and viewport-selection skips remain allowed when another job or project executes the required case.

## Unit merge gate

For each unit:

1. Run `ddev composer verify:fast` before every commit.
2. Run every additional suite named by the unit plan.
3. Run the Sol unit review.
4. Apply accepted findings.
5. Re-run the affected suites and `ddev composer verify:fast`.
6. Confirm `git status --porcelain` is empty.
7. Merge the unit into `feat/block-theme-fse-migration` without squashing away useful checkpoints.
8. Update this file on the integration branch with status, commit range, and gate results.

### Approved temporary exception — `commerce-e2e`, Units 1 to 3B

Ruled by the Unit 1 `gpt-5.6-sol` review on 2026-08-04 and adopted by the orchestrator.

`commerce-e2e` is NOT a required merge check for Units 1, 2, 3A and 3B. It stays active as an information check for the whole period, and it becomes required again in Unit 4A. Branch protection must match this rule rather than contradict it.

The reason is structural, not tolerance of a broken test. Task 6 deleted the classic rendering path that WooCommerce's classic templates depended on, and the block theme ships no commerce templates — `site-theme/woocommerce/` holds only a README. Unit 4A's Task A3 is the task that derives them. Under the unamended gate, requiring commerce green would have made Units 1 through 3B unmergeable for work none of them owns, and moving Unit 4A earlier would put commerce templates onto a base theme that Units 2 and 3 are still changing, forcing that work to be redone.

Evidence recorded at the time of the ruling: run `30862260835` commerce job, 7 failed and 5 passed, every failure `.woocommerce-order` not found at `tests/commerce/e2e/checkout.spec.ts:81`; the base `e2e` job in the same run succeeded; no commit since `b6c7ce1` touched `tests/commerce/`.

Every unit that merges under this exception MUST record it explicitly in its own merge entry in the log below. The exception expires at Unit 4A and must not be extended silently.

## Final direct-to-main gate

The orchestrator may push to `main` directly. It must not force-push.

Before the push:

1. Fetch `origin`.
2. If `origin/main` changed from the recorded base, integrate it into the integration branch and repeat the full final verification and final Sol review.
3. Complete the Opus review, accepted fixes, and Sol fix review.
4. Run every command in `BLOCK_THEME_PROPOSAL.md` section 12 for the base and commerce profiles.
5. Complete the fresh-clone proof and the isolated-target promotion proof.
6. Confirm all required GitHub Actions jobs pass without a required-test skip.
7. Confirm no bundle, manifest instance, backup payload, secret, `.env`, or customer state is tracked.
8. Confirm `git status --porcelain` is empty.
9. Merge `feat/block-theme-fse-migration` into local `main`.
10. Push with `git push origin main`.
11. Report the pushed SHA and all verification evidence.

## Log

- 2026-08-02: Initial four-plan planning and correction wave completed.
- 2026-08-03: Execution controls corrected. Work is now serialized because all worktrees otherwise share the `agency-starter` DDEV project. Release 3 and Release 4 now use separate branches and status gates. The orchestrator owns tracking. Required CI tests may not silently skip. Final delivery uses a reviewed direct push to `main` without force.
- 2026-08-03: Unit 0 recorded local `main` `18dfa90f8f319b78373ca752faf9cb8f1a097d48` and `origin/main` `c6e0b537c9b60c0f6355428908abf1614e6cc27f`. The five local commits are preserved. A new high-severity Composer advisory made the existing `verify:fast` baseline fail because `wp-coding-standards/wpcs` was locked at 3.4.0. Unit 0 adds a separate security lock update to 3.4.1 before the README and planning-pack commits.
- 2026-08-03: The accidental README text was an uncommitted working-tree insertion. The required one-line repair restored the tracked `HEAD` text exactly, so `git diff -- README.md` is empty. Unit 0 does not create an empty repair commit.
- 2026-08-03: The model policy was corrected after the Codex app displayed lower reasoning on read-only Luna plan audits. Code-writing tasks now use the local Codex CLI with explicit `gpt-5.6-luna` and max reasoning in the exact worktree. Terra high owns bounded reviews and all accepted review fixes. Sol high remains the unit and fix-diff reviewer. The exact Luna max CLI call completed successfully before Unit 1.
- 2026-08-03: Terra high corrected all four implementation plans after the read-only audit wave. The corrections fix unit ownership, commit gates, required-environment failures, deterministic state hashing, provider seams, staged promotion contracts, raw Theme JSON writes, lock release, commerce profile safety, exact fresh-clone inputs, wrapper verification, and the complete Global Styles proof. Sol reviewed the corrected cross-plan controls before the planning-pack commit.
- 2026-08-03: Unit 0 pushed `feat/block-theme-fse-migration` at `92df012d484b6604f82399890826df01e2864b46` through Windows Git Credential Manager and confirmed GitHub Actions read access through the connected GitHub app. This matches the existing Claude Code access path. The execution does not copy the Windows Git credential into a separate CLI store. Task 1 must add agent-runnable push triggers for its two image gates while retaining `workflow_dispatch` for maintainers.
- 2026-08-03: Orchestrator handover. The `gpt-5.6-sol` session stopped after Unit 0. Claude Code Opus 5 is now the orchestrator by user instruction. Unit 0 is confirmed complete and merged. No later unit had authorization from the stopped session, so execution restarts at Unit 1.
- 2026-08-03: Environment re-verified for the new orchestrator. DDEV v1.25.3 runs inside WSL2 Ubuntu, not on Windows; the `agency-starter` project is registered at the `/mnt/c/...` approot of this checkout. Every PHP and Composer command therefore runs as `wsl -d Ubuntu -e bash -lc "cd /mnt/c/Users/Aleksandar/Projects/wordpress-template && ddev composer <script>"`. Docker 29.4.3, Node v24.16.0, and npm 11.16.0 are on Windows. npm lock regeneration still uses `npx -y npm@10 install` because CI runs npm 10.
- 2026-08-03: GitHub access re-verified. `gh` is not installed. The Git Credential Manager token for `aleksandar-djabirski` returns HTTP 200 from the REST API with repository admin permission and carries the `repo` and `workflow` scopes. The orchestrator can therefore start a workflow dispatch, list runs, and download artifacts over REST, and can push a trigger ref with Windows Git. The repository is public and its default branch is `main`.
- 2026-08-03: Baseline gate re-run before any Unit 1 work. `ddev composer verify:fast` passed on `92df012` with deptrac at 0 violations, PHPStan clean, 22 architecture tests and 121 unit tests green.
- 2026-08-03: Task 1 plan corrected for the image gates, per the Required CI inputs rule. `on.push.branches` gains only `ci-capture/**`. `migration-baseline-capture` and the four visual-baseline steps now accept both `workflow_dispatch` and their dedicated capture ref. `e2e` and `commerce-e2e` skip the migration capture ref so the push costs no wasted WordPress boot. The parity step excludes both capture refs. Capture branches are disposable, are never merged, and no job commits a PNG.
- 2026-08-03: Model policy confirmed against the local Codex CLI 0.145.0 model cache. `gpt-5.6-luna`, `gpt-5.6-terra`, and `gpt-5.6-sol` all exist and all support `high`, `xhigh`, and `max`. Their defaults are medium, medium, and low, so every call must set the level explicitly. The bundled Codex plugin helper caps `--effort` at `xhigh` and cannot express `max`. The limit is in the helper script only. The model accepts `max`. The user decided to keep `max`, so bounded implementation uses the already-recorded direct CLI form `codex exec -m gpt-5.6-luna -c 'model_reasoning_effort="max"' -C <worktree>`. This is the same installed Codex toolchain, not a substitute model. A probe run on 2026-08-03 returned the banner `model: gpt-5.6-luna` and `reasoning effort: max`. Every delegated call records that banner as its proof.
- 2026-08-03: Unit 1 starts. Implementation uses `gpt-5.6-luna` at max. Bounded task review and all accepted review fixes use `gpt-5.6-terra` at high. The unit review uses `gpt-5.6-sol` at high. Every delegated call is logged below with its model and level.
- 2026-08-03: Unit 1 worktree created at `C:\Users\Aleksandar\Projects\wt\bt-task-1` on `feat/bt-task-1-theme-and-editing` from integration `b59c099`. The worktree is outside the repository working tree, so Git cannot track it and no ignore entry is needed. Branch, empty status, and integration ancestry verified. The Unit 0 README repair is confirmed present, so the Task 1 Step 2 readiness check passes.
- 2026-08-03: Task 1 Step 3 Phase 0 baseline captured in the worktree. PASS: `ddev composer verify:fast` (22 architecture / 121 unit), `ddev composer verify` (adds 27 integration), `npm run lint`, `npm run build` with no build drift, `npm run test:accessibility` (4 passed, no WCAG 2 A/AA violations). FAIL and diagnosed below: `npm ci`, `npm run test:e2e` at default parallelism, `npm run test:visual`.
- 2026-08-03: Phase 0 finding A — the e2e suite is not safe at high worker counts. `playwright.config.ts` sets `fullyParallel: true` and caps no workers, so a 16-core host runs 16 workers against one WordPress instance. Three `client_editor` logins failed at `tests/e2e/helpers/auth.ts:27` with a `wp-login.php?...&reauth=1` bounce, and the failing cases moved between projects across runs. `npm run test:e2e -- --workers=1` gives 13 passed, 5 skipped, exit 0. This is a pre-existing host-parallelism defect, not a code defect and not a migration blocker. CI hides it because its runner has few cores. Unit 1 does not fix it.
- 2026-08-03: Phase 0 finding B — `npm run test:visual` fails only on `home-mobile.png`, 60725 pixels, ratio 0.18 against `maxDiffPixelRatio: 0.01`. This is the documented Windows condition, not new damage. The committed baselines are Linux-CI-authoritative by plan constraint. The narrow viewport reflows text, so every later element shifts.
- 2026-08-03: Phase 0 finding C — baseline hygiene warning. The DDEV database volume is shared by project name, so it followed the worktree from the main checkout and still holds an earlier `scripts/enable-commerce` run: published `Shop`, `Cart`, `Checkout`, and `My account` pages (IDs 15 to 18), 2 `product` posts, `shop-manager` and `test-customer` users, and the `wp_woocommerce_*` tables. WooCommerce is not installed and `site-commerce` is inactive, so no commerce code runs and the base-profile results stay valid. The primary menu holds only `Home` and `Sample Page`, so the header render is unaffected. The authoritative pre-migration parity baselines come from a fresh CI install in Task 2 and are immune to this residue. The base profile is still not certified from this database; Unit 4B's final base certification must use a clean target.
- 2026-08-03: Unit 1 Task 1 implementation delegated. Model `gpt-5.6-luna`, reasoning level `max`, Codex CLI 0.145.0, session `019fc6fc-5a60-7ba3-aaa5-beb4315a780f`, working root pinned to the task worktree, sandbox `workspace-write`. The banner confirmed `model: gpt-5.6-luna` and `reasoning effort: max`. Luna created only `docs/block-theme-migration-baseline.md` and touched no other file.
- 2026-08-03: Luna returned two plan discrepancies and the orchestrator confirmed both against the working tree. First, `.is-open` appears in `tests/e2e/smoke.spec.ts` only at line 12 inside a comment block; the six other named selectors have executable assertions at lines 19 to 22 and 78 to 88. Second, `tests/Unit/AgencyPlatform/EditorRestrictionsPolicyTest.php` has eight test methods but only six reference `EditorRestrictions::ALLOWED_BLOCKS`; the two privileged-user tests at lines 36 and 45 do not. The Task 1 plan text is wrong on both points and the baseline record deliberately contradicts it. The orchestrator also confirmed two facts Luna added on its own: `ThemeBootstrap.php:57-61` registers the `primary` and `footer` nav locations, and `BlockIndexGenerator::files_containing()` filters on `pathinfo( $file, PATHINFO_EXTENSION )`.
- 2026-08-03: Unit 1 Task 1 bounded review delegated. Model `gpt-5.6-terra`, reasoning level `high`, session `019fc706-b928-7c91-a09e-a91911a6ac18`, same worktree, sandbox `workspace-write`. The banner confirmed `model: gpt-5.6-terra` and `reasoning effort: high`. Verdict: accept with fixes. One important finding — the record omitted the plan's required note that a later engagement task replaces the database-override detection internals; Terra added it. One minor finding — over-long prose, shortened. Terra changed no supplied measurement and no other file. The orchestrator confirmed both fixes and that `git status --porcelain` still lists only the one added file.
- 2026-08-03: Unit 1 Task 1 gate passed and committed. `ddev composer verify:fast` exit 0 in the worktree with deptrac at 0 violations, 22 architecture tests, and 121 unit tests. Commit `0e60df6` on `feat/bt-task-1-theme-and-editing`, worktree clean. Task 1 is complete.
- 2026-08-03: Worker sandbox policy recorded. Task 1 ran under Codex sandbox `workspace-write`, which was sufficient for a documentation task. From Task 2 onward a worker must reach the network for `npx -y npm@10 install` and must shell out to WSL for `ddev`, and neither is possible under `workspace-write` on Windows. Those workers run under `danger-full-access`. The compensating controls are the bounded brief, the pinned working root, the explicit owned-file list, and orchestrator review of every diff before any merge. The user was told of this escalation when it first happened.
- 2026-08-03: Tracking-commit cadence decided. The tracking file is updated on disk after every task, review, fix, gate, and commit, as required. It is COMMITTED at integration checkpoints rather than after each event, because it lives on the integration branch in the main checkout while DDEV is owned by the task worktree, and `ddev composer verify:fast` cannot run from the main checkout while that is true. Committing it on every event would force a DDEV handover each time and would risk the shared project root.
- 2026-08-03: Unit 1 Task 2 Steps 1 to 8 delegated. Model `gpt-5.6-luna`, reasoning level `max`, session `019fc70e-1c50-77b0-ae89-56c07fff3178`, sandbox `danger-full-access`. Commit `40e4c2b`. Luna reported no plan discrepancies. Gates green: `verify:fast` exit 0, `npm run lint` exit 0.
- 2026-08-03: Orchestrator independently verified Task 2A rather than accepting the worker report. `.github/workflows/ci.yml` parses as YAML and declares six jobs. `on.push.branches` is exactly `["main", "ci-capture/**"]`. `e2e` and `commerce-e2e` both carry the migration-capture-ref skip. `migration-baseline-capture` carries both trigger conditions and declares no `needs:`. `package-lock.json` gained exactly three packages: `pixelmatch`, `pngjs`, `@types/pngjs`. An initial worry that the install added roughly 770 packages was wrong — 1004 was a node_modules directory count and 1774 is npm's package count, which are different metrics.
- 2026-08-03: Orchestrator reviewed `tests/parity/helpers/parity.ts` directly because a wrong comparison would silently void every later parity gate. The dimension-mismatch handling is correct: both images are cropped to the overlap, the non-overlapping area is added to the differing-pixel count, and the ratio divides by the LARGER image area, so a taller or shorter page cannot pass by comparing only the overlap. Two suspected defects were tested empirically and both are false alarms. First, `pixelmatch` 6.0.0 is ESM-only (`"type": "module"`), but Node's `require(esm)` sets `__esModule: true` on the namespace, so Babel's `interopRequireDefault` yields the callable default and `import pixelmatch from 'pixelmatch'` works under Playwright's CommonJS transform. Second, `page.evaluate( () => document.fonts.ready )` does not throw; Playwright awaits the promise and serialises `FontFaceSet` to `{}`.
- 2026-08-03: Residual risk recorded against Task 2. The `pixelmatch` interop above depends on `require(esm)`, which is unflagged only from Node 22.12. CI pins `node-version: '22'`, which resolves to the latest 22.x and is therefore safe today. If CI is ever pinned to a 22.0 through 22.11 patch, `compareToBaseline` will fail at call time, and it will fail LATE because nothing calls `pixelmatch` until the Task 10 parity suite runs.
- 2026-08-03: Unit 1 Task 2A bounded review. Model `gpt-5.6-terra`, reasoning level `high`, sandbox `danger-full-access`. Verdict accept, no findings, no new commit. This agrees with the orchestrator's own independent review of `parity.ts`.
- 2026-08-03: Task 2 Step 9 orchestrator gate PASSED. The agent-runnable push trigger works end to end and this is its first live proof. `40e4c2b` was pushed to `refs/heads/ci-capture/migration-baselines`. Run `30804710764` completed with conclusion `success`. Job outcomes confirm the trigger design exactly: `migration-baseline-capture` success, `php-qa` success, `frontend` success, `integration` success, and both `e2e` and `commerce-e2e` SKIPPED by the cost guard. No workflow-dispatch tool was needed.
- 2026-08-03: Step 9 artifact accepted. Artifact `migration-baselines`, 313152 bytes, downloaded over the REST API. It held the four required PNGs, `metadata.json`, and `capture-environment.txt`. `capturedAtCommit` in the capture environment is `40e4c2b84b9133b2c73a8706494fdcfcb7b38b9f`, which matches the pushed SHA. The orchestrator opened and visually inspected all four PNGs as the plan requires. All four show the CLASSIC render — classic header with the site title, the `Home`/`Sample Page` nav on desktop, the hamburger toggle on mobile, and the classic dark footer bar. None shows a block-theme render, so the capture ran on the correct commit. Text rendered in DejaVu, so the parity font stack applied. The four captured values were copied into `metadata.json`, no `TO-BE-FILLED-BY-CAPTURE-RUN` value remains, and `capture-environment.txt` was deliberately not committed.
- 2026-08-03: Risk raised for the Sol unit review, not fixed by the orchestrator. The home-page baselines contain a large blank region. The desktop viewport is 1440x900 and the home content ends near y=330, so roughly 60 percent of the compared image is empty white on both sides. Because `maxDiffRatio` divides by the whole image area, a 0.05 threshold on the home page tolerates a much larger proportional change inside the actual content band. The sample-page captures are nearly full and do not have this weakness. The plan fixes `maxDiffRatio` at 0.05 deliberately and forbids RAISING it, so the orchestrator did not change it unilaterally. Sol should decide whether the home-page gate needs a tighter ratio, a content-bounded crop, or an explicit accepted-limitation note.
- 2026-08-03: Step 9 committed as `7511c37` after `ddev composer verify:fast` exit 0. The disposable capture branch `ci-capture/migration-baselines` was deleted from origin and no `ci-capture/*` ref remains. `feat/bt-task-1-theme-and-editing` is pushed.
- 2026-08-03: Unit 1 Task 2 Steps 10 to 12 delegated. Model `gpt-5.6-luna`, reasoning level `max`, sandbox `danger-full-access`. The brief requires a real Step 11 proof: move a baseline aside, observe the guard fail, restore it, and prove the restore was byte-identical with `git status`. A guard that passes while a baseline is missing is a critical defect.
- 2026-08-03: Unit 1 Task 2 Steps 10 to 12 complete. Commit `80034e0`. `MigrationBaselineGuardTest` adds five tests and the architecture suite went from 22 tests / 130 assertions to 27 tests / 173 assertions. Step 11's proof was really performed: `parity-desktop/home.png` was moved aside, the guard failed with `Declared migration baseline is missing on disk`, the file was restored, and the SHA-256 of all four PNGs and `metadata.json` matched afterwards with an empty `git status`.
- 2026-08-03: Plan-internal inconsistency found by Luna and confirmed by the orchestrator. Task 2 Step 3 authored a capture-spec comment that names `toHaveScreenshot`, and Task 2 Step 10's guard forbids that literal string anywhere in a parity spec. The two plan steps contradict each other. Luna resolved it with a one-line comment reword. The orchestrator verified the edit is comment-only, changes no behaviour, and leaves the four baseline PNGs byte-identical between `7511c37` and `80034e0`. Luna made this change outside its stated owned-file list and reported it after the fact rather than before, which the brief asked it to do; the change itself is correct and minimal, so it was accepted rather than reverted.
- 2026-08-03: Task 2B design question referred to Terra with the trade-off stated rather than decided by the orchestrator. The guard scans raw file contents, so it trips on COMMENTS that merely explain why the forbidden API is avoided. Option (a) keeps the literal scan because it cannot be evaded and a false positive is the safe failure direction for irreplaceable evidence. Option (b) strips comments before scanning so a parity spec can document its own reasoning, at the cost of new code that can itself be wrong. Terra was also asked to check for vacuous passes in guard loops and to test whether the `maxDiffRatio` regex can be defeated by a reformat or a multi-line literal.
- 2026-08-03: Unit 1 Task 2B bounded review. Model `gpt-5.6-terra`, reasoning level `high`. Verdict accept with fixes. One HIGH finding that a reader would not have caught: the five-percent ceiling guard used `/maxDiffRatio:/`, so the form `maxDiffRatio : 0.06` with a space before the colon — which TypeScript permits — bypassed the guard entirely. Terra proved it both ways: before the fix `0.06` passed at 171 assertions, after the fix it failed at the ceiling guard. Fix commit `ba46210` adds `\s*` before the colon in both patterns. The orchestrator verified the diff is exactly that and the baselines stayed byte-identical.
- 2026-08-03: Terra chose option (a) on the comment-scan trade-off, keeping the raw literal scan because it fails closed and a parser would add risk to an evidence guard. Vacuous-pass check returned no empty loop: 10 metadata keys, 4 PNG assertions, 4 ratio literals, and parity spec files that exist. Recorded residual risk: the ratio rule still assumes a direct numeric object property, so a computed value or an object spread could avoid the regex. That is an accepted limitation, not a defect to fix now.
- 2026-08-03: Task 2 complete. Commits `40e4c2b`, `7511c37`, `80034e0`, `ba46210`. `feat/bt-task-1-theme-and-editing` pushed through `ba46210`.
- 2026-08-03: Unit 1 Task 3 delegated. Model `gpt-5.6-luna`, reasoning level `max`, sandbox `danger-full-access`. Task 3 is a 15-step test-first sequence covering the capability policy, the admin-screen boundary, the `edit_theme_options` grant, deletion of `SiteEditorLockdown`, and the one allowed Release 1 change to the commerce capability matrix. The brief states the architecture rules that fail the build rather than leaving the worker to discover them: `AgencyPlatform: []` in deptrac forbids any project-layer dependency, no closures in hooks, the banned directory names, the `agency-platform` text domain, and the WooCommerce symbol ban. The gate is `ddev composer verify`, which includes the database-backed integration suite, and a required test may not become a skip.
- 2026-08-03: PLAN ORDERING DEFECT found at Task 3 and corrected. Luna implemented Task 3 correctly and then REFUSED to commit because Step 14's gate was red, which is the required behaviour. The cause is a false claim in the plan itself. Task 3 Step 12's supplied test carried the docblock line "Task 5 runs before this task and creates the required block-theme environment." The real execution order is Task 3, then Task 4, then Task 5. The two write-back tests send a REST update to `<stylesheet>//index` and `<stylesheet>//site-footer`, and those identifiers resolve only once `templates/index.html` and `parts/site-footer.html` exist, which happens in Task 5's spike and Task 6's conversion. Before then WordPress answers 404 and `wp_is_block_theme()` returns 0. Task 3 therefore could never pass its own gate, which contradicts this plan's rule that no commit lands on a gate the plan already knows is red.
- 2026-08-03: Evidence for the ordering defect. With the classic theme still active, Task 3 produced unit 160 tests / 352 assertions PASS, integration 39 tests / 112 assertions with exactly the two write-back tests failing on 404, architecture 27 / 173 PASS, deptrac 0 violations, `verify:fast` PASS, `verify` FAIL. `wp_is_block_theme()` returned 0.
- 2026-08-03: Correction applied by the orchestrator, who owns plan corrections. The false docblock claim was deleted. The two write-back methods move from Task 3 Step 12 to Task 6 Step 12, which runs after the conversion, and Task 6 Step 12 now carries their full code plus an instruction to confirm `wp_is_block_theme()` returns 1 first. Task 3 keeps the collection-read, navigation-create, and Global Styles tests, because those prove capability wiring rather than block-template resolution and they pass against the classic theme. The orchestrator removed the two methods from the worktree file itself and left a comment there explaining where they went, so a later reader cannot think they were simply dropped. This is orchestrator integration work arising from an orchestrator plan correction, not implementation delegated to Luna.
- 2026-08-03: Environment check after Task 3, FIRST PASS — INCOMPLETE, corrected below. Luna reported that it had run commerce setup, outside its brief. The orchestrator checked plugins and `composer.json`, found WooCommerce absent and `site-commerce` inactive, and wrongly concluded there was no damage. That check missed database state.
- 2026-08-03: CORRECTION to the previous entry. Luna's excursion DID cause damage, and it broke a pre-existing test. `ClientEditorCapabilitiesTest::test_client_shop_manager_role_is_not_registered_in_the_base_profile` exists at `ba46210` and passed at Task 3's baseline. It failed afterwards. Roles are database state, not code state, so a plugin check could never have found this. Luna's `test:integration:commerce` run briefly activated WooCommerce, `ShopRole::register_role()` called `add_role()`, and `client_shop_manager` persisted in the option after deactivation.
- 2026-08-03: The contamination was in the SEPARATE wp-phpunit test database `wordpress_test`, not the site database. Diagnosis path: the site database was cleaned with `ddev wp role delete client_shop_manager` and the suite still failed, which proved the source was elsewhere. `wordpress_test` was found to hold `wptests_wc_*` and `wptests_actionscheduler_*` tables and a `wptests_user_roles` option naming `client_shop_manager`, `shop_manager`, and `customer`. The failing test was CORRECT. This is exactly the invariant this file already states: never certify the base profile from a commerce-enabled database.
- 2026-08-03: Repair. `wordpress_test` was dumped to `/tmp/wordpress_test_backup.sql` inside the web container, then dropped and recreated empty. wp-phpunit reinstalled it cleanly and the base integration suite returned `OK (37 tests, 110 assertions)` with exit 0. No repository file was changed to make this pass, and no test was weakened or skipped.
- 2026-08-03: Finding E, raised for Unit 4A and Unit 4B. The base integration suite is not hermetic against a local test database that a previous commerce run has touched. CI is unaffected because each job builds a fresh database. Locally the database persists, so a developer who runs `test:integration:commerce` and then `test:integration` sees a false failure in the base profile. Two things deserve a decision later: whether the commerce suite should restore the base role set when it finishes, and whether `ShopRole` should remove `client_shop_manager` when WooCommerce is deactivated, because today deactivating WooCommerce leaves an orphan role registered.
- 2026-08-03: Unit 1 Task 3 committed. Plan correction `d3ba84a`, implementation `1ae633e`. Gates at that point: `verify` exit 0, deptrac 0 violations, architecture 27 / 173, unit 160 / 352, integration 37 / 110.
- 2026-08-03: Unit 1 Task 3 bounded security review. Model `gpt-5.6-terra`, reasoning level `high`. Verdict accept with fixes. It found a CRITICAL hole that the orchestrator had asked it to hunt for specifically.
- 2026-08-03: CRITICAL finding and fix, commit `54e2f47`. Granting `edit_theme_options` for the Site Editor also opened the legacy Widgets and Menus REST APIs. WordPress core gates `/wp/v2/widgets`, `/wp/v2/widget-types`, `/wp/v2/sidebars`, `/wp/v2/menus`, `/wp/v2/menu-items`, and `/wp/v2/menu-locations` on exactly that capability. Removing the Appearance menu closed the user interface but left every one of those routes reachable, so a client role could have read and written widgets, sidebars, and classic menus over REST. `AdminScreenPolicy` now denies those route prefixes through `rest_pre_dispatch`, which is the same short-circuit mechanism the plan's Decision 12 specifies for the Global Styles guard. Administrators bypass it through `manage_options`. Prefix matching also catches sub-routes such as `/wp/v2/widgets/123`.
- 2026-08-03: Orchestrator verified the fix rather than accepting the report. The hook is a named class method, not a closure. The text domain is `agency-platform`. The admin bypass is correct. The Site Editor is unaffected because it uses the block-based `/wp/v2/navigation` route, not the classic `/wp/v2/menus` route. Terra also closed a HIGH test gap: a direct `customize.php` request had no server-path test, and there is now an integration test proving it ends in `wp_die` with status 403. Integration grew from 37 / 110 to 46 / 133.
- 2026-08-03: `edit_theme_options` surface recorded for the unit review. Open: Site Editor, templates, template parts, block navigation, Global Styles, and the Font Library. Closed by 403: themes, theme install, theme editor, plugin install, plugin editor, Widgets, Menus, and settings screens. Closed by `customize = do_not_allow`: the Customizer, Header, and Background, with `customize.php` also returning 403. Closed by `edit_css = do_not_allow`: Additional CSS and the `custom_css` aliases. Closed by role capabilities: theme switching, plugin install and activation, file editing, and options management. Recursion check: `map_meta_cap` returns before calling `user_can()` for every capability except `edit_css` and `customize`, and the nested `manage_options` check returns immediately, so no re-entry loop exists and administrators keep the core mapping.
- 2026-08-03: Task 3 fix verified independently by the orchestrator, not accepted on report. `verify` exit 0 and integration 46 tests / 133 assertions. Task 3 complete and pushed through `54e2f47`.
- 2026-08-03: SYSTEMIC PLAN ORDERING DEFECT confirmed and corrected. The Task 3 correction earlier today treated the ordering claim as a one-off. It is not. Task 4 Step 10 carries the same assumption twice, including an explicit "Execution correction" paragraph stating that Task 5 has already created the block-theme files. Luna refused to start Task 4 and reported that `templates/index.html` was absent and `wp_is_block_theme()` returned false, which is exactly the behaviour the brief demanded instead of writing required tests that cannot pass.
- 2026-08-03: The plan settles its own ordering question. Task 5's `Interfaces` block states "Consumes: nothing from Tasks 3–4." Task 5 depends on neither task, and Tasks 3 and 4 both depend on Task 5's `templates/index.html`, `parts/site-header.html`, and `parts/site-footer.html`. The real dependency order is Task 1, Task 2, Task 5, Task 3, Task 4, Task 6. Correction committed as `4df59bb`. Task 3 already shipped at `1ae633e` before this was understood; it needs no rework because its two block-dependent tests were deferred to Task 6, which still creates the files they need.
- 2026-08-03: Second Task 4 clash recorded in the same correction. Step 11 introduces `test_code_editing_is_disabled_and_block_locking_is_enabled_for_client_editor` asserting `canLockBlocks` is true, while the repository still carries `test_code_editing_and_block_locking_are_disabled_for_client_editor` asserting it is false. Task 4 must REPLACE the older test. Two tests asserting opposite values cannot both pass.
- 2026-08-03: Unit 1 Task 5 delegated out of numeric order. Model `gpt-5.6-luna`, reasoning level `max`, sandbox `danger-full-access`. Task 5 is the Phase 0 spike that first makes `wp_is_block_theme()` return 1. Its gates are unusual and the brief says so explicitly: `ddev composer verify` plus the §8.3 checklist, with the Playwright suites deliberately NOT gates because the spike re-renders the home page on purpose. This is one of exactly three commits in the plan that use the narrow browser-gate exception.
- 2026-08-03: Unit 1 Task 5 complete. Commit `d4de578`. `wp_is_block_theme()` moved from 0 to 1. Gates: `verify` exit 0, deptrac 0 violations, architecture 27 / 175, unit 161 / 369, integration 52 / 149, `npm run lint` exit 0. Luna also had to update `docs/generated-block-index.md`, which the plan omitted from Task 5's file list but the freshness test requires.
- 2026-08-03: Coexistence proven by real HTTP responses, not by reading the plan. `/` and `/sample-page/` are block-rendered and carry no classic nav markup. `/hello-world/` still renders through `single.php` and contains the classic `#site-header-nav` markup twice. The spike genuinely coexists with the classic path, which is its entire purpose.
- 2026-08-03: ORCHESTRATOR BRIEF ERROR, recorded because it was the orchestrator's mistake and not the worker's. The Task 5 brief's definition of done required `/sample-page/` to keep rendering classically, reasoning from Decision 13. That was wrong: Task 5 deliberately creates a TEMPORARY `templates/page.html`, so page requests become block-rendered too, and coexistence is demonstrated at `single.php` instead. Luna followed the plan and reported the conflict with the brief rather than silently choosing one. Decision 13's index-only reasoning describes the narrower case, not Task 5's full file list.
- 2026-08-03: Unit 1 Task 5 bounded review. Model `gpt-5.6-terra`, reasoning level `high`. Verdict accept, no functional findings. It independently confirmed the Decision 10 landmark structure against the real rendered HTML: exactly one `<header class="site-header wp-block-template-part">` and one `<footer>`, each with a single `__inner` div and no nested duplication. The orchestrator had already checked the same thing separately and agrees. Terra also regenerated the block index and confirmed it matches.
- 2026-08-03: One cosmetic finding accepted and fixed by the orchestrator as commit `f6eadd9`. `test_global_stylesheet_directory_holds_only_base_and_typography` no longer described its own assertion after `editor.css` joined the allow-list. Terra left it because its brief scoped that file to the constant alone. The plan asks for the rename, the name was inaccurate, and an inaccurate test name misleads the next deliberate addition, so it is now `test_global_stylesheet_directory_holds_only_the_allowed_files`. Gate re-run: architecture 27 / 175 and unit 161 / 369, exit 0.
- 2026-08-03: Unit 1 Task 4 RESTARTED now that the block-theme environment exists. Model `gpt-5.6-luna`, reasoning level `max`, sandbox `danger-full-access`. The brief tells the worker explicitly why it is being restarted, that its earlier refusal was correct, that `wp_is_block_theme()` now returns 1, that its Step 10 Site Editor cases are required with no skip path, and that resolving the `canLockBlocks` clash by replacing the older opposite-asserting test is part of its job.
- 2026-08-03: Unit 1 Task 4 complete. Implementation `d7a2c51`, review fix `5d59a42`. Luna's test-first evidence was real: Step 2 failed with `Class "AgencyPlatform\Editor\BlockPolicy" not found` across 19 errors, Step 6 failed on undefined `EditorRestrictions::ALLOWED_BLOCKS`. Coverage grew from unit 161 / 369 and integration 52 / 149 to unit 184 / 404 and integration 75 / 215. Luna correctly replaced the older opposite `canLockBlocks` assertion rather than leaving two tests contradicting each other.
- 2026-08-03: Task 4 review found a HIGH issue the orchestrator judged before delegating the fix. `d7a2c51` added six `@phpstan-ignore arguments.count` comments to PRODUCTION code, at `GlobalStylesGuard.php:65` and `SaveValidation.php:109,119,137,150,167`. The cause was backwards. WordPress's real `WP_Error::__construct` takes `( $code, $message, $data )` and the production code used it correctly; the incomplete thing was the TEST stub at `tests/support/wp-error-stub.php`, which declared only two parameters. Static analysis had been silenced in shipping code to accommodate a test fixture. That also silences a genuinely wrong argument count added to those lines later, inside the code that decides whether a client's save is rejected. Terra extended the stub to three parameters plus `get_error_data()` and deleted all six suppressions. Verified by the orchestrator: zero `phpstan-ignore` comments remain anywhere under `agency-platform/src/`, and `analyse` reports 0 errors across 93 files.
- 2026-08-03: Second Task 4 judgement referred to Terra rather than decided alone. The block-index freshness scanner matched a literal block name inside `BlockPolicyTest.php`, and Luna dodged it by building the string at runtime — the same shape as the earlier `toHaveScreenshot` comment problem, where code contorts around a scanner. Terra proved the assertion is still real by removing the `agency` namespace and watching the test fail at `BlockPolicyTest.php:69`. It rules the runtime value safe here and recommends Task 6 refine the index relation, explicitly warning that the fix must NOT be to ignore all test files. Recorded for Task 6.
- 2026-08-03: Global Styles coverage confirmed against Decision 12. All four CSS surfaces — root, per-block, per-element, and variation — reach the recursive CSS check. Unit tests prove all four paths; integration tests prove REST rejection and unchanged stored data for root and block CSS. The administrator bypass runs before CSS parsing, so it applies to every surface. Residual item for the unit reviewer: the two coexisting `rest_pre_dispatch` filters are proven to preserve an earlier non-null result, but the real WordPress filter stack ORDER still deserves inspection.
- 2026-08-03: Unit 1 Task 6 complete — the conversion, and the point of no return. Model `gpt-5.6-luna`, reasoning level `max`, sandbox `danger-full-access`. Three commits as Step 18 specifies: `64e9b88` global styles and editor assets, `ea319eb` templates and parts converted to block markup, `e470788` classic path and stale tests removed. Worktree clean.
- 2026-08-03: Conversion verified by the orchestrator against the file system, not the report. Only `functions.php` remains as root theme PHP. `templates/` holds six `.html` files and no `.php`. `parts/` holds `site-header.html` and `site-footer.html` with no part directories. `src/Support/Parts.php` is gone. All four migration baseline PNGs are byte-identical to the Task 2 capture, which now matters more than at any earlier point because they are the only surviving record of the pre-migration design.
- 2026-08-03: Task 6 counts. Architecture rose from 27 / 175 to 41 / 255. Integration rose from 75 / 215 to 78 / 322. Unit FELL from 184 / 404 to 178 / 394, which is correct: `tests/Unit/SiteTheme/PartsTest.php` tested `SiteTheme\Support\Parts`, a class this task deletes. The brief required any falling count to be stated rather than absorbed silently, and Luna stated it. Test-first evidence was real: Step 2 failed with `Required base-profile block template is missing` for `templates/404.html`.
- 2026-08-03: The Task 4 carry-forward is resolved properly. `BlockIndexGenerator::related_tests()` now matches file names, serialised block markup, `blockName` fixtures, and `register_block_type()` declarations, and it scans HTML templates. It no longer matches arbitrary literal text. It did NOT take the shortcut of ignoring all test files, which the Task 4 reviewer explicitly warned against because that would blind the index to real block definitions.
- 2026-08-03: PLAN DEFECT — the browser-gate budget is off by one. Global Constraints allow exactly three commits to skip the browser gates: the Task 5 spike, and Task 6 conversion commits 2 and 3. Task 6 commit 1 changes rendered pixels while the visual baselines still assert classic markup, so it cannot pass either. Four commits need the exception, not three. This is not a consequence of the Task 5 reordering: commit 1 follows the spike in either order. Luna reported the red visual gate instead of editing a snapshot or a browser spec, which is the required behaviour. Raised for the Sol unit review to either widen the stated budget to four or restructure Task 6's first commit.
- 2026-08-03: ORCHESTRATOR PROCESS FAILURE, recorded because the user noticed it before the orchestrator did. The Task 6 Codex process finished its work and wrote its report, but the wrapping shell never exited, so no completion notification fired and the orchestrator waited on a signal that would never arrive. Roughly two hours were lost. The work itself was complete and correct throughout. Corrected practice from here: poll a long-running delegated task directly instead of waiting only on the completion notification, and check the worktree and the report file rather than trusting the absence of a signal.
- 2026-08-03: Unit 1 Task 6 bounded review. Model `gpt-5.6-terra`, reasoning level `high`. Verdict accept with fixes, commit `b6c7ce1`. The review did the specific verification the brief demanded rather than reading the diff. Deletion audit: every deleted path checked against Task 6's file list, all authorised, including the Step 12 deletion of `BlockThemeSpikeTest.php`, and the baseline PNG directory untouched.
- 2026-08-03: Task 6 review rendered every template class and counted landmarks, which is the check that matters after a conversion. `/` 200, `/sample-page/` 200, `/hello-world/` 200, `/2026/07/` 200, `/?s=hello` 200, and a missing URL 404 — each with exactly one `<header>` and one `<footer>`. Decision 10 holds across all six template classes, not only the home page.
- 2026-08-03: MAJOR finding fixed — the posts index shipped with no query pagination at `templates/index.html:12`. A blog index that cannot reach older posts is a real functional regression against the classic path, and no architecture test would have caught it. The fix adds `query-pagination` with previous, numbers, and next.
- 2026-08-03: Minor finding fixed — `frontend-reset.css:18-42` still carried classic-path compatibility rules after the classic path was deleted. Terra proved them dead: `.site-main` belonged to classic markup while the block templates emit `main#site-main`, and the old `.skip-link` rules were superseded by the active `.screen-reader-text` rules. Removed.
- 2026-08-03: The rewritten block-index relation was proven by controlled experiment rather than assertion. Terra added a temporary serialised `<!-- wp:agency/reference-callout /-->` declaration to an unrelated unit test, regenerated the index, and confirmed the block's Tests column named that test; then removed it and confirmed the index returned to its prior output. This closes the risk that a relation could quietly match nothing and leave the generated index permanently and uselessly "fresh".
- 2026-08-03: Task 6 final gates: `verify` pass with architecture 41 / 255, unit 178 / 394, integration 78 / 326; deptrac 0 violations and 0 errors; `npm run lint` pass. Runtime stylesheet proof: the frontend loaded the reset and shared styles, and the editor list contained shared and editor styles only and did NOT contain the reset. Unit 1 pushed through `b6c7ce1`.
- 2026-08-03: Phase 0 finding D — `npm ci` fails on this host with `EUSAGE` and `Missing: @parcel/watcher-<platform>@2.5.6 from lock file`. The host npm is 11.16.0 and the committed lock is correct for npm 10, which is what CI runs. `npx -y npm@10 ci` succeeds with 1004 packages. This is a local toolchain mismatch, not a repository defect. `AGENTS.md` already requires npm 10 to WRITE the lock; the baseline record adds that npm 10 is also required to INSTALL locally when the host npm is a newer major.
- 2026-08-03: Orchestrator handover verified before Task 7. Integration branch at `b59c099`. Task branch `feat/bt-task-1-theme-and-editing` at `b6c7ce1` with an empty status. DDEV `agency-starter` is running with approot `/mnt/c/Users/Aleksandar/Projects/wt/bt-task-1`, so the task worktree owns the project as the execution order requires. The site answers HTTP 200 and serves block markup: `wp-block-template-part` and `site-header__inner` are present and `site-header__toggle` is absent.
- 2026-08-03: BROWSER-GATE PLAN DEFECT confirmed by measurement and corrected as `f1d57da`. The earlier off-by-one entry was itself incomplete. Global Constraints claimed exactly three exception commits AND separately ordered Task 7 to run `test:e2e`, `test:accessibility` and `test:visual` green, while Task 7 Step 5 assigns those suites to Task 9. Six commits really need the exception: the Task 5 spike, Task 6 commits 1, 2 and 3, Task 7, and Task 8. Task 8 was missed by every earlier reading — it seeds navigation and the Demo page, so it changes rendered markup.
- 2026-08-03: Measured evidence for that correction, taken at `b6c7ce1` before any Task 7 change, with `--workers=1` because of Phase 0 finding A. `npm run test:e2e` 4 failed / 9 passed / 5 skipped, with every failure on a selector Task 6 deleted: `.site-header__site-title` at `smoke.spec.ts:20`, `#site-header-nav` at 78, `.site-header__toggle` at 79 and 90. `npm run test:visual` 2 failed / 2 skipped, both home-page snapshots. Desktop now fails as well as mobile, which Phase 0 finding B did not yet show — the conversion moved desktop pixels too, which is expected and is exactly why the visual gate stays exempt until Task 9 recaptures the baselines from Linux CI.
- 2026-08-03: The orchestrator's first reading of this defect was wrong and the measurement corrected it. The handoff and the orchestrator both stated that all three browser suites fail. `npm run test:accessibility` in fact PASSES 4 / 4 at `b6c7ce1`. It runs axe and asserts no classic selector. Accessibility was therefore REMOVED from the exception rather than waived with the other two: from Task 7 onward any commit that changes rendered markup must run it and it must pass. It is the only browser gate that can still catch a landmark or contrast regression while e2e and visual are blind.
- 2026-08-03: `ddev composer verify:fast` re-confirmed the green baseline in the worktree before the correction commit: deptrac 0 violations, architecture 41 / 255, unit 178 / 394.
- 2026-08-03: Unit 1 Task 7 delegated. Model `gpt-5.6-luna`, reasoning level `max`, Codex CLI 0.145.0, session `019fc853-59d1-7253-817e-57fb1c725d3e`, working root pinned to the task worktree, sandbox `danger-full-access`. The banner confirmed `model: gpt-5.6-luna` and `reasoning effort: max`. Task 7 adds five unlocked patterns, the deterministic demo media, the demo page fixture with its `{{MEDIA_ID}}` and `{{MEDIA_URL}}` tokens, and a representative editor preview for the dynamic `agency/reference-callout` block. The brief states the architecture rules that fail the build rather than leaving the worker to find them, names `test:accessibility` as a required gate with its passing baseline, and names `test:e2e` and `test:visual` as explicitly NOT gates with their measured red counts. It forbids any edit to the parity baseline PNGs and to every browser spec and snapshot, because Task 9 owns that rewrite.
- 2026-08-03: PLAN DEFECT at Task 7 and corrected as `8198f57`. Task 7's file list named `blocks/reference-callout/build/index.js` but not `blocks/reference-callout/build/index.asset.php`. Both files are tracked and `npm run build` rewrites the asset file's `version` hash on every bundle change, here from `2336b0e8210ff709dda1` to `68062fdf6188f8e242a6`. Committing one without the other leaves the build drifted, which is exactly what CI's build-drift check compares. Luna rebuilt as instructed, saw an unlisted tracked file change, and STOPPED before committing rather than widening its own scope. That is the behaviour the brief demanded and the file list was wrong, not the worker.
- 2026-08-03: ORCHESTRATOR TOOLING ERROR, recorded because it was the orchestrator's mistake. The first attempt to resume the Luna session passed `-C <worktree>`, which `codex exec resume` does not accept; the working root comes from the shell's current directory instead. The call failed in argument parsing before the model ran, so no work was lost and no gate was affected. The second attempt used `-c 'model="gpt-5.6-luna"'` with the session id and the correct cwd, and the banner confirmed `model: gpt-5.6-luna` and `reasoning effort: max` in the same session `019fc853-59d1-7253-817e-57fb1c725d3e`.
- 2026-08-03: Unit 1 Task 7 committed as `7babd00`, worktree clean, exactly the 13 files the corrected list names. Five patterns register as files, `tests/fixtures/demo-media.png` is a real 1200x675 8-bit RGBA PNG, `tests/fixtures/demo-page.html` keeps both `{{MEDIA_ID}}` and `{{MEDIA_URL}}` tokens intact for Task 8, and `reference-landing-section.php` is untouched so Decision 7 and `locked-pattern.spec.ts` still hold.
- 2026-08-03: Task 7 gates re-run INDEPENDENTLY by the orchestrator rather than accepted from the worker report, and they agree exactly: `ddev composer verify` with architecture 41 tests / 280 assertions, unit 178 / 394, integration 78 / 326, deptrac 0 violations and 0 errors; `npm run lint` exit 0; `npm run test:accessibility` 4 passed. Architecture assertions rose 255 to 280 while the test count held at 41, which is consistent with per-file assertions over the five new pattern files. Luna hit one real lint failure on first run — a Prettier line-length violation in the new `cite` string — fixed the formatting and re-ran rather than committing red.
- 2026-08-03: GUARD GAP found by the orchestrator while reading the architecture suite, referred to the Task 7 review rather than fixed unilaterally. `BlockThemeStructureTest::markup_files()` at line 246 iterates only `/templates` and `/parts`. `test_no_hardcoded_database_refs_in_git_owned_markup` therefore does NOT scan `/patterns`, although a pattern file is equally Git-owned markup and the guard's own rationale — that a `"ref"` binds a database row existing in one environment only — applies to it identically. None of the five new patterns carries a `ref` today, so this is a latent gap and not a live defect.
- 2026-08-03: Unit 1 Task 7 bounded review delegated. Model `gpt-5.6-terra`, reasoning level `high`, session `019fc864-6805-75d1-b86a-ac4bb1f2233a`, same worktree, sandbox `danger-full-access`. The banner confirmed `model: gpt-5.6-terra` and `reasoning effort: high`. The brief tells the reviewer not to repeat the checks the orchestrator already made and points it at what a file read cannot settle: whether the five patterns really REGISTER in the live registry, whether their declared categories exist, whether the hand-authored markup round-trips without an invalid-block fallback, and whether the frontend really emits the class the new editor preview styles.
- 2026-08-03: Unit 1 Task 7 bounded review verdict: accept with fixes, commit `021be87`. It found a MEDIUM defect that every gate had passed over and that no test in this repository can see. `agency/content-page` declares `Categories: pages`, and `pages` is not a WordPress core pattern category. The pattern registered, so nothing failed — it was simply filed under a category that does not exist, which makes it unfindable in the inserter. This is exactly the failure class the brief sent the reviewer to hunt.
- 2026-08-03: The orchestrator verified that finding rather than accepting it. Core registers 21 pattern categories in `web/wp/wp-includes/block-patterns.php` — about, audio, banner, buttons, call-to-action, columns, contact, featured, footer, gallery, header, media, navigation, portfolio, posts, query, services, team, testimonials, text, videos — and `pages` is not among them. Terra registers it on `init` at priority 9; core's `_register_theme_block_patterns` runs on `init` at the default priority 10 (`block-patterns.php:478`), so the ordering is correct. The fix is a named static method with the `site-theme` text domain, so it breaks neither the closure ban nor the text-domain rule.
- 2026-08-03: Live registry queried by the orchestrator, not read from the report. All five Task 7 patterns are registered with their intended titles and categories — `agency/hero` featured, `agency/split-content` text, `agency/feature-grid` featured, `agency/cta` call-to-action, `agency/content-page` pages — alongside the untouched `agency/reference-landing-section`. `WP_Block_Pattern_Categories_Registry` now returns `pages` => `Pages`.
- 2026-08-03: The review also closed the guard gap the orchestrator raised. `BlockThemeStructureTest::markup_files()` now scans `/patterns` as well as `/templates` and `/parts`, so the hard-coded `"ref"` rule covers Git-owned pattern markup. Terra ruled this belongs in Unit 1 because Unit 1 owns that test file.
- 2026-08-03: Review verification the orchestrator accepts as real. Every registered pattern was parsed and round-tripped with balanced block comments and no `core/freeform` fallback. `render.php:53` and `style.css:27` both emit `.reference-callout__testimonial`, so the new editor preview genuinely matches the frontend class and adds only its `--preview` modifier. No inline `style` attribute exists in the five patterns or the fixture, satisfying Decision 5. No wrong text domain. `git diff --quiet 8198f57 7babd00 -- tests/parity` exited 0, so the four irreplaceable parity PNGs are byte-identical.
- 2026-08-03: SECOND BRIEF-COMPLIANCE LAPSE, recorded as a trend rather than a one-off. Terra changed `web/app/themes/site-theme/src/Bootstrap/ThemeBootstrap.php`, outside Task 7's file list, and reported it after the fact although the brief required it to report BEFORE changing. This is the same shape as Luna's earlier `toHaveScreenshot` comment reword. Both changes were correct and minimal so both were kept rather than reverted, but two workers have now widened scope and reported afterwards. Raised for the Sol unit review.
- 2026-08-03: Plan defect from the review accepted and corrected by the orchestrator as `97e49cf`. Task 7's Interfaces block credited Task 7's `scripts/setup` with substituting the `{{MEDIA_ID}}` and `{{MEDIA_URL}}` tokens. Task 7 owns neither `scripts/setup` nor the seeding; Task 8 owns both, and Task 7 Step 5 already said so. The correction also records WHY the raw fixture is deliberately not valid block markup: `"id":{{MEDIA_ID}}` is invalid JSON inside a block attribute, so nothing may parse the fixture before Task 8 substitutes it. Terra confirmed no code parses the raw fixture today and advised against adding a raw-fixture parse guard; Task 8 should instead assert the SEEDED page has no remaining tokens and parses cleanly.
- 2026-08-03: One orchestrator finding of its own referred back to Terra high in the same session. `test_theme_registers_the_pages_pattern_category` asserts that a SOURCE STRING matching `register_block_pattern_category\(\s*'pages'` exists in `ThemeBootstrap.php`. That is a proxy for the behaviour, not the behaviour: it would still pass if the registration moved to a hook that never fires for the block editor or the REST inserter, while the category silently stopped being available. This unit's own standard — which Terra set when it proved the block-index relation by controlled experiment — is that a guard must be shown to FAIL for the right reason. The fix must add a real runtime assertion in the integration suite against the live registries, prove it non-vacuous by breaking the registration and recording the exact failure text, and decide with a stated reason whether the source-scan test survives alongside it.
- 2026-08-03: Task 7 review fix delivered as `8b607b7`. Terra replaced the source-scan proxy with `tests/Integration/Theme/PatternRegistrationTest.php`, which queries the live `WP_Block_Pattern_Categories_Registry` and `WP_Block_Patterns_Registry`. It proved non-vacuity by moving the registration hook to `admin_init` and recording the real failure — "The pages pattern category must register on init for the editor and REST inserter. Failed asserting that null is not null." — then restoring `init`. It REMOVED the proxy rather than keeping both, which is correct: a source scan that cannot fail for the real reason reads as coverage while providing none.
- 2026-08-03: Unit 1 Task 7 COMPLETE. Final gates re-run independently by the orchestrator: deptrac 0 violations and 0 errors, architecture 42 / 287, unit 178 / 394, integration 80 / 338, `npm run lint` exit 0, `npm run test:accessibility` 4 passed. Worktree clean. Task 7 commits `7babd00`, `021be87`, `8b607b7`; plan corrections `f1d57da`, `8198f57`, `97e49cf`.
- 2026-08-03: Two more plan defects corrected before Task 8 as `6b96f23`. First, Task 8's Interfaces block credited Task 6 with the fixtures Task 7 built — the third instance in this unit of a task naming the wrong producer for an input. Second and more damaging, the CI step comment claimed a ref-less navigation block "resolves unambiguously only when exactly one `wp_navigation` post exists". That is the superseded claim Task 8 exists to correct; Task 8's own preamble and Decision 14 both state the real rule. Left in place it would have taught the next reader the opposite of what the code does and would have justified writing a wrong "assert exactly one record" test. The `scripts/setup` comment already had it right; only the CI copy carried the old wording.
- 2026-08-03: Unit 1 Task 8 delegated. Model `gpt-5.6-luna`, reasoning level `max`, session `019fc886-d467-7923-aecd-29ec679ca918`, sandbox `danger-full-access`. Committed as `49fb1ba`, exactly the two owned files `scripts/setup` and `.github/workflows/ci.yml`. The classic-menu scan returns zero matches across `scripts`, `.github/workflows` and the theme, which is the central proof of the task: a surviving `wp menu location assign` fails every fresh bootstrap on a block theme. Luna also found and replaced a THIRD classic seed the plan never mentioned, in the migration-baseline job, and reported it.
- 2026-08-03: PLAN DEFECT in the Task 8 fresh-install proof, found by the orchestrator while reading the worker report, corrected as `beff0d3`. Step 5 configured the disposable proof project with `--docroot=web/wp`. This repository is Bedrock and its tracked `.ddev/config.yaml:12` declares `docroot: web`. The correction also added the missing proof hostname and the WSL invocation note.
- 2026-08-03: Unit 1 Task 8 bounded review. Model `gpt-5.6-terra`, reasoning level `high`, session `019fc8b4-a278-7602-bd5e-8b6678d8a705`. Verdict REJECT — the first rejection in this engagement. The docroot ruling is decisive and the orchestrator confirmed it against the source: `config/application.php:101-103` defines `CONTENT_DIR` as `/app`, `WP_CONTENT_DIR` as `$webroot_dir . /app`, and `WP_CONTENT_URL` as `WP_HOME . /app`. With docroot `web/wp`, every `/app/...` request resolves to `web/wp/app/...`, which does not exist, so theme CSS, block build JS and the imported demo media all 404. `/` and `/demo/` can still return 200 with similar HTML, which is exactly why the worker's "the image rendered" claim looked convincing and was not valid for the shipped configuration. This is the same silent failure class as the Task 7 `pages` category.
- 2026-08-03: Terra's other Task 8 verification passed and the orchestrator accepts it. Live `/` returns 200 with rendered `wp-block-navigation` links for Home and Sample Page in the header, which proves the seeded record actually RESOLVES rather than merely existing. Setup idempotence re-proven independently, printing both skip lines and leaving exactly one published `Primary` navigation and one published `demo` page with `/%postname%/`. CI YAML parses with six jobs and no WooCommerce symbol leaked outside the commerce job. Step labels read `[9/11]`, `[10/11]`, `[11/11]` with no stale label. Terra ruled `.env.example:8` correct as shipped — it matches the tracked DDEV project name, and a disposable proof with a different name must override it — so no Unit 1 change is needed.
- 2026-08-03: SECOND ORCHESTRATOR ERROR in the same area, recorded because it was mine and it was wrong in the fix itself. `beff0d3` told the proof to `sed` `WP_HOME` in `.env` BEFORE running `scripts/setup`. That cannot work: `.env` does not exist yet. Step 2 of `scripts/setup` creates it from `.env.example` and leaves any existing `.env` untouched, so there is no window to set the host before the first run without losing the generated salts. Corrected as `242e14d`: run setup once, fix `WP_HOME` (`WP_SITEURL` is `${WP_HOME}/wp` and follows it), reset only the disposable database, then run setup again as the real proof, and a third time for idempotence.
- 2026-08-03: The orchestrator is re-running the Task 8 fresh-install proof itself rather than delegating it, because a proof is a gate and the orchestrator owns gates, and because the previous proof environment was destroyed before its defect was understood. Disposable worktree created at `C:\tmp\bt-task-1-setup-proof` from `242e14d`; `agency-starter` stopped cleanly to release the shared project; proof project configured with `name: bt-task-1-setup-proof` and `docroot: web`, both verified in its own `.ddev/config.yaml` before starting.
- 2026-08-03: The proof project first refused to start with `mkdir /mnt/c/tmp/bt-task-1-setup-proof/.ddev/db_snapshots: file exists`, while both `ls` inside WSL and `Test-Path` on Windows reported the path ABSENT. This is a stale drvfs metadata entry left by the previous proof run at the same path, and DDEV had already warned that the project sits on the Windows filesystem. Creating the directory from the Windows side cleared it and `ddev start` then succeeded. Recorded because the next proof at a reused path will hit it again.
- 2026-08-04: TASK 8 FRESH-INSTALL PROOF RE-RUN AND PASSED under the corrected docroot. The decisive evidence is the four `/app/...` URLs that the wrong docroot would have broken: `frontend-reset.css` HTTP 200 / 1434 bytes, `shared.css` 200 / 2707, `demo-media.png` 200 / 4390, `reference-callout/build/index.js` 200 / 2112. The demo image is 4390 bytes, byte-identical to the fixture committed in `7babd00`, which proves the media import worked end to end. Under `docroot: web/wp` every one of these would have 404'd while `/` and `/demo/` still returned 200 — which is exactly how the first proof passed while proving nothing about assets.
- 2026-08-04: Remaining Task 8 proof checks, all run by the orchestrator. `/` 200 with `wp-block-navigation` rendering a `Home` link, a post template and a footer; `/demo/` 200 with the image and the reference callout and ZERO unresolved `{{` tokens in the seeded content; exactly one published `Primary` navigation record (ID 4) and one `demo` page (ID 6); permalink structure `/%postname%/`; a second `scripts/setup` printed both skip lines and changed no IDs; the classic-menu scan returned no matches inside the proof tree. Task 8 therefore STANDS — its code was never in question, only its proof.
- 2026-08-04: Proof environment torn down cleanly. The proof project was stopped and unlisted, its `.ddev/config.yaml` reverted, and the worktree removed with `git worktree remove --force` because DDEV had written untracked files into `.ddev/`. The force applied only to the disposable proof tree, never to the task worktree or the main checkout. `git worktree list` no longer names it, the path is gone, and `agency-starter` is running again from the task worktree and answering HTTP 200.
- 2026-08-04: Unit 1 Task 9 delegated — the task that ENDS the six-commit browser-gate exception. Model `gpt-5.6-luna`, reasoning level `max`, session `019fc974-d561-7a73-bac6-65bc5a8e00e5`, sandbox `danger-full-access`. The brief states plainly why this task carries more weight than the others: everything rendered since the Task 5 spike is unverified by any browser suite, so Task 9 closes over all of it at once, and a passing spec that does not really assert anything is the worst possible outcome. It also makes the Windows-baseline ban the loudest rule in the brief, because a single `--update-snapshots` on this host would bake this machine's font rendering into the gate permanently and silently.
- 2026-08-04: Unit 1 Task 9 committed as `e7f7f32` — exactly the eight owned files, 680 insertions, worktree clean, and NO PNG. `tests/visual/__screenshots__/` was never touched, which held through a long iterate-to-green grind in which reaching for `--update-snapshots` would have silenced the noise. The classic selectors are gone: `.site-header__site-title` becomes `.site-header__branding .wp-block-site-title`, `#site-header-nav` becomes `header.site-header nav.wp-block-navigation`, `.site-header__toggle` becomes `.wp-block-navigation__responsive-container-open`, and the `aria-expanded` toggle assertion becomes the overlay's `is-menu-open` class.
- 2026-08-04: Task 9 added three new specs — `site-editor.spec.ts` (244 lines), `client-workflow.spec.ts` (252 lines) and `forbidden-admin-screens.spec.ts` (53 lines) — plus `/demo/` coverage in both the accessibility and visual suites. The client-workflow spec drives real actions as `client_editor` and SAVES, on the stated reasoning that a visible panel is not proof of the client's ability to complete the task, and it restores template parts, templates and Global Styles over REST afterwards so it leaves no database state behind.
- 2026-08-04: DOCUMENTATION REGRESSION raised for the Task 9 review rather than fixed by the orchestrator. `tests/e2e/smoke.spec.ts` deleted the comment explaining WHY the internal-link check compares ORIGINS instead of string-prefixing — that prefix matching would classify `https://base.evil.com` as internal to `https://base.com` — and replaced it with a bland one-liner. The code still compares origins correctly, so this is documentation loss and not a behaviour change, but it is precisely the kind of security reasoning that gets re-broken once nobody remembers why it was written that way.
- 2026-08-04: FORBIDDEN SKIP found by the orchestrator during a hand-run skip audit of Task 9. `tests/accessibility/smoke.spec.ts:41` carried `test.skip( true, '/sample-page/ returned 404 ... skipping.' )`. It is PRE-EXISTING — `e7f7f32` only appended the demo test below it — but it directly violates this file's rule that a required test must FAIL when its environment is unavailable rather than skip and leave a green release gate. It is neither a profile-selection nor a viewport-selection skip, which are the only permitted forms. It matters more after the migration, not less: `/sample-page/` is one of only TWO pages covered by migration parity under Decision 6, so if it ever disappeared, parity would fail loudly while the accessibility suite reported green. The new `/demo/` test eight lines below already did the right thing with a hard 200 assertion, so the file carried the correct and the forbidden pattern side by side.
- 2026-08-04: Task 9 e2e and accessibility gates verified independently by the orchestrator, not accepted from a report. e2e moved from 4 failed / 9 passed / 5 skipped to 0 failed / 42 passed / 32 skipped. Accessibility moved from 4 passed to 6 passed. A separate `--project=chromium-desktop` run returned 36 passed / 1 skipped, which proves the 32 skips are pure viewport selection and that the desktop-only specs really execute somewhere rather than skipping in every project.
- 2026-08-04: Non-vacuity evidence the orchestrator considers the strongest signal in Task 9. `forbidden-admin-screens.spec.ts` ships CONTROL tests — "an administrator reaches themes.php normally" and "an administrator sees the code editor option". A spec that asserts only that a client CANNOT reach a screen passes just as happily when the screen is broken for everyone; the controls prove the denial is role-specific rather than universal breakage. Luna added them without being asked.
- 2026-08-04: Unit 1 Task 9 bounded review. Model `gpt-5.6-terra`, reasoning level `high`. Verdict accept with fixes, commit `dabda2b`. The forbidden skip is replaced by `expect( response?.status() ).toBe( 200 )`, matching the demo test. The deleted security rationale is restored and is sharper than the original: "Compare URL origins. String-prefix matching would treat https://base.evil.com as internal to https://base.com."
- 2026-08-04: Two Task 9 review improvements the orchestrator did not ask for and rates higher than the ones it did. First, every `any` is gone: proper `WpEditor` and `EditorBlock` types now live in `tests/e2e/helpers/wp.ts` with overloaded `dispatch`/`select` signatures per store. This matters because an `any` in an assertion path can silently swallow a mistyped store name and let a test pass against nothing. Second, `expect( clientId ).toBeTruthy()` became an explicit `throw`, which actually narrows the type instead of only asserting at runtime.
- 2026-08-04: Decision 10 is now genuinely enforced in the browser suites. `client-workflow.spec.ts:89` and `site-editor.spec.ts:113` both assert `.site-header__inner` is visible in the part-only canvas. That is the correct selector: `header.site-header` exists on the frontend and in a page/template canvas, but NOT when a client edits the part alone, so without this assertion those tests could have passed against an empty canvas. Selectors were confirmed against the live DOM rather than the plan text.
- 2026-08-04: Unit 1 Task 9 COMPLETE. Final gates re-run by the orchestrator after the fix: e2e 42 passed / 32 skipped / 0 failed, accessibility 6 passed, `npm run lint` exit 0, deptrac 0 violations and 0 errors, architecture 42 / 287, unit 178 / 394, integration 80 / 338. Worktree clean. The six-commit browser-gate exception is closed on the e2e and accessibility side; only the visual baselines remain.
- 2026-08-04: Visual baseline capture started as an orchestrator gate. `feat/bt-task-1-theme-and-editing` pushed through `dabda2b`, then `dabda2b` pushed to `refs/heads/ci-capture/visual-baselines`, which is the agent-runnable trigger and needed no workflow-dispatch tool. Run `30862260835` created for `dabda2b`. The capture ref is disposable, is never merged, and is deleted once the artifact is accepted. Every PNG must be opened and inspected by eye before it is committed, because these images become the permanent reference for the visual gate and a wrong baseline is invisible to every test.
- 2026-08-04: VISUAL BASELINES CAPTURED, INSPECTED AND COMMITTED as `89d1cac`. CI run `30862260835` on `dabda2b` produced the `migration`-free `visual-baselines` artifact, 361888 bytes, four PNGs. The orchestrator opened and visually inspected every one before committing, as the plan requires. `home-desktop`: block header with the site title, the seeded Home / Sample Page navigation, the posts list and the dark footer. `home-mobile`: the same with core's navigation overlay toggle. `demo-desktop`: every section of the Phase 1 demonstration page — hero, split content, the solid primary-colour media, the three-column feature grid, the reference-callout dynamic block, and the closing call to action. `demo-mobile`: the same with columns collapsed to one. This is the first end-to-end sighting of the demonstration page and it validates Task 7's fixture and patterns, Task 8's seeding running inside CI, and Task 9's specs in a single artifact.
- 2026-08-04: The old `home-desktop.png` and `home-mobile.png` photographed the CLASSIC theme and were replaced, because the frontend they recorded no longer exists. `git diff --stat b6c7ce1 HEAD -- tests/parity` is EMPTY, so the immutable pre-migration record in `tests/parity/baselines` is untouched. The capture ref was deleted from origin after the artifact was accepted.
- 2026-08-04: `npm run test:visual` FAILS locally on Windows against the new baselines — 3 failed, 1 passed, 4 skipped. This is expected and is not a defect. The baselines are Linux-CI-authoritative by Global Constraint, this host rasterises fonts differently, and that difference is exactly why generating a baseline here is banned. CI is the authority, so a verification run was pushed rather than judging the baselines from the local result.
- 2026-08-04: Verification technique recorded for reuse. Pushing a capture ref whose name is NOT `ci-capture/visual-baselines` — here `ci-capture/verify-unit1` — runs the visual suite AGAINST the committed baselines instead of regenerating them, because the regeneration step is gated on that exact ref or on the `workflow_dispatch` input. Run `30863038336` on `89d1cac`. Pushing the feature branch itself triggers nothing, since `on.push.branches` is only `main` and `ci-capture/**`.
- 2026-08-04: CROSS-UNIT CONFLICT raised for the Sol unit review, deliberately NOT resolved by the orchestrator because it changes the merge criteria for four units. The commerce job FAILED in run `30862260835`: 7 failed / 5 passed, every failure `.woocommerce-order` not found at `tests/commerce/e2e/checkout.spec.ts:81`, the order-received page. The base `e2e` job in the same run SUCCEEDED, so this is not a base-profile problem and not a Task 8 regression — Task 8's commerce changes only added navigation and Demo seeding, which cannot affect order display.
- 2026-08-04: Analysis of that conflict. This is the FIRST time the commerce suite has ever executed against the block theme; the earlier capture run `30804710764` skipped it through the migration-ref cost guard. The classic `.woocommerce-order` markup came from WooCommerce's classic templates, which Task 6 deleted, so the failure is the scheduled consequence of the conversion and Unit 4A owns the repair — it owns commerce templates, commerce patterns and commerce tests. The problem is that this file's Unit merge gate says to "confirm required GitHub Actions jobs are green" before merging a unit, and commerce stays red from Task 6 until Unit 4A. Under a literal reading, Units 1, 2, 3A and 3B can none of them merge. The two rules were never reconciled. Sol must decide: exempt `commerce-e2e` from the merge gate until Unit 4A with the exemption recorded per unit, or move Unit 4A earlier, or gate the commerce job off until 4A re-greens it. The orchestrator's recommendation is the first, because moving 4A earlier would force commerce work onto templates that Units 2 and 3 are still changing.
- 2026-08-04: A CI cost observation for Task 11 or 12, not a correctness defect. The `ci-capture/visual-baselines` ref pays for a full WooCommerce commerce boot that contributes nothing to the artifact. The migration capture ref has an explicit cost guard skipping `e2e` and `commerce-e2e`; the visual capture ref has no equivalent and cannot skip `e2e`, because that job performs the regeneration. The guard should skip only `commerce-e2e` on the visual capture ref.
- 2026-08-04: Unit 1 Task 10 delegated, then REFUSED to commit because its own gate was red. Model `gpt-5.6-luna`, reasoning level `max`. Both causes were PLAN defects, not code defects, and the refusal was the required behaviour for the fifth time in this unit.
- 2026-08-04: MAJOR PLAN DEFECT — the parity gate was unpassable by construction, corrected as `df68860`. Task 10 Step 5 told the worker to run `npm run test:parity` locally and expect PASS. Parity is Linux-CI-authoritative for exactly the reason fixed Decision 8 already gives: determinism comes from `ubuntu-24.04` plus `fonts-dejavu-core` plus an injected DejaVu stack. `PARITY_FONT_CSS` forces `"DejaVu Sans", sans-serif !important`, and this Windows host has NO DejaVu installed, so the browser silently falls back to its own sans-serif. Different glyph metrics reflow every line and shift everything below.
- 2026-08-04: The orchestrator TESTED the worker's stated cause rather than accepting it, and the worker's hypothesis was wrong. Luna attributed the failure to a site-title mismatch. Setting `blogname` to the baseline capture value and re-running moved the ratios by less than 0.3 percentage points — 7.28 to 7.26, 18.15 to 18.14, 20.17 to 20.41, 35.56 to 35.69 — which rules out site text and leaves font fallback as the explanation. The local red ratios are therefore NOT evidence of a migration regression.
- 2026-08-04: THIRD environment mismatch found by the orchestrator, which the worker had not seen. Three different site titles are installed by three different environments: `Agency Starter Baseline Capture` at `.github/workflows/ci.yml:518` (the capture job), `Agency Starter CI` at line 218 (the e2e job), and `Agency Starter (local)` at `scripts/setup:142`. The site title is rendered text in the header of every captured page, so the comparing environment must install the same title the baseline used. `metadata.json` recorded viewport, browser, font stack and font package but NOT the site title, and that omission is what let the mismatch pass unnoticed. Task 10 now adds `siteTitle` to the metadata and sets `blogname` in the parity CI job.
- 2026-08-04: DECISION 10 CORRECTED in the same commit. It claimed `header.site-header` exists in a "page/template" canvas. In WordPress 7.0, `wp-admin/site-editor.php:144-149` sets `$context_settings['post']` from the `/page/(\d+)` route, which makes `/page/{id}` a POST editing context rendering content only — no header, no footer, no `main`. The orchestrator confirmed this in core rather than accepting the report. The editing-parity run had already proven it by failing before any pixel comparison because `header.site-header` was absent.
- 2026-08-04: Unit 1 Task 10 COMPLETE after the corrections, commit `fc2b811`. The worker chose to compare CONTENT REGIONS — frontend `main#site-main` against the editor's `.editor-styles-wrapper` — rather than driving the template route, and asserts no template chrome inside a `/page/{id}` canvas. Verified by the orchestrator: the baseline PNGs are byte-unchanged and the only metadata change is the added `siteTitle` line; the sole `maxDiffRatio` in the tree is `0.05`.
- 2026-08-04: RESIDUAL RISK raised for the Sol unit review. The editing-parity diagnostic ratios are 69 to 89 percent, far above migration parity's 7 to 36 percent. Font fallback explains much of the migration numbers but probably not 89 percent. The editor canvas carries its own padding and chrome, so the comparison of `main#site-main` against `.editor-styles-wrapper` may be structurally mismatched rather than merely mis-thresholded. CI is the authority for the numbers; Sol must judge the METHOD.
- 2026-08-04: Unit 1 Task 11 COMPLETE, commit `d0da3f3`. `wp agency check-overrides` now reports drift instead of failing on it, which is correct under the block-theme editing model — a client saving a template in the Site Editor writes exactly the rows the command used to call a breach. Both new helpers are pure and take the report by value, so when the state track later swaps in its own differ only the line building `$report` changes; `drift_is_failure()` is a single expression. `DatabaseOverrideClassifyTest.php` verified untouched with a clean working-tree diff, its thirteen methods intact.
- 2026-08-04: Unit 1 Task 12 COMPLETE, commit `edbe0ab` — the final task of the unit. Six documentation files corrected: `AGENTS.md`, `docs/architecture.md`, `docs/editing-strictness.md`, `docs/ownership-rules.md`, `docs/adding-a-block.md`, `docs/validation-scenarios.md`. Every quoted claim the plan listed was found and replaced; the worker reported no NOT FOUND entries. `README.md` is confirmed untouched by the whole unit — `git diff --stat b59c099 HEAD -- README.md` is empty — because Unit 4B owns it.
- 2026-08-04: README HANDOFF TO UNIT 4B, recorded verbatim so it cannot be lost. (1) Line 3: "a hybrid block/classic theme" becomes "a native block theme with full Site Editor access for clients". (2) The "Does it come with a theme?" answer keeps its framing but says the layouts are block templates (`templates/*.html`) the client can edit visually. (3) The playbook's step 2: "Header/footer → `parts/`" becomes "Header/footer → `parts/site-header.html` and `parts/site-footer.html`; shared visual rules → `assets/global/shared.css`". (4) The trade-off paragraph replaces "customers edit text, images, and products and compose pages from an approved set of blocks while structure, styles, and templates stay locked" with "customers edit the whole site visually — templates, header, footer, navigation, colours and typography — from an approved set of blocks, while the code editor, raw HTML, shortcodes and Additional CSS stay closed and every change is testable against Git". (5) Project layout: "Hybrid theme: templates, parts, blocks, patterns, tokens" becomes "Block theme: HTML templates and parts, blocks, patterns, tokens". (6) Recount and update every test-count figure — "the 22 architecture tests", "Architecture (22 tests)", "Unit (121 tests)", "Integration (27 tests)" — from a real `ddev composer verify` run at the time.
- 2026-08-04: ALL TWELVE UNIT 1 TASKS COMPLETE. Task commits in execution order: `0e60df6`; `40e4c2b` `7511c37` `80034e0` `ba46210`; `1ae633e` `54e2f47`; `d4de578` `f6eadd9`; `d7a2c51` `5d59a42`; `64e9b88` `ea319eb` `e470788` `b6c7ce1`; `7babd00` `021be87` `8b607b7`; `49fb1ba`; `e7f7f32` `dabda2b`; `89d1cac`; `fc2b811`; `d0da3f3`; `edbe0ab`. Plan corrections: `d3ba84a` `4df59bb` `f1d57da` `8198f57` `97e49cf` `6b96f23` `beff0d3` `242e14d` `a75ec52` `df68860`. Sixteen plan defects were found and corrected across the unit.
- 2026-08-04: Unit 1 whole-unit review delegated. Model `gpt-5.6-sol`, reasoning level `high`, session `019fcc2c-3fbe-73d0-b077-cf636777a35b`, sandbox `danger-full-access`. The banner confirmed `model: gpt-5.6-sol` and `reasoning effort: high`. The brief states the six deferred items as RULINGS the reviewer must make rather than observations to note, because each one has been deferred to "the unit review" by an earlier task and none can be settled by a per-task view.
- 2026-08-04: UNIT 1 WHOLE-UNIT REVIEW COMPLETE. Model `gpt-5.6-sol`, reasoning level `high`. Verdict: DO NOT MERGE. The main gates pass, but seven important findings and six rulings must be settled first. The review earned its place by finding things no per-task review could see.
- 2026-08-04: Sol ruling 1 — commerce. Approve a temporary exemption for Units 1 to 3B, keep the job as an information check, restore it as required in Unit 4A, and align branch protection. This matches the orchestrator's recommendation and is now recorded above as an approved exception with its evidence and expiry.
- 2026-08-04: Sol ruling 2 — home parity. REJECTS Task 10's accepted limitation and requires a tighter ratio of about `0.02`, with no crop. Its arithmetic: the desktop content occupies roughly 330 of 900 pixels, so a 5 percent whole-image limit permits about 13.6 percent change inside the content band, while 2 percent gives about 5.5 percent. A crop was rejected because it could hide added whitespace or a moved footer, which is exactly what the gate exists to catch. The orchestrator accepts this over Task 10's position.
- 2026-08-04: Sol ruling 3 — the ratio ceiling guard is NOT acceptable. `MigrationBaselineGuardTest.php:99` resolves ratios by regex, so a computed value or an object spread is invisible and line 108 continues without failing. The test can pass while checking nothing. Required fix: machine-readable page settings or a real parse, an assertion on the exact page count, and a failure on any value that is not a direct numeric literal.
- 2026-08-04: Sol ruling 4 — REST filter order ACCEPTED, and settled by evidence rather than reasoning. The live filter list returns both `AdminScreenPolicy::block_legacy_design_rest_routes` and `GlobalStylesGuard::guard_global_styles_write` at priority 10. `AdminScreenPolicy` ignores Global Styles routes and `GlobalStylesGuard` preserves every earlier non-null result, so the order is safe. This closes an item open since Task 4.
- 2026-08-04: Sol ruling 5 — editing parity is NOT structurally sound, confirming the orchestrator's suspicion and sharpening its cause. `templates/page.html:3` puts `core/post-title` AND `core/post-content` inside `main`, while the `/page/{id}` editor route renders post content only, and the two roots carry different layout duties. The suites are comparing different regions, which is why the diagnostic ratios reached 89 percent. This is a method defect, not a threshold defect.
- 2026-08-04: Sol ruling 6 — commerce test-state hermeticity belongs to Unit 4A, NOT Unit 4B as the orchestrator had provisionally routed it. Unit 4A must restore the base role set after its integration suite or use a separate test database, and add a sequence check that runs commerce and then base verification. `ShopRole.php:80` should remove `client_shop_manager` when WooCommerce is inactive, which makes its "only when active" contract true and leaves a safe closed state after deactivation.
- 2026-08-04: Sol finding 4 — the Task 5 Phase 0 spike proof used `--docroot=web/wp`, the same defect later corrected for Task 8. Sol asks for the spike proof to be repeated for `d4de578` in a disposable worktree under WSL2 with `docroot: web`. Recorded as an orchestrator gate.
- 2026-08-04: Sol finding 6 — the authoritative Unit 1 record appeared absent, the file ending at "Unit 1 starts" with 141 lines. The diagnosis is partly wrong and worth recording precisely: the record exists in full on disk in the MAIN checkout, and is simply not committed into the task worktree, which follows the tracking-commit cadence decided on 2026-08-03 — the file is committed at integration checkpoints because it lives on the integration branch while DDEV is owned by the task worktree. The requirement is nevertheless real and is satisfied by committing the record at the merge.
- 2026-08-04: Sol invariant checks, all independently run and all passing. The four baseline PNG blob IDs are unchanged since capture commit `7511c37` and only `metadata.json` changed later. Root theme PHP is `functions.php` alone, at 24 lines. Zero `templates/*.php`. No classic navigation or rendering calls survive — the only match was a comment naming `wp_nav_menu()`. No direct numeric `maxDiffRatio` above `0.05`. No `toHaveScreenshot` under `tests/parity/`. No required release case was reduced to an environment skip. The block-index generator exits 0 and its output matches. Final `git status --porcelain` empty.
- 2026-08-04: Unit 1 accepted-fix wave delegated. Model `gpt-5.6-terra`, reasoning level `high`, sandbox `danger-full-access`. Scope: the editing-parity region mismatch, the fail-open ratio guard with a required break-and-restore proof, the home ratio at `0.02`, the incomplete baseline metadata validation, the `AdminScreenPolicy` docblock that contradicts its own code, and a test that fails if `content-page.php` drifts from the four component patterns it deliberately copies.
- 2026-08-04: Unit 1 accepted fixes complete, commit `9c9d029`. All six findings addressed and verified by the orchestrator against the tree: no PNG changed, the home ratio is now `0.02` with the sample page at `0.05`, and nothing exceeds `0.05` anywhere. Architecture rose 42 to 44.
- 2026-08-04: The fail-open ratio guard was fixed with a REAL break-and-restore proof, which is the standard this unit has held throughout. Terra replaced a literal with a computed value and the guard failed with "Expected 4 declared parity pages and 4 complete entries with direct numeric maxDiffRatio literals, but found 4 declared pages and 3 direct entries. A computed value or object spread can bypass the ratio ceiling." The literal was restored and the guard passed. The guard now counts declared pages against resolved literal entries, so a page it cannot resolve is a FAILURE rather than a silent pass — which is exactly what the old regex allowed.
- 2026-08-04: Unit 1 final gates re-run independently by the orchestrator after the fixes: deptrac 0 violations and 0 errors, architecture 44 / 403, unit 182 / 402, integration 80 / 338, e2e 42 passed / 32 skipped / 0 failed, accessibility 6 passed, `npm run lint` exit 0.
- 2026-08-04: Sol finding 4 DISCHARGED. The Task 5 spike proof was re-run at `d4de578` in a disposable worktree with the corrected `docroot: web`, and the plan text was corrected first as `5892aad`. Results: `wp_is_block_theme()` returns 1; `/` and `/sample-page/` return 200 and are block-rendered with zero classic-nav markup and two template-part markers each; `/hello-world/` returns 200 and is still CLASSIC with one classic-nav marker and zero block parts. So the spike's coexistence claim holds exactly as originally recorded.
- 2026-08-04: The asset half of that proof — the part the wrong docroot would have hidden — also passes. Every `/app/...` URL referenced by the page returns HTTP 200 with real bytes: `base.css` 1411, `typography.css` 764, `site-footer.css` 798, `site-header.css` 2145. The referenced asset set is the PRE-conversion one, including the classic `parts/site-header/site-header.css` and `.js`, which independently confirms the proof really ran against the spike commit and not against later code.
- 2026-08-04: The orchestrator's prediction for that proof was correct — a wrong docroot changes asset resolution, not template resolution, so the spike's template-level claims were never at risk. It was still run rather than argued, because twice in this unit a proof that "obviously" held did not, and dismissing a reviewer's Important finding by reasoning is the failure this process exists to prevent.
- 2026-08-04: The stale drvfs `db_snapshots` failure reproduced exactly as recorded for the Task 8 proof, and the recorded remedy — creating the directory from the Windows side — cleared it. Worth noting that writing the cause into the plan, rather than only changing the flag, paid for itself within one task.
- 2026-08-04: Spike proof environment torn down: project stopped and unlisted, `.ddev/config.yaml` reverted, worktree removed, `git worktree list` clean, `agency-starter` restarted from the task worktree and answering HTTP 200.
- 2026-08-04: FIRST FULL CI VERIFICATION of Unit 1, run `30916465866` on `5892aad`. PHP QA, PHP integration and Frontend all succeeded; the migration-baseline capture correctly skipped; commerce failed as expected under the recorded exemption; and the End-to-end job failed at "Run migration and editing parity suites". That parity failure is a REAL finding about the migration, not an environment artefact, and it is exactly what the gate exists to produce.
- 2026-08-04: CI parity numbers on Linux with real DejaVu fonts. Migration home DESKTOP passed at the tightened 2 percent limit. Migration home mobile 5.09 percent against 2 percent. Migration sample-page 18.13 percent desktop and 26.54 percent mobile against 5 percent. All four editing-parity cases failed. The decisive observation is that home desktop PASSED while sample-page desktop was 18 percent, with the same header, footer and viewport — which localises the fault to page-content rendering rather than site chrome.
- 2026-08-04: DEJAVU FONTS INSTALLED ON THE WINDOWS HOST at the user's prompting, and this was the single highest-value action of the session. The eight TTFs were copied from WSL's own `fonts-dejavu-core` — the same package CI installs — into `%LOCALAPPDATA%\Microsoft\Windows\Fonts` and registered under `HKCU\Software\Microsoft\Windows NT\CurrentVersion\Fonts`. No administrator rights, no download, and reversible by deleting eight files and their registry values.
- 2026-08-04: The install was verified by convergence, not by assumption. Migration sample-page desktop measured 18.19 percent locally against 18.13 percent in CI — 0.06 points apart — and home desktop began passing locally exactly as it does in CI. DESKTOP is therefore now authoritative locally. MOBILE still diverges, over-reporting by roughly three times (home mobile 15.04 local against 5.09 in CI), so mobile remains CI-authoritative. Measurement time per iteration fell from about twelve minutes to about two.
- 2026-08-04: ORCHESTRATOR PROCESS FAILURE, recorded because the user caught it. The orchestrator had accepted a twelve-minute CI round-trip per parity measurement and was preparing to iterate that way. The user asked whether DejaVu could simply be installed. It could, in five minutes, from a package already present in WSL. The lesson generalises beyond fonts: when an environment limitation is making the work slow, fix the environment before grinding against it.
- 2026-08-04: With real fonts the diff images became readable, which converted a bare percentage into a diagnosis. `sample-page-migration-diff.png` showed three defects in causal order — the block render's content starting at a different x, so the container width or inline padding differed; body text wrapping at different points, so misalignment accumulated down the page; and the footer landing about 80 pixels lower as the consequence rather than an independent fault. The site-title text difference contributed about 0.06 points and was correctly dismissed.
- 2026-08-04: MIGRATION PARITY GEOMETRY GAP CLOSED, commit `3b3aa51`. Model `gpt-5.6-luna`, reasoning level `max`. The fix gives `#site-main` the global content-size max-width with automatic inline margins and inline padding, and normalises `.wp-block-post-content` child margins to the classic one-em rhythm with a quarter-em first child. Desktop results: sample-page moved from 18.19 percent to PASSING at 5 percent, and home continued to pass at 2 percent. Mobile improved from 15.04 to 4.70 percent on home and from 30.57 to 8.48 percent on sample-page. Six of eight parity cases now pass locally.
- 2026-08-04: Verified by the orchestrator against the tree rather than the report: no threshold rose — home is still `0.02` and sample-page `0.05` — no baseline or visual PNG changed, and no `maskSelectors` entry was added. The gap was closed by correcting the rendering, which is what Task 10 Step 5 mandates.
- 2026-08-04: RAISED FOR REVIEW, not fixed. The geometry fix adds five `!important` declarations to `assets/global/shared.css`. They are plausibly necessary to beat the per-block spacing WordPress generates — the same specificity fight fixed Decision 11 describes — but `!important` in a shared stylesheet is precisely what becomes unmaintainable, and a reviewer should judge whether a higher-specificity selector would do instead.
- 2026-08-04: Post-fix gates re-run independently by the orchestrator: deptrac 0 violations and 0 errors, architecture 44 / 403, unit 182 / 402, integration 80 / 338, e2e 42 passed / 32 skipped / 0 failed, accessibility 6 passed, `npm run lint` exit 0. Branch pushed at `3b3aa51` and CI run `30925782912` started to settle the mobile question, which local measurement cannot answer.
- 2026-08-04: CI run `30925782912` on `3b3aa51`. PHP QA, PHP integration and Frontend green; migration capture correctly skipped; commerce failed under the recorded exemption. The End-to-end job failed at "Run visual regression suite" — which is CORRECT and expected, because `3b3aa51` changed `#site-main` geometry and the committed baselines were captured at `dabda2b` before that change. The e2e and accessibility steps both succeeded.
- 2026-08-04: CI DESIGN DEFECT found by reading step-level outcomes rather than trusting the job conclusion. "Run migration and editing parity suites" was SKIPPED, not passed — so parity was NOT verified in that run. The cause is precise: the step carries an `if:` expression with no status function, and GitHub Actions inserts an implicit `success()` in that case, so `if: <expr>` behaves as `if: success() && <expr>`. The failed visual step made `success()` false and silently suppressed parity. Two independent gates, one able to mask the other, and a skipped required gate looks nothing like a failed one in the run summary. The fix is `!cancelled() && <expr>`, which runs parity regardless of the visual outcome while still failing the job on its own merits. Routed to Unit 4B's operations sweep rather than widening Task 10 now; recorded here so it cannot be lost.
- 2026-08-04: Visual baselines being regenerated at `3b3aa51` through run `30926814021`, because the geometry fix legitimately changed rendered pixels. The remaining sequence to close Unit 1 is: accept and commit the four regenerated PNGs after inspecting each by eye, re-run CI so the visual gate passes and the parity suites finally EXECUTE, then merge if green. Mobile parity remains the one genuinely unmeasured question; desktop already passes locally at 0.06-point fidelity to CI.
- 2026-08-04: Visual baselines regenerated at `3b3aa51` and committed as `ec27012`, after the geometry fix legitimately moved rendered pixels. All four PNGs were opened and inspected by eye and each shows the fix working: `home-desktop` content now starts at the constrained inset with the intro paragraph wrapping to two lines and the footer correspondingly lower; `home-mobile` now carries inline padding instead of sitting flush against the viewport edge, which was the mobile defect; `demo-desktop` renders every section inside the constrained container; `demo-mobile` the same in a single column. `tests/parity/__migration_baselines__` remains untouched.
- 2026-08-04: RECURRENT CI INFRASTRUCTURE FLAKE, recorded for Unit 4B's operations sweep. Three separate runs failed at "Install PHP dependencies (inside DDEV)" with the identical cause: `curl error 56 while downloading https://downloads.w.org/release/wordpress-7.0.2-no-content.zip: QUIC connection has been shut down`, Composer exiting 100. Runs `30926814021` attempt 1, `30928843072` attempt 1, and one earlier occurrence. Each cost a full cycle and each was recovered by `rerun-failed-jobs` with no code change.
- 2026-08-04: The flake compounds with the step-ordering defect recorded above, and the pair is worse than either alone. Every job installs WordPress core from `downloads.w.org` over QUIC with no retry, so a transient drop kills the job; and because later steps carry an implicit `success()`, every gate behind the failure is marked SKIPPED rather than failed. A run can therefore report "failure" while the gates that matter were never executed, and the run summary does not distinguish that from a genuine gate failure. Mitigation for Unit 4B: add Composer retry or cache the core archive between runs, AND change the parity step to `!cancelled() && <expr>`.
- 2026-08-05: MOBILE MIGRATION PARITY CLOSED, commit `d84bda9`. ALL EIGHT parity cases now pass. Home mobile moved from 4.70 to 1.51 percent against a 2 percent limit, and sample-page mobile from 8.48 to 1.07 percent against 5 percent. Desktop was already passing and is untouched: the change is one `@media (width <= 781px)` block.
- 2026-08-05: That work was delegated to OpenCode at the user's direction. Model `opencode-go/deepseek-v4-flash`, variant `high`, agent `build`, session `ses_0313717bcffevqOpxapZ0i38jx`, run with `-Auto -AllowAuto` for autonomous LOCAL approval only. Requested and observed model matched, which the wrapper contract requires. The model has no image input, so it analysed the diff PNGs programmatically with Pillow, found roughly 27-pixel repeating bands, and traced them to three measured causes: the navigation inheriting a 24-pixel core layout margin the classic header never had; core's responsive open-menu button being 24 pixels where the classic toggle was 40, which together made the header 270 pixels instead of the classic 261 and pushed everything below it down; and the footer box sitting about 3 pixels low and ending about 20 pixels short of the classic two-line footer.
- 2026-08-05: Verified independently by the orchestrator rather than accepted from the report: `npm run test:parity -- --workers=1` returned 8 passed; `git status` showed only `shared.css`; no baseline or visual PNG changed; thresholds remain `0.02` and `0.05`; no `maskSelectors` entry was added. `ddev composer verify` 44 / 403, 182 / 402, 80 / 338 with deptrac 0; e2e 42 passed / 32 skipped / 0 failed; accessibility 6 passed; lint exit 0. The visual suite now fails on exactly the two MOBILE snapshots, which is the correct consequence of a mobile-only CSS change and a useful confirmation that the change is properly scoped.
- 2026-08-05: OpenCode created a scratch directory and a temporary repro spec inside `tests/parity/` during the run, both outside its stated owned-file list. It removed both before finishing, so the final worktree contained only `shared.css`. Noted because a stray file under `tests/parity/` would have failed `MigrationBaselineGuardTest`, which asserts an exact declared page count and forbids `toHaveScreenshot` anywhere in that directory.
- 2026-08-05: RESERVATION recorded, not blocking. The footer rule uses magic numbers — `margin-block-start: 21px` and `padding-block: 33.5px`, including a half pixel — rather than design tokens. The comment explains their derivation from the classic 88-pixel footer box, but a reviewer should judge whether tokens would serve better.
- 2026-08-05: ORCHESTRATOR OVERSTATEMENT, corrected here because it is also wrong in the `d84bda9` commit message. OpenCode reported that `parts/site-footer.html` hardcodes "© Agency Starter. All rights reserved." where the classic footer rendered the year and `get_bloginfo('name')`, and the orchestrator described this as a lifecycle-breaking regression in which a renaming client keeps the stale name forever. That is FALSE. `collect_files()` in `scripts/rename-project` collects every tracked file except `/web/wp/`, `/vendor/`, `/node_modules/`, `/.git/`, `/build/` and the lock files, so `parts/site-footer.html` IS covered and the documented rename path does replace the string.
- 2026-08-05: The accurate, narrower finding that remains. The classic footer read `get_bloginfo('name')` and therefore tracked the DATABASE; the block footer is static text fixed at rename time. A client who changes the site title in the Site Editor will see the HEADER update, because it uses the dynamic `core/site-title` block, while the FOOTER does not follow. The classic footer also included the year, which the block version dropped. This is a real fidelity gap and an inconsistency between two parts of the same chrome, but it is not a lifecycle defect. Raised for Unit 4B rather than fixed in Unit 1, since parity is green and the fix needs a binding mechanism.
- 2026-08-05: THE COMPOSER FLAKE WAS FIXED RATHER THAN RE-ROLLED, commit `07baa2c`. A FIFTH run died at "Install PHP dependencies (inside DDEV)" with the same `curl error 56 ... QUIC connection has been shut down` fetching WordPress core. The orchestrator had stated in advance that four occurrences justified re-running but a fifth would mean the gate itself was unreliable, and acted on that rather than rolling the dice again. All three DDEV jobs now retry `composer install` three times with a 15 second pause; a genuine dependency error still fails on the third attempt.
- 2026-08-05: The reason this was worth doing mid-verification, despite the general rule against changing a pipeline while certifying against it. The flake was not merely costing minutes: because later steps carry an implicit `success()`, a failure at dependency install marked the e2e, accessibility, visual and parity gates SKIPPED rather than failed. A run could therefore report failure while the gates that matter never executed, and the summary does not distinguish that from a real gate failure. An unreliable early step combined with silent downstream skipping can certify a branch nobody actually tested.
- 2026-08-05: UNIT 1 FINAL CI GREEN. Run `30959240939` on `07baa2c`. PHP QA success, PHP integration success, Frontend success, End-to-end SUCCESS — and within it the e2e suite, the accessibility suite, the visual regression suite, and the migration and editing parity suites ALL succeeded. The migration-baseline capture correctly skipped. `commerce-e2e` failed under the approved temporary exception recorded above. This is the first run in which the parity gates actually EXECUTED and PASSED on Linux rather than being masked by an earlier failure.
- 2026-08-05: UNIT 1 MERGED into `feat/block-theme-fse-migration` with `--no-ff`, preserving every checkpoint. The merge records the final CI evidence and restates the commerce exception with its Unit 4A expiry, as the exception itself requires of each merging unit.
- 2026-08-05: Sol finding 6 is discharged by this commit. The authoritative Unit 1 record now exists on the integration branch, which is what the finding actually required; the record had been complete on disk throughout and was withheld only by the tracking-commit cadence decided on 2026-08-03.
- 2026-08-05: Unit 1 closing summary. Twelve tasks, sixteen plan defects found and corrected, one whole-unit review with seven important findings and six rulings, and a genuine rendering regression caught and closed by the parity gate without raising a threshold, touching a baseline, or masking a difference. The two defects that cost the most were both SILENT: a pattern registered under a category WordPress core does not define, and a fresh-install proof configured with the wrong docroot so its asset checks proved nothing. Neither would ever have failed a test. That is the pattern to hunt in Units 2 to 4B.
- 2026-08-05: Carried into later units. Unit 4A owns the commerce templates, the commerce test-state hermeticity and the `ShopRole` deactivation contract. Unit 4B owns the `README.md` handoff recorded above, the parity step's implicit-`success()` ordering defect, the footer's static site name, the five `!important` declarations in `shared.css`, the `ci-capture/visual-baselines` ref paying for a commerce boot it does not need, and the three high-severity Dependabot advisories on the default branch.
- 2026-08-05: UNIT 2 TRANSITION COMPLETE, per the execution-order procedure. The integration-branch gate passed on the merged tip. DDEV was released from the Unit 1 worktree with `ddev stop` then `ddev stop --unlist agency-starter`. Worktree `C:\Users\Aleksandar\Projects\wt\bt-task-2` created on `feat/bt-task-2-state-export-diff` from `72ba796`, clean. `ddev start` from that worktree succeeded, the database volume followed the project name as documented, and the site answers HTTP 200. The new worktree has NO `vendor/` yet — `ddev composer install` must run before the first gate.
- 2026-08-05: MODEL POLICY CHANGED BY THE USER for Unit 2 onward, to match real budget availability. Anthropic is abundant, OpenCode is cheap and disposable, Codex is the scarcest. Implementation for all 13 tasks uses `opencode-go/deepseek-v4-flash` at variant `max`, agent `build`. Bounded review uses `gpt-5.6-luna` at `xhigh` but ONLY on the five tasks where a silent failure is plausible and expensive — 3 (HMAC signer), 4 (schema validator), 10 (exporter and bundle reader), 11 (differ), 13 (CLI wiring and the alias) — plus one unit review. The Opus orchestrator reviews every other diff itself and runs all gates. That is about 6 Codex calls against Unit 1's roughly 20.
- 2026-08-05: READ-ONLY PLAN AUDIT RUN BEFORE ANY UNIT 2 IMPLEMENTATION — a new practice adopted after Unit 1 surfaced sixteen plan defects during execution, each costing a refuse, diagnose, correct and re-delegate cycle. Model `gpt-5.6-terra`, reasoning `high`, sandbox `read-only`, 128k tokens. It found SIX defects including two blockers and a circular dependency, and concluded that "the unmodified plan has no valid task order". The practice paid for itself immediately and should be repeated for Units 3A, 3B, 4A and 4B.
- 2026-08-05: Unit 2 audit BLOCKER 1 — Task 8, plan lines 2655 to 2668. `test_include_content_adds_the_content_provider()` expects `content` in `StateRegistry::resolve( null, true )`, but Task 8 creates only three providers (line 2714) and `ContentState` is first created and registered in Task 9 (lines 2831, 2961). Task 8's own unit gate at line 2729 must therefore fail. This is the same species as Unit 1's Task 3 defect: a task whose declared gate is red by construction. Correction: move the two `content` assertions to Task 9.
- 2026-08-05: Unit 2 audit BLOCKER 2 — Task 13, plan lines 4479 and 4627 to 4649. The plan modifies `src/Cli/AgencyCommands.php`, which UNIT 1 owns; Unit 2's grant is one sequenced `Plugin.php` registration line only. Worse, deleting `DatabaseOverrideCheck` while the alias still constructs it at `AgencyCommands.php:53` makes the alias fatal at runtime, not merely red. Correction: obtain and record an explicit ownership transfer, or defer both the alias rewrite and the deletion to the Unit 1 owner.
- 2026-08-05: Unit 2 audit IMPORTANT 3 — a genuine CIRCULAR DEPENDENCY between Task 6 and Task 8, at plan lines 1850, 2099 to 2105, 2550 and 2559. Task 6 declares "Consumes: nothing beyond PHP" and then requires `ReferenceResolver` to call `StateRegistry::provider()`, which Task 8 creates, while Task 8 consumes `ReferenceScanner`, which Task 6 creates. No reordering resolves it. Correction: keep only `ReferenceScanner` in Task 6 and move `ReferenceResolver` to Task 9, where its integration test already belongs.
- 2026-08-05: Unit 2 audit IMPORTANT 4 — Task 13, lines 4629 to 4638, claims `check_overrides()` "takes no arguments at all". That is false on this branch. Unit 1 already gave it `array $args, array $assoc_args = array()` at `AgencyCommands.php:52` and added `drift_is_failure()` and `drift_summary_lines()` at lines 86 and 94. The plan describes a repository that no longer exists.
- 2026-08-05: Unit 2 audit IMPORTANT 5 — Global Constraints line 42 says Task 3 appends its own `.env.example` entries, but Task 1 already adds both HMAC settings at lines 232 to 239 and Task 3 neither lists nor stages that file. Correction: make Task 1 the sole owner of all three state settings and delete the constraint sentence.
- 2026-08-05: Unit 2 audit IMPORTANT 6, a SILENT defect — Task 13 lines 4536 to 4559 present a test as one that "drives the real alias logic", but it calls `$this->runner()->check_overrides()` rather than `AgencyCommands::check_overrides()`, registers no WP-CLI command and never exercises `CliOutput::emit()`. A broken alias passes it. Correction: rename it a runner test and add a real DDEV `wp agency check-overrides` test covering both flag cases.
- 2026-08-05: CORRECTED UNIT 2 TASK ORDER, from the audit: `1 → 2 → 3 → 4 → 5 → 6 (scanner only) → 7 → 8 → 9 (resolver and providers) → 10 → 11 → 12 → 13`.
- 2026-08-05: Unit 2 deptrac assessment from the audit. No plan step names `SiteCore`, `SiteIntegrations`, `SiteCommerce` or `SiteTheme`, which is the main risk cleared. One residual: Task 3 line 1012 suggests `AgencyPlatform\Logging\Logger::redact()`. deptrac permits intra-layer references, but Unit 2's stated limit is WordPress core and PHP only, so either remove the optional dependency or record an explicit exception.
- 2026-08-05: The six plan corrections above are NOT yet applied to the plan file. They are the incoming orchestrator's first task, before any implementation, each committed separately with its evidence. `docs/superpowers/plans/UNIT-2-ORCHESTRATOR-PROMPT.md` carries the full handoff including the model policy, the environment facts, the ownership boundaries and the Unit 1 process lessons.
- 2026-08-05: UNIT 2 STARTS. Opus 5 continues as orchestrator. Model policy for this unit differs from Unit 1 by user instruction: implementation for ALL 13 tasks uses `opencode-go/deepseek-v4-flash` variant `max`, agent `build`; `gpt-5.6-luna` at `xhigh` performs bounded reviews for the five high-risk tasks ONLY — 3 (HMAC signer), 4 (schema validator), 10 (exporter and bundle reader), 11 (differ), 13 (CLI wiring and alias) — plus one unit review at the end. The orchestrator is the reviewer for every other task and reads each diff itself. Every delegated call records its observed model and reasoning level; a differing observed model is a FAILED invocation, not a result.
- 2026-08-05: Unit 2 handoff state confirmed against git rather than accepted from the prompt. Worktree `C:\Users\Aleksandar\Projects\wt\bt-task-2` is on `feat/bt-task-2-state-export-diff` at `72ba796` with an empty `git status --porcelain`. `vendor/` was absent as recorded; `ddev composer install --no-interaction --prefer-dist` completed with exit 0.
- 2026-08-05: UNIT 2 BASELINE GATE. `ddev composer verify` exit 0 in the worktree: deptrac 0 violations and 0 errors, PHPStan `[OK] No errors`, architecture 44 tests / 403 assertions, unit 182 / 402, integration 80 / 338. This matches the Unit 1 carry-forward exactly, so Unit 2 starts from a genuinely green base and any later movement in these counts is attributable to Unit 2 work.
- 2026-08-05: SEVENTH PLAN DEFECT, found by the orchestrator while verifying the sixth and NOT part of the read-only audit. Task 13 rewires `check_overrides()` onto `StateCommandRunner`, which orphans `AgencyCommands::drift_is_failure()` and `drift_summary_lines()`. Those two helpers are covered by `tests/Unit/AgencyPlatform/CheckOverridesReportTest.php`, whose four tests call nothing else and whose `report()` fixture hand-builds the `DatabaseOverrideCheck` shape (`overrides` / `expected` / `synced_patterns`) that Task 13 deletes. The unmodified plan mentions neither the helpers nor that file, so it would have left dead production code plus a green unit test for a report nothing produces — and a worker that deleted the helpers on its own initiative would have made the unit suite fatal on `Call to undefined method`. Task 13 now deletes all three together and its Step 5 greps for both helper names as well as for `DatabaseOverrideCheck`.
- 2026-08-05: THE TASK 13 OWNERSHIP BLOCKER IS RESOLVED BY TRANSFER, NOT BY DEFERRAL, and the transfer is recorded in the Shared-file ownership section above rather than only in the plan. The two options were weighed explicitly. Deferring the alias rewrite would ship a `wp agency check-overrides` that fatals the moment `DatabaseOverrideCheck` is deleted; deferring the deletion would leave §11.10 drift reporting with two competing implementations across a unit boundary and would push the removal into a unit that does not own the state subsystem. The transfer is deliberately narrow — `check_overrides()`, the two drift helpers, their test file — and `register()`, `sanitize()` and `verify_env()` remain Unit 1's.
- 2026-08-05: TASK 4 CORRECTION resolves the deptrac residual raised above by RECORDING the exception rather than removing the dependency. `web/app/mu-plugins/agency-platform/src/Logging/Logger.php:60` really declares `public static function redact( array $context ): array`, and `deptrac.yaml:56` declares `AgencyPlatform: []`, which forbids a dependency on another LAYER. A reference from `agency-platform/src/State/` to `agency-platform/src/Logging/` stays inside the `AgencyPlatform` layer and is not a violation. It is now the single stated exception to Unit 2's "WordPress core and PHP only" limit, and any other cross-directory reference must be reported before it is written.
- 2026-08-05: The Task 6 defect was worse than "a wrong Consumes line". The unmodified plan had NO VALID TASK ORDER: Task 6 shipped `ReferenceResolver`, whose `resolve()` reads `StateRegistry::provider()` from Task 8, while Task 8 consumes `ReferenceScanner` from Task 6. Neither ordering compiles. Because PHPStan level 6 runs inside `verify:fast`, Task 6 would have failed its own commit gate on a call to an unknown class. Task 6 now ships the pure scanner without `scan()`; Task 9 ships `ReferenceResolver` and adds `scan()`, which is where the resolver's integration test already lived. `StateProvider::record()` already carries the `bool $with_references = true` recursion guard from Task 5, so no earlier contract changes.
- 2026-08-05: Unit 2 plan corrections committed as `e85c919`. The seven corrections are recorded inline at the point each applies, plus a new "Execution order — corrected 2026-08-05" section at the head of the plan carrying the corrected order and a defect table. Every correction states the evidence that proves the original text wrong, so a later reader can check the ruling rather than trust it. Tracking updated on the integration branch as `c48ddc7`.
- 2026-08-05: Unit 2 Task 1 delegated. Model `opencode-go/deepseek-v4-flash`, variant `max`, agent `build`, session `ses_030c582f1ffebOoVIxW9XeXJwT`, run with `-Auto -AllowAuto` for autonomous LOCAL approval only. Requested and observed model matched, which the wrapper contract requires. Committed as `08427d4` — exactly the seven owned files, worktree clean, no scope widening.
- 2026-08-05: EIGHTH PLAN DEFECT, found during execution rather than by the audit. Task 1 Step 8 specified `phpcs:ignore Generic.CodeAnalysis.UnusedFunctionParameter.Found` for `EnvironmentConfig::resolve()`'s unused `$name`. The installed PHPCS emits that warning as `UnusedFunctionParameter.FoundAfterLastUsed`, so the suppression never matched and `lint:php` failed inside `verify:fast`. The worker hit the red gate, fixed the sniff code inside its own file, and REPORTED it rather than working around it or broadening the suppression — the required behaviour.
- 2026-08-05: The orchestrator verified that defect instead of accepting the report. Restoring the plan's original `.Found` code and running phpcs directly reproduced `56 | WARNING | The method parameter $name is never used (Generic.CodeAnalysis.UnusedFunctionParameter.FoundAfterLastUsed)` with exit status 1, which proves the suppression was inert rather than merely redundant. The worktree was restored clean afterwards. Plan corrected as `e3db21a`, with a standing note for the remaining tasks that sniff codes are version-specific and a mismatched `phpcs:ignore` is SILENTLY inert — the same silent-failure class as Unit 1's `pages` category and its wrong-docroot proof.
- 2026-08-05: `.gitignore` PROVEN BY EXPERIMENT, not by reading the file. Reading `.gitignore` proves nothing about whether exported bundles — which carry customer content and internal site structure — actually stay out of Git. A real `var/agency-state/probe-bundle.json` was written with real content: `git status --porcelain` stayed empty, `git check-ignore -v` attributed it to `.gitignore:74 /var/agency-state/`, and a nested `var/agency-state/nested/deep.json` was ignored too. The probe tree was removed and the worktree confirmed clean. No test in this repository makes this check.
- 2026-08-05: Task 1 dependency placement verified from `composer.lock` rather than from `composer.json` alone, because `require` versus `require-dev` is exactly the kind of error nothing would fail on until a production host tried to validate a manifest. `swaggest/json-schema` is in `packages`, `packages-dev` contains no swaggest entry, and the lock platform is `php >=8.3`. `swaggest/json-diff` came in transitively and is covered by `composer audit` inside `verify:fast`.
- 2026-08-05: Unit 2 Task 1 gates re-run INDEPENDENTLY by the orchestrator and they agree with the worker exactly: `verify:fast` exit 0, deptrac 0 violations and 0 errors with 43 uncovered `Swaggest\*` vendor classes, architecture 44 tests / 404 assertions, unit 188 / 410. Unit rose by exactly the required 6. `StateDirectory.php` confirmed absent, as Task 7 owns it. Zero `phpstan-ignore` comments anywhere under `agency-platform/src/`.
- 2026-08-05: The architecture assertion rise from 403 to 404 was verified rather than waved through. `tests/Architecture/DirectoryRulesTest.php:146-152` asserts once per scanned directory, so the new `src/State/` directory adds exactly one assertion. No count fell.
- 2026-08-05: Carried to the Unit 2 review, not fixed. `EnvironmentConfig::get()` casts a scalar constant with `(string) $value`, so a constant defined as `false` becomes `''`, reads as ABSENT, and falls through to `getenv()`. It is harmless today because every setting routed through this class is a string — `AGENCY_STATE_DIR`, `AGENCY_REPO_ROOT`, and the two HMAC settings — but the class is a fixed cross-task contract, so widening it mid-task was rejected in favour of a reviewer ruling.
- 2026-08-05: Unit 3 contract symbols located in the corrected plan and pending verification in code before the unit review: `PromotionStrategy` and `PromotionStrategies` in Task 5, `SchemaValidator::SCHEMA_PROMOTION_MANIFEST` in Task 4. Unit 3 stalls immediately if Unit 2 ships without all three.
- 2026-08-05: Unit 2 Task 2 delegated. Model `opencode-go/deepseek-v4-flash`, variant `max`, agent `build`, session `ses_030bb69d0ffe0JbHkLCnFiL9jr`. Requested and observed model matched. The worker implemented the task, hit a RED `verify:fast`, and REFUSED TO COMMIT — the sixth such refusal in this engagement and again the correct behaviour, because the plan was wrong rather than the worker.
- 2026-08-05: NINTH PLAN DEFECT, and it is subsystem-wide rather than a Task 2 problem. The plan's own verbatim `Normalizer` code cannot pass this repository's `lint:php`. `WordPress.Security.EscapeOutput.ExceptionNotEscaped` fires on every variable interpolated into an exception message, and this plan REQUIRES such messages throughout — Task 3 alone demands one "naming `AGENCY_PROMOTION_HMAC_KEYS` and the offending key id". The orchestrator reproduced the failure independently before acting: 4 errors at `Normalizer.php:91,198,205` plus 2 `WordPress.PHP.IniSet.Risky` warnings, and phpcs exits non-zero on warnings here.
- 2026-08-05: The cause was diagnosed rather than patched. There is NO other `throw new` anywhere in `agency-platform` or the three `site-*` plugins, so Task 2 was simply the first task to throw a `StateException` and the entire state subsystem met this sniff at once. A per-task fix would therefore have had to be repeated in almost every remaining task.
- 2026-08-05: Two candidate fixes were REJECTED with reasons. First, `esc_html()` on the message is actively harmful: it would make `Normalizer` and every other state class depend on WordPress, which breaks the Global Constraint that unit-tested methods stay WordPress-free so `tests/support/wp-stubs.php` never grows into a shadow WordPress, and it would mangle CLI diagnostics for no security benefit because these messages never reach a browser. Second, a `phpcs:ignore` at every throw site is unbounded across 13 tasks and each one is a place a genuine escaping bug could later hide — the exact pattern Unit 1 ruled against when six suppressions were added to shipping code to accommodate an incomplete test stub.
- 2026-08-05: ACCEPTED FIX — a `phpcs.xml` exclusion scoped to `web/app/mu-plugins/agency-platform/src/State/*` only. `StateException` is a WP-CLI-only failure type that the command surface catches and converts into an exit code, so the sniff's premise that the message may reach a browser is false for this subsystem and true everywhere else. Committed as `560d1fb`.
- 2026-08-05: THE EXCLUSION WAS PROVEN SCOPED, NOT MERELY APPLIED. A probe class containing `throw new \RuntimeException( 'probe ' . $why )` was placed OUTSIDE `src/State/`, at `agency-platform/src/Health/PhpcsScopeProbe.php`, and phpcs still reported `FOUND 1 ERROR ... (WordPress.Security.EscapeOutput.ExceptionNotEscaped)`. The probe was then deleted and the worktree confirmed clean. Without that check the exclusion could have blinded the sniff repository-wide and no test would ever have failed — the same silent class as Unit 1's `pages` category and its wrong-docroot proof.
- 2026-08-05: The two sniffs were deliberately treated DIFFERENTLY rather than alike. `IniSet.Risky` is BOUNDED — `canonical_json()` is the single place that pins `serialize_precision`, plus one test that pins it to prove independence, four sites in total — so it keeps a per-site `phpcs:ignore` with a real reason, matching the house style already in `AgencyCommands.php` and `EnvironmentConfig.php`. The formatting violations in the plan's verbatim test block were FIXED with phpcbf, never suppressed. `phpcs.xml` is now orchestrator-owned; no worker task may edit it.
- 2026-08-05: Unit 2 Task 2 committed as `502fc86` after the fix wave, exactly its two owned files. Gates: `verify:fast` exit 0, deptrac 0 violations, architecture 44, unit 204 / 431 — a rise of exactly 16. phpcs reports zero errors AND zero warnings on both files. The worker confirmed phpcbf changed only layout and that `NormalizerTest` is still 16 green.
- 2026-08-05: DETERMINISM PROVEN EMPIRICALLY AGAINST THE REAL CLASS, not accepted from a passing unit suite. `stateHash` is the contract every later drift and tamper gate rests on, so a probe was run inside the container against the shipped `Normalizer`: key-order independence gives an identical hash; `1` and `1.0` both encode to `{"v":1}`; `-0.0` encodes to `{"v":0}`; `NAN`, `INF` and `-INF` each throw `StateException` rather than serialising; a bundle document carries exactly one trailing LF; CRLF and LF content hash identically; `normalize_content()` is idempotent; `prune_empty()` keeps `0`, `0.0`, `false` and `'0'` while dropping `null`, `''`, whitespace-only strings and empty arrays; and the hash is 64 lowercase hex characters.
- 2026-08-05: ONE OF THOSE PROBES WAS VACUOUS AND WAS RE-RUN, recorded because catching it is the standard this engagement holds reviewers to. The `serialize_precision` restore check compared before against after while the environment already sat at the pinned value `-1`, so it would have passed even if the `finally` restore were deleted. Re-run with `php -d serialize_precision=3`: the setting returned to `3` afterwards, and `0.12345678901234567` encoded to 17 significant digits, which proves the `-1` pin was genuinely in effect DURING the call rather than the two ends merely agreeing.
- 2026-08-05: Unit 2 Task 3 delegated. Model `opencode-go/deepseek-v4-flash`, variant `max`, agent `build`, session `ses_030ad4834ffeY4gtmZDtR8DCYu`. Requested and observed model matched. Committed as `8cc7163`, exactly its two owned files. Gates: `verify:fast` exit 0, deptrac 0 violations, architecture 44, unit 217 — a rise of exactly 13. `.env.example` was left untouched, and the worker independently confirmed Task 1's two HMAC entries were already present, which is the ownership correction from defect 3 working as intended.
- 2026-08-05: The phpcs ruling paid for itself one task later. Task 3's verbatim plan test block tripped the same formatting sniffs, and because the ruling was already recorded in the plan the worker fixed them with phpcbf and continued instead of stopping on a red gate for a second time.
- 2026-08-05: Unit 2 Task 3 bounded review delegated — the first of the five high-risk reviews the Unit 2 model policy allows. Model `gpt-5.6-luna`, reasoning level `xhigh`, sandbox `danger-full-access`, working root pinned to the task worktree. Verdict: ACCEPT WITH FIXES, three LOW findings.
- 2026-08-05: ORCHESTRATOR TOOLING ERROR, recorded because it was mine. The Codex invocation was piped through `tail -120`, which DISCARDED the model banner the policy requires as proof. The model was recovered from Codex's own session rollout instead: `turn_context` records `"model":"gpt-5.6-luna"`, `"effort":"xhigh"`, `sandbox_policy: danger-full-access`, and the worktree as `cwd`. The single `gpt-5.6-sol` string in that file appears inside Codex's own base instructions listing available overrides, not as the model that ran. The invocation was therefore compliant, but the remaining four reviews must capture the banner directly rather than rely on log archaeology.
- 2026-08-05: The Task 3 review earned its cost on the question the brief sent it to answer. Its break-and-restore proof was real: removing `$purpose .` from BOTH production MAC calculations made both cross-purpose replay tests fail with exactly `Failed asserting that exception of type "AgencyPlatform\State\StateException" is thrown.`, and both files were restored to byte-identical SHA-256 with an empty `git status`. It also enumerated, per test, what each one would tolerate while still passing — several were individually weak even though the suite combined to catch the main cases.
- 2026-08-05: Review findings by experiment rather than reading. `verify()` rejected non-hex signatures, array and object signatures, extra-field and duplicate-key documents, and a wrong key id; it accepted a document signed by a retained older key; a short key was a hard failure; and no unknown-id fallback exists. No code path can put a key VALUE into a message, log, dump or stack trace — messages carry key IDs only, and the test file does not echo keys.
- 2026-08-05: Sol-equivalent ruling accepted on the unvalidated `$purpose` parameter. `sign( $payload, 'typo' )` does succeed, but no external input supplies the purpose, both production callers pass the constants, and an allow-list would change a signature that is a FIXED cross-unit contract Unit 3 reuses verbatim. Recorded as acceptable, not as a defect.
- 2026-08-05: The three accepted findings were fixes worth making, not cosmetics. The exit code IS the command contract — the CLI surface turns these exceptions into process exit codes a deploy gate reads, so a tamper exiting 1 instead of 4 would be a real defect that every pre-fix test passed over. Fix commit `9e40209`: the invalid-key-id diagnostic now names the offending id, and five tests now assert `EXIT_TAMPER` or `EXIT_HARD_ERROR` by constant rather than only the exception class.
- 2026-08-05: Non-vacuity evidence for the fix, supplied as required. Making the no-signature path throw `hard_error` instead of `tamper` produced `Failed asserting that 1 is identical to 4.` at `HmacSignerTest.php:161`; restoring it returned `OK (1 test, 2 assertions)`. The orchestrator additionally verified the diff shape itself: each test catches, asserts `exit_code()`, and RE-THROWS so `expectException` still holds, which strengthens the existing assertion rather than replacing it.
- 2026-08-05: Task 3 fix gates re-run independently by the orchestrator and they agree: `verify:fast` exit 0, deptrac 0 violations, architecture 44, unit 217 / 454. The test COUNT held at 217 while assertions rose 449 to 454, which is exactly right for five strengthened tests and no new ones. The `json_encode` used for the safe id representation carries a justified `phpcs:ignore` matching the existing `Normalizer.php:86` precedent, keeping the state subsystem WordPress-free; `$key_id` can only ever be `int|string` because PHP coerces array keys, so the representation is total.
- 2026-08-05: ORCHESTRATOR PROCESS FAILURE on Task 4, recorded because it was mine. The OpenCode wrapper was launched through a tool whose timeout caps at 600 seconds, and Task 4 exceeded it, so the wrapper was KILLED after writing its files but before running gates or reporting. The wrapper writes nothing until it returns, so the worker's report — including its test-first evidence — was lost entirely.
- 2026-08-05: The Unit 1 lesson applied and paid off. Rather than trust the kill signal, the orchestrator judged the WORKTREE: `SchemaValidator.php` closed cleanly, `state-bundle-v1.json` parsed as JSON with all twelve required properties, and `SchemaValidatorTest.php` held 9 closed test methods. A Windows-side process check confirmed nothing was still in flight. The work was therefore gated and committed rather than discarded and redone.
- 2026-08-05: THE LOST TEST-FIRST EVIDENCE WAS RECONSTRUCTED, not skipped and not assumed. `SchemaValidator.php` was moved aside and the suite re-run: all 9 tests failed with `Class "AgencyPlatform\State\SchemaValidator" not found`, `Tests: 9, Assertions: 7, Errors: 2, Failures: 7`. The class was restored and confirmed byte-identical by diff. This is the same standard demanded of every worker and it would have been easy to wave through.
- 2026-08-05: Unit 2 Task 4 committed as `2a41298` by the ORCHESTRATOR rather than the worker, with the reason recorded in the commit message. Gates: `verify:fast` exit 0, deptrac 0 violations, architecture 44 / 406, unit 226 / 466 — a rise of exactly 9. The architecture assertion rise of 2 matches the two new directories `resources/` and `resources/schemas/`, which `DirectoryRulesTest` asserts per-directory. `SCHEMA_PROMOTION_MANIFEST` ships with value `promotion-manifest-v1`.
- 2026-08-05: Remaining OpenCode and Codex invocations now launch DETACHED via `nohup` so the 600-second tool cap cannot kill them, and Codex output is captured in full rather than piped through `tail`, which is what lost the Task 3 banner.
- 2026-08-05: Unit 2 Task 4 bounded review. Model `gpt-5.6-luna`, reasoning level `xhigh`, sandbox `danger-full-access`, workdir pinned to the task worktree, session `019fd075-fb1a-7ca3-9336-ddebdf41a047`. The banner was captured DIRECTLY this time. Verdict: ACCEPT WITH FIXES — two MEDIUM, one LOW, all three accepted. The brief told the reviewer explicitly that this commit had had less worker self-checking than the others.
- 2026-08-05: TWO ORCHESTRATOR MONITORING CHECKS WERE WRONG AND WERE CAUGHT BEFORE ACTING ON THEM. The first watch matched the literal string `ACCEPT WITH FIXES` — which appears in the orchestrator's OWN brief, echoed back — and reported a verdict that did not exist. The second used `pgrep -f codex`, which cannot see Windows processes from Git Bash, and declared the review dead while its output subsequently grew from 31 KB to 98 KB. Either would have aborted a running review and wasted a Codex call from the scarcest budget. Fixed: the terminal marker is now `tokens used`, and liveness is checked Windows-side.
- 2026-08-05: Task 4 review finding 2 is the highest-value find of the unit so far and is the same silent class as Unit 1's `pages` category. `test_the_promotion_manifest_constant_exists…` asserted only the constant's value and a filename suffix. It NEVER CALLED `validate()`, so it would have passed even if missing schemas were silently accepted — which is the single behaviour it exists to guarantee and precisely what Unit 3 depends on. A green test proving nothing.
- 2026-08-05: The review also positively CLEARED the hazard the orchestrator was most worried about, which is as valuable as what it found. `Normalizer` collapses integral floats to int before `SchemaValidator` runs, so the bytes the schema sees are not the bytes the caller passed; a `"type": "integer"` rule could in principle have been defeated. Probed directly: `1.0` is accepted for integer fields (correct after normalisation), `1.5` is rejected, and no numeric rule can be defeated or spuriously tripped. It also confirmed no remote `$ref` exists — which would have been a network call inside `src/State/` while looking entirely innocent.
- 2026-08-05: TENTH PLAN DEFECT and a ruling. Task 4's Interfaces block required `message lists every violation`; the pinned `swaggest/json-schema` is fail-fast and cannot. The worker was told to STOP rather than fake it if the library could not comply, and it did, with evidence. The orchestrator verified that evidence against `vendor/` rather than accepting it: `Context.php` contains zero error-collection code, `InvalidValue::$subErrors` is populated only in the `oneOf`/`anyOf` composite branches at `Schema.php:433-473` and `494-514`, and a document carrying both an invalid `stateHash` and an unknown field measurably yields one message with `SUBERRORS: 0`.
- 2026-08-05: Loop-and-retry was rejected as UNSOUND rather than merely awkward — removing a violating key to find the next makes `required` fire for the removed key and `providers.minProperties: 1` fail once the last provider is gone, so it can neither terminate reliably nor report honestly. A library swap to `justinrainbow/json-schema` or `opis/json-schema` was considered and rejected with the reason recorded so a reviewer can overturn it: bundles and manifests are MACHINE-GENERATED by this subsystem, not hand-authored, so a schema violation signals an exporter defect rather than a typo an operator is iterating on. Validation CORRECTNESS is unchanged either way — an invalid document is still rejected, which is the property that protects the production host. Amended to first-violation-with-JSON-pointer plus an explicit statement that validation stopped there. Plan amended as `9d32268`; fixes committed as `0771a08`. Raised for the unit review.
- 2026-08-05: Unit 2 Task 5 delegated. Model `opencode-go/deepseek-v4-flash`, variant `max`, agent `build`. The FIRST invocation failed with `OpenCode response did not finish successfully` from the wrapper's own validator — a transient provider failure that wrote nothing, so the retry started from a clean tree. Session `ses_030b...` on retry; requested and observed model matched. Committed as `89c7b66`. Gates: `verify:fast` exit 0, deptrac 0, architecture 44 / 406, unit 239 / 487 — a rise of exactly the plan's 8 + 4 tests.
- 2026-08-05: ALL THREE UNIT 3 CONTRACT SYMBOLS NOW EXIST IN CODE, not merely in the plan: `PromotionStrategy` and `PromotionStrategies` from Task 5, and `SchemaValidator::SCHEMA_PROMOTION_MANIFEST` from Task 4. `PromotionStrategies::all()` returns an empty array with nothing registered, rejects any filtered value that is not a `PromotionStrategy` with a message naming the key, re-keys by `provider_slug()`, sorts and memoises. Unit 3 implements the strategies; Unit 2 only defines the seam, which is exactly the split the tracking file requires.
- 2026-08-05: ELEVENTH PLAN DEFECT, and the second consequence of the same Task 6/8 circularity recorded as defect 2. Task 5 specified `BaseStateProvider::detect_references()` as scanning `$content['markup']`, but `ReferenceScanner` does not exist until Task 9 and referencing an unknown class fails PHPStan level 6 inside `verify:fast`. The worker shipped the only thing that compiles — an unconditional `return array();` — and reported it, which was correct. The DEFECT is that nothing forced a later task to restore it: Task 9's file list did not name `BaseStateProvider.php` at all.
- 2026-08-05: Why that landmine mattered more than it looked. Left returning `array()`, every provider inheriting `BaseStateProvider` reports ZERO references. References carry §7.4's v1 navigation policy — the exported navigation's content hash must travel inside the reference so a templates-only bundle can still be judged at finalisation. A bundle whose references are all empty is structurally valid, passes the schema, hashes deterministically, signs correctly, and is WRONG, and every gate stays green. This is the third instance in Unit 2 of the silent-failure class Unit 1 closed on twice.
- 2026-08-05: Correction committed as `72b4f57` after the orchestrator verified both halves against the tree rather than the report — `BaseStateProvider.php:54-56` really does return `array()` unconditionally, and plan line 1700 really does specify the scan. Task 9's file list now names `BaseStateProvider.php` as NON-OPTIONAL and a new Step 3c carries the restored implementation plus a REQUIRED break-and-restore proof against the base class default path, not merely through a provider that overrides it.
- 2026-08-05: Unit 2 Task 6 committed as `2d271d4`, exactly its two owned files. Gates: `verify:fast` exit 0, deptrac 0 violations, architecture 44, unit 253 — a rise of exactly 14. The orchestrator verified the scope correction held before reading the report: `ReferenceResolver.php` was NOT created, `ReferenceScanner::scan()` was NOT added, and the class exposes only the four pure methods `scan_parsed()`, `is_unresolved()`, `unresolved()` and `sort_references()`. The Task 6/9 split therefore worked as designed.
- 2026-08-05: TWELFTH PLAN DEFECT — a determinism test that proved nothing, reported by the worker and confirmed empirically by the orchestrator. Task 6's `test_reference_order_is_independent_of_attribute_map_order()` used a fixture of `id` + `customRef`. `customRef` matches NO matcher, because the unknown-ref suffixes are `Id`, `Ids` and `_id`, so both orderings emitted ONE identical reference and the assertion was vacuous. Probed against the shipped scanner: that fixture yields `count=1`. It would have passed against a completely unsorted implementation.
- 2026-08-05: Why that mattered. Reference order feeds the bundle and therefore `stateHash`, so an unsorted scanner would make the hash vary with PHP's attribute traversal order and produce random phantom drift on healthy sites — surfacing much later as an unexplainable differ bug rather than as a scanner defect.
- 2026-08-05: THE ORCHESTRATOR'S FIRST CORRECTION WAS ITSELF VACUOUS, and the worker caught it. The orchestrator prescribed `id` + `mediaId`; that fails for a different reason. `core/cover` is in `MEDIA_BLOCKS`, so `match_block_level()` consumes `id` at `ReferenceScanner.php:176` BEFORE the attribute loop runs, making its position invariant no matter how the attribute map is ordered. The worker ran the required break-and-restore proof, observed the strengthened test STILL PASS with `sort_references()` neutered, confirmed the neuter was live rather than opcache-masked, and REFUSED TO COMMIT — the seventh refusal in this engagement and the first one aimed at an orchestrator instruction rather than a plan defect.
- 2026-08-05: The working fixture is `mediaId` + `someId`, both unknown-ref catch-alls emitted from INSIDE the attribute loop, so traversal order genuinely affects emission. With sorting disabled they give `["mediaId","someId"]` versus `["someId","mediaId"]`; sorted, `["mediaId","someId"]` both ways. The orchestrator verified the `MEDIA_BLOCKS` reasoning against the scanner source before accepting the worker's alternative.
- 2026-08-05: Fix committed as `b63cf8c`, plan correction as `722328a`. The test now FAILS with sorting removed, the production scanner is byte-identical to `2d271d4`, and unit assertions rose 545 to 547. Both failed fixtures are recorded in the test comment with the reason each failed to discriminate, so neither is reintroduced as a "simplification", and an explicit `assertCount( 2, … )` guards against the test going vacuous again if the matcher table changes.
- 2026-08-05: Pattern recorded for the unit review and for Units 3 and 4. FOUR of the twelve Unit 2 defects are now "a test that cannot fail" defects — the inert `phpcs:ignore` sniff code, the promotion-manifest test that never called `validate()`, this determinism fixture, and the orchestrator's own replacement for it. That is decisively the dominant failure mode of this unit, exactly as Unit 1's closing note predicted, and it is why every such fix in Unit 2 carries a recorded break-and-restore proof rather than a green suite.
- 2026-08-05: OpenCode wrapper reliability noted, not yet actioned. Two invocations failed transiently with `OpenCode response did not finish successfully` from the wrapper's own validator — Task 5's first attempt and Task 7's first attempt. Both wrote nothing, so each retry started from a clean tree and no work was lost. Unit 1's precedent applies: a handful of transient failures justifies retrying, but a persistent pattern would mean the delegation path itself is unreliable and must be fixed rather than re-rolled.
- 2026-08-05: Unit 2 Task 7 committed as `2555967`, exactly its seven owned files. Gates: `verify:fast` exit 0 and `test:integration` exit 0, deptrac 0 violations, architecture 44, unit 266 / 564 — a rise of exactly 13 — and integration 80 to 84 / 344. The orchestrator verified before reading the report that `Health/SanitizeSteps.php` was NOT touched, so the ownership boundary held; the new site-uuid step registers through the `agency_platform_sanitize_steps` filter rather than editing the registry file that Task 13 owns.
- 2026-08-05: The Task 7 worker reported three plan gaps and closed two of them on instruction. The plan's `StateDirectory` specified NEITHER a web-root guard NOR a traversal guard; the brief required adding a missing guard and reporting it, and the worker did both. It also reported the third gap honestly rather than papering over it: the plan's Files block contains NO `StateDirectory` test file, so the guards shipped with zero automated coverage and were proven only by an ad-hoc probe it then deleted.
- 2026-08-05: THIRTEENTH PLAN DEFECT — the first genuinely SECURITY-relevant one, and found by PROBING the shipped class rather than reading it. Both guards look correct on inspection. `assert_no_traversal()` ran only in the RELATIVE branch, so an ABSOLUTE override skipped it entirely, and `assert_outside_web_root()` compared raw string prefixes, which a `..` walks straight past. Measured against the shipped code with `repo_root = /var/www/html`: `AGENCY_STATE_DIR=<root>/var/../web/leak` was ACCEPTED and resolves to `/var/www/html/web/leak`, and `<root>/../../etc/leak` was accepted raw.
- 2026-08-05: Why that mattered. Everything under `web/` is served over HTTP and exported bundles carry customer content and internal site structure. A misconfigured or malicious `AGENCY_STATE_DIR` could therefore have published customer state to the public internet — the exact outcome the guard exists to prevent — while every gate stayed green, because the plan had specified no test for the class at all.
- 2026-08-05: Fixed as `a195692` by canonicalising `.` and `..` lexically BEFORE the containment check, with no `realpath()` so a directory that does not exist yet still resolves. The orchestrator RE-RAN its own attack against the fixed code rather than accepting the report: `<root>/var/../web/leak` is now rejected with exit 1, `<root>/../../etc/leak` is canonicalised to `/var/etc/leak` with no `..` surviving, and a legitimate `/srv/agency-state` is still accepted. The guard was deliberately not made over-eager — an absolute path outside the repository is valid operator configuration and only the web root is forbidden.
- 2026-08-05: The other half of defect 13 is closed as well. `tests/Unit/AgencyPlatform/State/StateDirectoryTest.php` now exists with 10 tests covering both bypass cases and asserting `exit_code()` against `EXIT_HARD_ERROR` rather than only the exception class. Proven non-vacuous: with canonicalisation reverted the bypass tests fail with `-'/var/etc/leak' +'/var/www/html/../../etc/leak'`. The worker tested the real `path()` end to end with `putenv()`, following the existing `LeadDeliveryResolutionTest` precedent, rather than adding a WordPress stub. Plan record committed as `c7e4d52`. Gates after the fix, re-run independently: `verify:fast` exit 0, deptrac 0, architecture 44, unit 276 / 580.
- 2026-08-05: Method note for the remaining units. THREE times now in Unit 2 the decisive defect was invisible to code review and visible only by executing the shipped code — the `serialize_precision` restore that passed vacuously, the determinism fixture that emitted one reference, and this web-root bypass. Reading confirms intent; running confirms behaviour. Where a property is security-critical or determinism-critical, the orchestrator now probes the built artefact directly instead of reviewing the diff and trusting a green suite.
- 2026-08-05: Unit 2 Task 8 committed as `ddc5347`. Gates: full `ddev composer verify` green, deptrac 0 violations, architecture 44 / 407, unit 284 / 590, integration 94 / 381. The orchestrator verified the Task 8 correction held BEFORE reading the report: neither forbidden `content` assertion was added, `test_the_default_set_never_contains_content()` is present, and `resolve( null, false )` iterates the REGISTERED provider map rather than reading `STRUCTURAL_SLUGS`.
- 2026-08-05: Three Task 8 findings accepted. The plan's prose over-claimed that normalised markup is byte-identical to `serialize_blocks( parse_blocks( $fixture ) )` — WordPress PRESERVES `\r\n` inside block innerHTML, so the claim holds only against the LF-normalised input, which is what `normalize_block_markup()` feeds the parser. The plan's own verbatim test only ever asserted "no `\r`", which is why its code stayed green while its prose was wrong. Providers also needed the `AgencyPlatform\State\Providers` namespace to satisfy the single PSR-4 root, rather than depending on a regenerated classmap in every environment. An orchestrator error is recorded too: the Task 8 brief gave a hardcoded line range that cut off mid-sentence because earlier plan edits had shifted the numbering; briefs for Tasks 9 to 13 locate their section by `grep` instead.
- 2026-08-05: Unit 2 Task 9 completed in two waves and committed as `0dc4c87` — the largest task of the unit. It closed THREE carried-forward corrections at once: `ReferenceResolver` plus `ReferenceScanner::scan()` arriving here as the Task 6 split intended, `BaseStateProvider::detect_references()` restored, and both deferred `content` assertions plus the full `STRUCTURAL_SLUGS` assertion restored. Final gates re-run independently by the orchestrator: `verify` exit 0, deptrac 0, architecture 44, unit 286 / 592, integration 105 / 411.
- 2026-08-05: The two proofs that mattered in Task 9 were really performed. `detect_references()` was shown to FAIL as `return array();` with `The base detect_references() default must scan markup: an empty list means the Task 5 return-array() landmine is still in place. Failed asserting that an array is not empty.` at `ProviderRecordsTest.php:249`, then passed when restored. And the recursion guard works in practice: a synced pattern that references itself terminates and resolves to `synced-patterns:loop`, which is exactly what the `$with_references = false` parameter exists for.
- 2026-08-05: FOURTEENTH PLAN DEFECT, and it is the orchestrator's own fallout rather than worker error. `TemplatesState` and `TemplatePartsState` called `scan_parsed()`, which performs NO resolution — it emits `targetKey`, `targetHash` and `targetIdentity` as `null` by design, and only `scan()` runs them through `ReferenceResolver`. Task 8's text still names `scan()`, but `scan()` did not exist when Task 8 ran because the Task 6↔8 circularity fix had moved it to Task 9. Measured: `Failed asserting that null is identical to 'navigation:primary'.` and `Failed asserting that null is identical to 66.`
- 2026-08-05: Why defect 14 was not cosmetic. §7.4's v1 navigation policy requires the exported navigation's content hash to travel INSIDE the reference, so that a bundle exported with `--providers=templates` can still be judged at finalisation. Templates are precisely the provider that case exists for, so permanently unresolved template references would have silently defeated the policy Unit 3 enforces.
- 2026-08-05: FIFTEENTH PLAN DEFECT. Task 9 Step 3b's third test calls `exported_document()`, a helper built on `StateExporter` — a Task 10 class that cannot exist in Task 9. Measured: `Error: Call to undefined method Tests\Integration\State\ReferenceResolverTest::exported_document()`. Deferred to Task 10 with an explicit instruction to DELETE it rather than weaken it into something that passes.
- 2026-08-05: Both rulings recorded as `57d9735`. Defect 14 granted Task 9 a one-call change in each of the two Task 8 providers and nothing else; the orchestrator verified the result is exactly 2 insertions and 2 deletions across both files, with no adjacent tidying. `MediaReferencesState` deliberately KEEPS `scan_parsed()`, because it scans a synthetic markup source rather than a record and its targets are the attachments themselves.
- 2026-08-05: THE BOUNDARY DISCIPLINE PAID FOR ITSELF HERE, and this is the entry to re-read in later units. The Task 9 worker could have edited `TemplatesState.php` in one line and produced a green gate. Instead it stopped, reported, and waited — the eighth refusal of the engagement. Had it taken the easy green, the code would have worked while the staleness stayed in Task 8's prose for the next reader, and the orchestrator would never have learned that its own circularity fix was incomplete.
- 2026-08-05: Unit 2 Task 10 committed as `b019c88`, exactly its five owned files. Gates at that point: `verify` exit 0, deptrac 0, architecture 44 / 407, unit 293 / 623, integration 115 / 442. The worker also declined to add a dead dependency the plan's Interfaces block listed — `StateDirectory` is named as consumed but no assembly step uses it, because the exporter writes no files and Task 12 owns the writing.
- 2026-08-05: SIXTEENTH PLAN DEFECT — defect 8 recurring, and caught before it propagated. The plan specified `phpcs:ignore WordPress.WP.AlternativeFunctions.file_system_write_file_put_contents`. WPCS groups `file_put_contents` under `file_system_operations` (`vendor/wp-coding-standards/wpcs/WordPress/Sniffs/WP/AlternativeFunctionsSniff.php:154-163`), so the plan's suppression was SILENTLY INERT. The orchestrator verified the grouping in the sniff source, then corrected BOTH occurrences — the Task 10 test at line 3416 and the Task 12/13 guidance at line 4450, where the wrong code would otherwise have been copied into two further tasks. The worker independently confirmed the real code fires when the suppression is removed.
- 2026-08-05: Unit 2 Task 10 bounded review. Model `gpt-5.6-luna`, reasoning level `xhigh`, sandbox `danger-full-access`, session `019fd13c-85b9-7f12-aec3-f0db6b5717a9`, banner captured directly. Verdict: **REJECT** — the second rejection of the engagement and the first in Unit 2.
- 2026-08-05: SEVENTEENTH PLAN DEFECT and the most serious of the unit. CRITICAL: `StateBundle::to_array()` returned the full document INCLUDING records while `is_verified` was false, and the wrapper getters and `provider_slugs()` were unguarded as well. `StateBundle` is the ONLY sanctioned reader precisely so a tampered or unsigned bundle cannot be trusted, so a caller could bypass the signature entirely by choosing the convenient accessor. It survived because the unverified-accessor test covered `records()` alone — finding 5 is why finding 1 existed.
- 2026-08-05: The same review found the FIFTH "test that cannot fail" of this unit. `StateExportTest` hashed `canonical_provider_records()` directly instead of the hash the exporter actually produces, so a broken exporter that hashed wrapper fields would have passed unnoticed. Two MEDIUM findings — tests serialising bundle bytes with `wp_json_encode()` rather than the canonical serializer, and the single-accessor test — and one LOW — a sensitive-data test asserting four literals, so a leak of any other secret passed — were accepted with it.
- 2026-08-05: The review also POSITIVELY CLEARED the exporter itself, which is as valuable as what it found. All nine wrapper fields and all four provider metadata fields leave `stateHash` unchanged, record changes move it, repeated exports give an identical hash with a different `exportId` generated by `wp_generate_uuid4()`, and the canonical bytes are a fixed point ending in exactly one `0a` with no CR. So the exporter was sound; the reader's guard and the tests' aim were not.
- 2026-08-05: All six findings fixed as `426f67f`. Both required break-and-restore proofs were real: reverting the `to_array()` guard failed the new accessor test, and making the exporter hash the whole document failed the rewritten hash test with `The bundle stateHash does not match its providers: the content was altered after signing.` The sensitive-data test was rebuilt from four literals into an ALLOWLIST assertion over 8 seeded secrets across 2 users and 2 posts — denylists fail open, allowlists fail closed.
- 2026-08-05: THE CRITICAL FIX WAS VERIFIED BY ATTACK, not by reading the diff or trusting the worker's list of 14 guarded methods. A reflection probe enumerated ALL 16 public methods of `StateBundle`, invoked each against an unverified instance carrying planted secret content, and confirmed every content accessor throws exit 4 with the planted string leaking nowhere. `is_verified()` is the only ungated answerer and correctly returns false. Enumerating rather than checking a supplied list is the point: the original defect existed precisely because one accessor was missed. Plan record committed as `eb32a86`; gates re-run independently — deptrac 0, architecture 44 / 407, unit 293 / 657, integration 115 / 541.
- 2026-08-05: Unit 2 Task 11 stopped on a red gate and reported three defects rather than editing files outside its list — the NINTH refusal of the engagement, and correct again. Committed after rulings as `fe0bb58`. Gates re-run independently: `verify` exit 0, deptrac 0, architecture 44 / 407, unit 308 / 697, integration 130 / 575.
- 2026-08-05: EIGHTEENTH DEFECT — and it is a REAL SHIPPED BUG rather than a plan defect, the most consequential find of the unit alongside the unverified-accessor leak. `basename( '404.html', '.html' )` yields `'404'`, and PHP SILENTLY CASTS a numeric string to an INTEGER when used as an array key. `StateRecord::create()` declares `string $slug` under `strict_types=1`, so it throws a `TypeError`. The shipped `site-theme/templates/404.html` exists, so `wp agency state-diff` would have fataled on essentially every real block theme.
- 2026-08-05: Why defect 18 survived two tasks. The differ is the FIRST code in the subsystem to read the ACTUAL theme baseline; Tasks 7 and 8 exercised `GitBaseline` only through seeded fixtures with non-numeric names. A fully green suite therefore sat on top of a product that could not run against its own shipped theme. This is the clearest example in the engagement of tests passing over untested reality.
- 2026-08-05: Reading the same function surfaced TWO further problems the worker had not flagged. First, the docblock declared `@return array<string, string>` — a type PHP cannot hold for `'404'` at all, so the declared type was a fiction that helped conceal the bug; corrected to `array<array-key, string>` with a comment naming the cast. Second, `ksort( $markup )` used the default `SORT_REGULAR`, which orders an integer key against string keys inconsistently with the differ's own `strcmp` key ordering — a determinism hazard INSIDE the Git baseline, now `ksort( $markup, SORT_STRING )` so the baseline can never feed a comparison in a different order than the database side produces.
- 2026-08-05: Defect 18 fixed with `(string)` casts at the call sites plus a regression test asserting a `404` template yields the STRING slug `'404'`, key `templates:404`, and no drift. Break-and-restore proof supplied: reverting only the cast reproduces the identical `TypeError` at `TemplatesState.php:95`.
- 2026-08-05: NINETEENTH DEFECT. The plan required providers to accept `?GitBaseline` so the differ could inject a test baseline, but neither `TemplatesState` nor `TemplatePartsState` had a constructor, so `new TemplatesState( $git )` silently discarded the argument and read the real theme directory. Granted and added. The plan's claim that `write_baseline_template` is a `SeedsStateFixtures` helper is also false — that trait has no such method.
- 2026-08-05: The reported "dead `$git` parameter versus PHPStan level 6" DISSOLVED once defect 19 was fixed, and the ruling is worth keeping. The worker's instinct was that PHPStan would force an `@phpstan-ignore`. It does not: the parameter was only dead because the seam it feeds was missing, so fixing the cause removed the complaint. That is exactly the outcome the no-`@phpstan-ignore` rule exists to force, and it is the same lesson as Unit 1's `WP_Error` stub — the suppression is almost never the fix.
- 2026-08-05: Plan records committed as `e9facb2`. Unit 2 Task 11 bounded review dispatched — model `gpt-5.6-luna`, reasoning level `xhigh`, sandbox `danger-full-access`, launched detached so the 600-second tool cap cannot kill it. The brief asks specifically for hostile template names beyond `404` — `0.html`, `007.html`, `1e3.html`, `-1.html` — because a defect of that class rarely has exactly one instance.
- 2026-08-05: Unit 2 Task 11 bounded review. Model `gpt-5.6-luna`, reasoning level `xhigh`, sandbox `danger-full-access`, session `019fd193-7b18-7b61-b420-f6c7fd820c40`, banner captured directly. Verdict ACCEPT WITH FIXES — one HIGH, one MEDIUM, one LOW, all accepted, and ALL THREE are again "a test that would pass while the behaviour is broken".
- 2026-08-05: The review's most valuable output was confirming the differ's BEHAVIOUR is correct by execution, which is what made the three findings test-rigour work rather than logic bugs. In Git mode `navigation`, `synced-patterns`, `content` and `fonts` all returned `db-owned` with `countsAsDrift: false`; a real `templates:page` divergence returned `changed` / `promotable` / drift; in bundle mode an existing navigation row changed after export returned `changed` / `db-owned` / drift. Custom CSS reports drift as `forbidden` rather than database-owned, which is correct under §5.4 rather than an inconsistency.
- 2026-08-05: The hostile-name request paid off exactly as intended. Beyond the shipped `404.html`, the template names `0`, `007`, `1e3` and `-1` ALL produce valid string slugs after the defect-18 fix, and the real and hostile template sets both match the differ's `strcmp` order, confirming `ksort( …, SORT_STRING )`. A defect of that class rarely has exactly one instance, which is why the brief asked for more than the one known case.
- 2026-08-05: The three findings. HIGH — the clean-install Git test asserted only `has_drift === false` and no `removed` entries, so a differ that compared NOTHING AT ALL would pass it, which is precisely what it exists to disprove. MEDIUM — the bundle-mode test exported the bundle BEFORE navigation existed, so `update_navigation()` created a new row and the test proved "added" rather than "changed"; a broken existing-row comparison passed. LOW — the unit test asserted one field of four.
- 2026-08-05: Fixes committed as `44b5ef1`. The rewritten clean-install test derives its expected key set by GLOBBING the theme directories directly — never through `GitBaseline` or the differ — which is the independence property whose absence created the vacuous version. It asserts the exact eight-key set with every entry `unchanged`. Both bundle-mode paths are now separate tests, changed-row and added-row, rather than one test that only ever proved creation.
- 2026-08-05: Defect 18 now carries a PERMANENT triple guard inside the clean-install test: `templates:404` must appear in the expected key set, must survive the strict key-column `assertSame`, and must resolve to an `unchanged` entry. A bug that fataled the differ against this repository's own shipped theme is worth over-guarding.
- 2026-08-05: Non-vacuity proof for the HIGH fix was supplied and is the right shape: making the differ skip Git-backed providers entirely made the rewritten test fail, and the production file was confirmed byte-identical afterwards by `git hash-object` against the HEAD blob. Gates re-run independently by the orchestrator: `verify` exit 0, deptrac 0, architecture 44 / 407, unit 308 / 701, integration 131 / 584.
- 2026-08-05: COUNT OF THE DOMINANT FAILURE MODE, for the unit review and for Units 3 and 4. EIGHT of the nineteen Unit 2 defects are tests that could not fail: the inert `phpcs:ignore` sniff code (twice, defects 8 and 16), the promotion-manifest test that never called `validate()`, the reference-order fixture that emitted one reference, the orchestrator's own replacement for that fixture, the `stateHash` test that hashed a helper rather than the exporter, the single-accessor bundle test that let a CRITICAL leak survive, and now this Git-mode test that passed on an empty report. Every fix in Unit 2 therefore carries a recorded break-and-restore proof rather than a green suite.
- 2026-08-05: Unit 2 Task 12 committed as `9d2d26b` — 8 files, 1,034 insertions. Gates re-run independently by the orchestrator: `verify` exit 0, deptrac 0, architecture 44, unit 308, integration 147 / 622. The ownership boundary that mattered HELD: `Plugin.php` and `Cli/AgencyCommands.php` are untouched by the commit, confirmed from the commit stat, because both belong to Task 13's rebase-gated commit.
- 2026-08-05: The Task 12 REPORT WAS LOST — the wrapper died without writing, the same failure as Task 4 — so the orchestrator verified the task entirely itself rather than accepting any claim. That verification found the commit also carries a 6-line change to `tests/Integration/State/SiteUuidSanitizeTest.php`, which is NOT in Task 12's Files block. The change is coherent and minimal — one test asserting `StateSubsystem::register()` wires the sanitize step — so it is ACCEPTED rather than reverted, on the same reasoning Unit 1 used for two comparable cases. It is recorded as the THIRD instance of the report-after-the-fact pattern and raised for the unit review, because the brief asked for report-BEFORE-change.
- 2026-08-05: ENVIRONMENT GAP FOUND AND FIXED, and it would have failed Task 13 rather than Task 12. `.env` DID NOT EXIST in this worktree: it is gitignored and `scripts/setup` had never been run against `wt/bt-task-2`. Every gate had nonetheless passed, because `verify:fast` needs no database and the integration suite uses wp-phpunit's separate `wordpress_test` database — so a fully green unit sat on top of a site that returned HTTP 000 and a WP-CLI that answered `Error establishing a database connection`.
- 2026-08-05: Why that mattered now. Task 13 REQUIRES a real `wp agency check-overrides` invocation, and this file's own rule is that a missing environment is a FAILED gate and never a skip. Discovering it at the gate would have looked like a code defect. `scripts/setup` was run — it is idempotent, development-guarded, and explicitly never clobbers an existing `.env` or an installed site — and it reported `already exists — skipping` for the client-editor user, the Primary navigation and the Demo page, confirming it adopted the inherited database rather than reinstalling. After it: `wp option get blogname` returns a value, `wp_is_block_theme()` returns 1, the site returns HTTP 200 from inside the container, and `git status --porcelain` is still empty. Same lesson as Unit 1's DejaVu fonts — fix the environment rather than grind against it.
- 2026-08-05: The STDOUT/STDERR separation deferred from Task 10 could NOT yet be proven and is explicitly still open. `wp agency state-export` currently answers `'state-export' is not a registered subcommand of 'agency'`, which is CORRECT at this point: `StateSubsystem` is wired into `Plugin.php` by Task 13's single sequenced line, not by Task 12. One positive signal even so — that error went to STDERR with STDOUT empty, so the separation holds on the failure path. The full proof with captured streams runs once Task 13 registers the command.
- 2026-08-05: ALL THIRTEEN UNIT 2 TASKS COMPLETE. Task 13 committed as `41e26d9` — 11 files, 302 insertions and 629 deletions. Gates re-run independently: `verify` exit 0, deptrac 0, architecture 44, unit 296, integration 156 / 648.
- 2026-08-05: Every Task 13 hard constraint held and was verified from the commit rather than the report. `Plugin.php` gained EXACTLY ONE line — `new \AgencyPlatform\State\StateSubsystem(),`, fully qualified, no `use` import — and lost none. `register()`, `sanitize()` and `verify_env()` are untouched, with the `\WP_CLI::add_command` registration line byte-identical. All three deletions landed: `DatabaseOverrideCheck.php` (200 lines), `DatabaseOverrideClassifyTest.php` (284), `CheckOverridesReportTest.php` (67). Both grep gates return zero matches for `DatabaseOverrideCheck` and for the two drift helpers.
- 2026-08-05: The unit test count FELL 313 to 296 and the fall is exactly right — 4 tests from `CheckOverridesReportTest` plus 13 from `DatabaseOverrideClassifyTest`, with no unit tests added. The worker stated the fall with its arithmetic rather than absorbing it silently, which is what the brief demanded.
- 2026-08-05: THE STDOUT/STDERR PROOF DEFERRED SINCE TASK 10 IS NOW COMPLETE, run by the orchestrator on the real registered command with genuinely separated streams. `wp agency state-export --output=-` exits 0; STDOUT carries 1083 bytes of VALID JSON ending in exactly one `0a` with zero CR; STDERR carries the 274-byte §7.3 sensitivity warning; and a case-insensitive scan for `sensitiv|customer content|warning` inside STDOUT returns ZERO. The `check-overrides` deprecation notice also lands on STDERR. The machine-readable pipe is therefore genuinely uncorrupted, which is the property Task 10 could only assert as a string.
- 2026-08-05: The sharpest Task 13 finding is one the plan would have got wrong in a way that PASSES. Step 3c's alias test was specified to seed with `$this->make_template()`, but that writes into the PHPUnit test database inside an UNCOMMITTED TRANSACTION, which a CHILD WP-CLI PROCESS CANNOT SEE. The test would have run real WP-CLI against an effectively empty site and passed for the wrong reason. The worker seeded through real WP-CLI into the site database instead, including the `wp_theme` term assignment, and verified it end to end. That is exactly the trap this task existed to avoid.
- 2026-08-05: THIRD INSTANCE of the inert-sniff-code defect (defects 8 and 16 being the first two). `proc_open` is not covered by `WordPress.WP.AlternativeFunctions` at all — the emitted code is `WordPress.PHP.DiscouragedPHPFunctions.system_calls_proc_open` — and `fclose()` needs `WordPress.WP.AlternativeFunctions.file_system_operations_fclose`. The worker read the real phpcs output rather than trusting the plan, as the standing note now requires.
- 2026-08-05: TWENTIETH DEFECT, minor but instructive. The plan's "Verified inventory of every `DatabaseOverrideCheck` reference in the repository" was INCOMPLETE: it missed `src/State/Providers/GlobalStylesState.php:102`, a docblock mention written by this unit's OWN Task 8 after the plan was authored. The task's own zero-match grep gate caught it, which is the gate working as designed. Reworded as docblock prose only, the same category the plan already authorises for `SanitizeSteps.php`.
- 2026-08-05: ANCESTRY DEVIATION reported by the worker and resolved by the orchestrator, whose commits caused it. `git merge-base --is-ancestor feat/block-theme-fse-migration HEAD` exits 1 because the integration branch carries two commits made AFTER the task branch forked — `9c2b98f` and `c48ddc7`, both the orchestrator's own tracking records. Verified benign: `git diff HEAD...feat/block-theme-fse-migration -- ':!docs/'` is EMPTY, so there is no code divergence and the merge will be clean. The worker correctly proceeded without rebasing, since this plan forbids branch operations inside a worker task.
- 2026-08-05: Unit 2 Task 13 bounded review dispatched — the fifth and last of the allowed bounded reviews. Model `gpt-5.6-luna`, reasoning level `xhigh`, sandbox `danger-full-access`, launched detached. The brief requires it to SEED REAL DRIFT before checking exit codes, because a clean site cannot demonstrate the `--fail-on-drift` → 1 case and that is precisely where a false pass would hide.
- 2026-08-05: ORCHESTRATOR OVERSTATEMENT CORRECTED IN PLACE, recorded because accuracy matters more than looking consistent. Defect 11 was first written as "every provider inheriting `BaseStateProvider` reports ZERO references". That is FALSE as built: providers scanned inline, and grep confirmed `detect_references()` had NO call sites anywhere in the tree, so it was dead code rather than live data loss. The plan's Step 3c now states the real defect — a designed seam that no provider used — and additionally requires providers to route through it.
- 2026-08-05: Unit 2 whole-unit review. Model `gpt-5.6-luna`, reasoning level `xhigh`, sandbox `danger-full-access`, session `019fd20a-bdf7-74d0-bedb-5d04d048ca0b`, banner captured directly. Verdict: **DO NOT MERGE** - one CRITICAL, one HIGH, one MEDIUM. All three accepted and fixed as `0d1f73f`.
- 2026-08-05: TWENTY-THIRD DEFECT, CRITICAL, and the finding that justifies running a whole-unit review at all. `wp agency state-export --output=/var/www/html/web/leak.json` EXITED 0 AND CREATED THE FILE. Defect 13 had already guarded the DEFAULT state directory, but `--output` never routed through `StateDirectory`, so the protection covered the path nobody types and missed the one they do. Everything under `web/` is served over HTTP and exported bundles carry customer content and internal site structure. FIVE per-task reviews could not see it, because the exporter is Task 10 and the CLI surface is Task 12 - it only exists once they are assembled.
- 2026-08-05: The fix was required to route every output path through ONE shared public guard in `StateDirectory`, not a second copy, on the explicit reasoning that two implementations of a single security rule drifting apart is exactly how this defect arose. Symlink resolution was added too, so a symlinked path component cannot smuggle a write into the web root.
- 2026-08-05: Re-verified by the orchestrator with a WIDER attack set than the review used: absolute into the web root, relative into the web root, `..` traversal, a path nested deep under `web/app/uploads/`, and a SYMLINK pointing at `web/` are all refused with exit 1 and no file created, while a legitimate `var/agency-state` path still writes and `--output=-` still streams. The guard is comprehensive without being over-eager.
- 2026-08-05: TWENTY-FOURTH DEFECT, HIGH, and it FALSIFIED SOMETHING THE ORCHESTRATOR BELIEVED WAS PROVEN. `record( $key, false )` ignored the flag entirely - an independent probe returned `referenceCountWhenFalse: 1`. That parameter is the documented recursion guard `ReferenceResolver` relies on. Task 9's self-referencing synced-pattern test passes for some OTHER reason, so a green test had been taken as evidence the guard worked while the contract was completely unenforced. Implemented across the base provider and both theme providers, with a per-provider test and a break-and-restore proof.
- 2026-08-05: TWENTY-FIFTH DEFECT, the ELEVENTH vacuous test of the unit: a drift test asserting only exit code 1, which any unrelated hard error satisfies.
- 2026-08-05: THE ORCHESTRATOR'S REJECTION OF THE TASK 13 CRITICAL FINDING WAS UPHELD by the unit review on independent verification. The Task 13 reviewer had called the rebase gate CRITICAL and demanded a rebase; the orchestrator rejected that because the only commits breaking ancestry are its own docs-only tracking commits, `git diff HEAD...feat/block-theme-fse-migration -- ':!docs/'` is empty, and rebasing is forbidden here and would rewrite published history over a documentation-only difference. The unit reviewer confirmed the merge-base is `72ba796`, the divergence is documentation-only, and ruled: do not rebase, use the planned `--no-ff` merge.
- 2026-08-05: The unit review's other rulings, all adopted. `EnvironmentConfig::get()` stays unchanged in this unit - every setting routed through it is a string and a blank value is defined as absent; a boolean-aware contract is only needed if a boolean setting is introduced. Fail-fast schema validation is acceptable because bundles are machine-generated and the message carries a JSON pointer plus an explicit statement that validation stopped; the library must not be swapped in a later unit without a NEW aggregation requirement.
- 2026-08-05: The unit review's answer on suite trustworthiness is recorded verbatim rather than softened: the full suite passes but MISSED both the output-path exposure and the ignored reference flag, so it is not by itself a sufficient release gate. That judgement, not the green tick, is the honest summary of this unit's testing.
- 2026-08-05: End-to-end behaviour confirmed by execution in the unit review, not by reading. Export succeeded; a one-byte change to the HMAC key id was REFUSED with exit 4; Git-mode diff returned exit 2 for real drift; bundle-mode diff returned 0 with no drift; `check-overrides` returned 0 without the flag and 1 with it during drift; after cleanup everything returned to 0 and every seeded artefact was gone. Determinism held across a real round trip: two exports gave different `exportId` with the identical `stateHash` `1f17ff...737ac`, and one record change moved it to `289fa0...a7ff0`.
- 2026-08-05: Unit 3's contract verified IN CODE by a live probe, not merely by grep. Two fake strategies were registered through the `agency_platform_promotion_strategies` filter under arbitrary keys and came back correctly keyed and sorted as `["alpha","zeta"]`. `PromotionStrategy`, `PromotionStrategies` and `SchemaValidator::SCHEMA_PROMOTION_MANIFEST` all exist, and the promotion schema FILE is correctly deferred to Unit 3.
- 2026-08-05: UNIT 2 MERGED into `feat/block-theme-fse-migration` as `bd2b233` with `--no-ff`, preserving all 33 checkpoints. Final gates before the merge, re-run independently: `verify` exit 0, deptrac 0 violations and 0 errors, architecture 44 / 407, unit 300 / 678, integration 161 / 680, worktree clean, and no bundle, manifest, `.env`, secret or customer state tracked anywhere in the unit's commits.
- 2026-08-05: THE `commerce-e2e` EXCEPTION IS RESTATED FOR THIS MERGE, as the exception itself requires of every unit that merges under it. `commerce-e2e` is KNOWN RED and is NOT a required check for Units 1 to 3B. The cause is structural: Unit 1's Task 6 deleted the classic rendering path WooCommerce's classic templates depended on, and Unit 4A owns the commerce templates that replace it. The exception expires at Unit 4A and must not be extended silently.
- 2026-08-05: Unit 2 status set to `merged` in the status table.
- 2026-08-05: CI VERIFICATION OF UNIT 2, and the first run proved why step-level reading is mandatory. Run `31014030470` on `394e798` reported conclusion `failure`, but the End-to-end job had genuinely SUCCEEDED; the only real failure was `PHP integration`, with `proc_open(): posix_spawn() failed` and `Tests: 161, Assertions: 662, Errors: 2`. Judging that run by its conclusion would have been wrong in both directions.
- 2026-08-05: The cause was a REAL CI defect in Unit 2's own work. `CheckOverridesAliasTest` shells out to the real `wp` binary to prove `wp agency check-overrides` is genuinely registered, but the `PHP integration` job runs wp-phpunit against MariaDB with NO DDEV, NO WordPress install and NO WP-CLI. The test was failing CORRECTLY - a missing environment is a FAILED gate and never a skip - but it had no executable home. It passed locally only because DDEV supplies both.
- 2026-08-05: ORCHESTRATOR INTEGRATION DECISION, recorded like the `AgencyCommands` transfer and the `phpcs.xml` ruling because `phpunit.xml` and `.github/workflows/ci.yml` are not Unit 2's to own. The alias test was given the treatment this repository already uses for commerce: a separate suite in its own directory with its own composer script, run only in the job that has the environment. `tests/Integration/State/CheckOverridesAliasTest.php` moved to `tests/Cli/`, a `cli-integration` suite was added over `tests/Cli`, a `test:integration:cli` script was added, and ONE step was added to the `e2e` job. `verify` and `verify:fast` deliberately do NOT run the new suite so both stay usable without DDEV. Job count unchanged at 6; the commerce job untouched. Merged as `71e6df7`.
- 2026-08-05: The move surfaced a detail worth recording: changing the file's directory depth broke `proc_open`'s working directory, so `dirname( __DIR__, 3 )` became `dirname( __DIR__, 2 )`. Verified empirically - `wp` works from `/var/www/html` and fails from `/var/www`. A silent cwd change would have made the test fail for a reason unrelated to the alias.
- 2026-08-05: Run `31016415792` on `71e6df7` confirmed the fix - `PHP integration` SUCCEEDED - but both DDEV jobs then died at `Install PHP dependencies (inside DDEV)` with the documented `curl error 56 ... QUIC connection has been shut down` fetching WordPress core. It EXHAUSTED ALL THREE RETRIES that Unit 1 added after five occurrences.
- 2026-08-05: Sharpened observation for Unit 4B, which already owns this item. The 3x retry helps against isolated blips but was defeated by a sustained outage window, because three attempts in quick succession all hit the same downed CDN path. The durable fix is caching the WordPress core archive between runs rather than retrying the same fetch faster. This run is evidence that the item should not slip.
- 2026-08-05: The compound defect appeared exactly as this file predicts. The install failure marked the e2e, accessibility, visual AND parity steps SKIPPED rather than failed, so a run reporting `failure` had never executed the gates that matter. Re-running the failed jobs is the documented remedy and it was used.
- 2026-08-05: UNIT 2 CI GREEN after the re-run of `31016415792`. Every required job passes: PHP QA success, Frontend success, PHP integration success, End-to-end SUCCESS. `migration-baseline-capture` correctly skipped under its cost guard. `commerce-e2e` failed at `Commerce e2e journeys (Playwright, COMMERCE=1)` - the exempted structural failure at the expected step, not at dependency install.
- 2026-08-05: EVERY GATE VERIFIED TO HAVE ACTUALLY EXECUTED, not merely reported green, by reading each End-to-end step: `WP-CLI alias integration suite` success, `Run e2e suite` success, `Run accessibility suite` success, `Run visual regression suite` success, `Run migration and editing parity suites` success. The three skipped steps are legitimate mutually-exclusive branches, and one of them is the proof - `Skip visual regression (no committed baselines yet)` was SKIPPED, which means committed baselines exist and the real visual step ran rather than being bypassed.
- 2026-08-05: The disposable capture refs were deleted from origin and zero `ci-capture/*` refs remain. Unit 2 is complete: 13 tasks, 25 plan defects corrected, 5 bounded reviews plus 1 whole-unit review, integration branch at `71e6df7` and pushed.
- 2026-08-05: UNIT 3A STARTS. Scope is Tasks 1 to 21 of `2026-08-02-block-theme-task-3-promotion-lifecycle.md` on `feat/bt-task-3-promotion-lifecycle`, worktree `C:\Users\Aleksandar\Projects\wt\bt-task-3`, created from integration `fb24141`. Tasks 22 and 23 are Unit 3B and are not started. Model policy for this unit is unchanged from Unit 2: implementation `opencode-go/deepseek-v4-flash` variant `max` agent `build` for every task; bounded `gpt-5.6-luna` at `xhigh` on tasks 4, 9, 12, 13, 14, 15 and 21 only; one `gpt-5.6-luna` xhigh unit review at the end. The orchestrator reviews every other diff itself.
- 2026-08-05: Unit 3A worktree bootstrapped. Both gaps the handoff predicted were real: no `vendor/`, no `.env`, no `node_modules/`. `ddev composer install --no-interaction --prefer-dist` exit 0, then `ddev exec bash scripts/setup` exit 0. The environment gate the handoff demanded before any delegation now answers: `ddev exec wp option get blogname` returns `Agency Starter Baseline Capture`, `wp_is_block_theme()` returns 1, and the stylesheet is `site-theme`. The database volume did survive the handover, so no reinstall happened and the Unit 1 baseline-capture site is intact.
- 2026-08-05: UNIT 3A PRE-TASK-1 AUDIT, first finding, and it FALSIFIES A CLAIM IN THE HANDOFF PROMPT rather than confirming one. The handoff warned that `phpcs.xml:56`'s `WordPress.Security.EscapeOutput.ExceptionNotEscaped` exclusion, scoped to `agency-platform/src/State/*`, would NOT cover promotion code in a new directory, and told the orchestrator to decide deliberately whether to widen it. That is wrong. PHPCS expands `*` to `.*` in an `exclude-pattern`, so `src/State/*` already matches `src/State/Promotion/*`. Proven by planting three identical probe classes, each throwing `new \RuntimeException( 'bad ' . $value )`: `src/State/Promotion/ZzProbe.php` CLEAN, `src/State/ZzProbe2.php` CLEAN, and the control `src/Health/ZzProbe3.php` ERROR at line 10 with `WordPress.Security.EscapeOutput.ExceptionNotEscaped`. The control firing is what makes the two clean results meaningful rather than vacuous. **`phpcs.xml` therefore needs NO change for Unit 3A and must not be widened.** All three probes were deleted and `git status --porcelain` is empty.
- 2026-08-05: UNIT 3A PLAN DEFECT 1, CRITICAL, and it is the same species as Unit 1's Task 3 and Unit 2's Task 8 — a task whose declared gate is red by construction. Task 7's `PreparablePromotionStrategy extends PromotionStrategy` redeclared `prepare( StateRecord, string ): StagedPromotionEntry`, calling it "Task 2's corrected prepare contract". The correction never landed: the MERGED `src/State/PromotionStrategy.php` declares `prepare( StateRecord $record, string $target_path ): array` returning `array{preparedPath, preparedHash, originalHash}`. PHP return types are invariant for non-class types, so narrowing `array` to a class type is a fatal declaration-compatibility error at class load. Task 7 could never have loaded, let alone passed its gate.
- 2026-08-05: Defect 1 corrected in the plan, and the fix was chosen for a specific safety reason rather than convenience. The sub-interface now adds `stage( StateRecord, string ): StagedPromotionEntry` and does NOT redeclare `prepare()`. Widening Task 2's merged interface was rejected: it is out of Unit 3A's grant, and it would push a `State\Promotion\StagedPromotionEntry` dependency upward into a Task 2 interface that Unit 3B also implements. `AbstractBlockTemplateStrategy::prepare()` now exists only to satisfy the inherited signature and REFUSES with `PromotionException::hard()` naming `stage()`. It deliberately does not delegate to `stage()` plus `commit()`: that would be a second write path reaching the filesystem outside the run-level transaction, which is precisely how Unit 2's `--output` web-root leak happened, and it would silently break the all-or-nothing guarantee `commit_all()` exists to provide. The plan now requires a test asserting the refusal, its `PromotionExitCode::HARD_ERROR`, and that the message names `stage()` — not merely that something threw.
- 2026-08-05: UNIT 3A PLAN DEFECT 2, HIGH. Task 6's `PreparedFileWriter::stage()` carried `@return array{themeRelativePath…, previousBytes…}` above a native return type of `StagedPromotionEntry`. PHPStan level 6 rejects a `@return` that is not a subtype of the native type, so the declared gate was red by construction here too. Corrected: those fields are what the returned entry's `manifest_fields()` carries, and the docblock now says so.
- 2026-08-05: UNIT 3A PLAN DEFECT 3, HIGH, and it is an ENVIRONMENT defect the handoff did not predict. Task 5 required "`GitRepositoryTest` runs against the real repository". It cannot. Unit 3A works in a git worktree, so `.git` is a FILE reading `gitdir: C:/Users/Aleksandar/Projects/wordpress-template/.git/worktrees/bt-task-3`. That Windows path is outside the DDEV mount, and the integration suite runs INSIDE the container, where every git command fails with `fatal: not a git repository: /var/www/html/C:/Users/...`. Proven by running it. `git` itself is present at `/usr/bin/git`; only the repository is unreachable. An ambient-repository test would also assert over whatever branch and dirty state the developer happened to have, so it was non-deterministic even where it did run.
- 2026-08-05: Defect 3 corrected by making the test hermetic rather than by making the environment reachable. `GitRepositoryTest` now builds a throwaway fixture repository — the SAME pattern Task 10's `PromotionPreparerTest` already used, so the plan became self-consistent instead of gaining a new convention. `GIT_CONFIG_GLOBAL` and `GIT_CONFIG_SYSTEM` are pinned to `/dev/null` so a host `~/.gitconfig` cannot change the result. The correction also closes two would-be vacuous tests that the original wording invited: `current_branch()` must be asserted BOTH on a branch and after `git checkout --detach` (an implementation that always returned `null` would otherwise pass), and `dirty_paths()` must be asserted empty, then with an untracked file, then with a modified tracked file (an implementation that always returned an empty array would otherwise pass — and Task 10's whole run-level dirty-tree refusal rests on that one method). The "it never skips" rule is kept: a missing git binary FAILS the test.
- 2026-08-05: UNIT 3A PLAN DEFECT 4, MEDIUM, found inside defect 3's own correction text. The original discovery assertion required `GitRepository::discover()->root()` to equal `realpath( dirname( __DIR__, 7 ) )`. Seven is correct only from the SOURCE file `src/State/Promotion/GitRepository.php`; `__DIR__` in `tests/Integration/Promotion/` is three levels below the repository root, so the assertion was wrong by five levels and would have failed or, worse, silently compared two wrong paths. The plan now asserts the override branch with `AGENCY_REPO_ROOT` pointed at the fixture, and asserts the parent count by reflection over `GitRepository`'s own filename — which is where the count is actually used, and which holds in the container, in a worktree and in CI alike.
- 2026-08-05: UNIT 3A PLAN DEFECTS 5 and 6, both LOW, both corrected. Defect 5: the "Consumed Task 2 interfaces" block claimed Task 2's `prepare()` returned `StagedPromotionEntry`; it now states the real `array{preparedPath, preparedHash, originalHash}` shape. Defect 6: the File Structure listing omitted `StagedPromotionEntry.php` and `CanonicalJsonFileWriter.php`, both of which Task 6 creates. Every other Task 2 symbol the plan names was checked against the merged code one by one — `CliOutput::emit`, `EnvironmentConfig::get`, all four `Normalizer` statics, `PromotionPolicy::PROMOTABLE`, five `ReferenceScanner` members, `StateRecord::content_hash`/`to_array`, `StateRegistry::provider`, `StateBundle::load`, `StateDirectory::ensure`, `SchemaValidator::SCHEMA_PROMOTION_MANIFEST`, `HmacSigner::PURPOSE_*`, and `StateException::hard_error`/`tamper`/`exit_code` — and every one exists with the claimed shape. `WP_Upgrader::create_lock()` and `release_lock()` were also confirmed to exist as static methods in the pinned WordPress 7.0.2, so Task 12's Decision 13 holds.
- 2026-08-05: OWNERSHIP CLARIFICATION recorded before it can bite, in the same style as Unit 2's three transfers. This file's Unit 3B grant says 3B owns "only the Theme JSON adapter, `PromotionStrategyRegistrarTest.php`, and the Global Styles promotion surface named in Tasks 22–23". But Task 7 — Unit 3A — is the task that CREATES `tests/Integration/Promotion/PromotionStrategyRegistrarTest.php`, because the Release 3 allow-list assertion is the whole point of Task 7. The grant is therefore read as: Unit 3A creates the file and owns its Release 3 assertions; Unit 3B owns only the later MODIFICATION that adds the third strategy when Task 23 registers `global-styles`. Unit 3B must not rewrite the Release 3 assertions.
- 2026-08-05: Unit 3A baseline established on the bootstrapped worktree and it matches the handoff exactly: `ddev composer verify` exit 0, deptrac 0 violations, architecture 44 / 407, unit 300 / 678, integration 159 / 662. Plan corrections committed separately as `623dd50` before any implementation started.
- 2026-08-05: DELIVERY-MECHANISM DEFECT found on the first delegation and fixed for the whole unit. The OpenCode wrapper failed twice with `OpenCode response did not finish successfully`, having written nothing. It was NOT the known transient: reading only the terminal event of the artifact showed `reason: "unknown"` with zero input and output tokens, and the two recorded tool calls showed the worker had done exactly the right thing — `git status`, then grep for its task heading — before dying on the next step, which would have been reading the 3,700-line plan file. A trivial probe prompt through the same wrapper returned `MODEL: opencode-go/deepseek-v4-flash/max` and the word READY, so the provider and the model policy were both healthy and this was a context blow-out, not an outage.
- 2026-08-05: Fix adopted, and it keeps the handoff's intent rather than its letter. The handoff forbids hardcoded line ranges because Unit 2 lost a task to a stale one. A small extractor now pulls a task's section out of the CURRENT plan file BY HEADING and inlines it into the brief, so there is still no hardcoded line number anywhere, the worker never opens the 3,700-line file, and the worker always sees the orchestrator's corrections. Task 1 then succeeded on the first attempt with the same model and variant.
- 2026-08-05: Unit 3A Task 1 implemented. Model `opencode-go/deepseek-v4-flash`, variant `max`, agent `build`, session `ses_02d516794ffenSIYMF0LbY3q7q`, banner confirmed `opencode-go/deepseek-v4-flash/max`. Commit `bcc0149`, exactly the seven owned files, 424 insertions. The worker ran the required test-first FAIL (12 errors, `Class "AgencyPlatform\State\Promotion\PromotionException" not found`) and supplied two break-and-restore proofs with exact failure text and byte-identical restores.
- 2026-08-05: FIRST GENUINE SHIPPED-CODE DEFECT of Unit 3A, and the worker found it in itself rather than the orchestrator finding it — the report's "temptations" section flagged that `PromotionOutcome::exit_code()` derived refusals from the outcome map and ignored the `RecordRefusal` list the object is constructed with. That is worth recording because asking workers what they were tempted to change is what surfaced it. Two silent holes followed. A caller that recorded refusals without ALSO marking those records `refused` in the outcome map produced exit 0 while the manifest carried a full refusal report — a promotion run reporting success with refusals in it. And `successes()` counted every value that was not the literal string `refused`, so a typo, or a status a later task adds without updating this class, was silently counted as a success.
- 2026-08-05: Fixed by the orchestrator as `3865bcc`, failing closed in both directions. Statuses are now checked against an allow-list of the four documented success values plus `refused`; anything else is a hard error naming the record and the value. The set of record keys marked refused must equal the set of distinct record keys in the refusal report, checked BOTH ways. It compares sets rather than counts on purpose, because one record may legally carry several refusals — several unresolved references in one template — and a count comparison would have forbidden that.
- 2026-08-05: The fix carries its own break-and-restore proof rather than a green suite. Removing both constructor guards failed exactly the four new guard tests, including the one asserting the thrown exception's exit code rather than merely that something threw. Restoring returned `PromotionOutcome.php` to sha256 `e77c7b1a2eec6a8e1cdfa9b9f1937513d99f7296f31f61cf235eb717004f32fc`, identical to before the break, and the suite returned to `OK (42 tests, 51 assertions)`.
- 2026-08-05: `PromotionSettingsTest` added in the same fix, closing a gap the worker correctly reported and correctly did NOT fix on its own because the file was outside its stated grant. The plan listed no test for `PromotionSettings`, yet its five parsing methods decide lock TTLs, backup retention and backup chunk sizes. A malformed value must fall back to the documented default rather than to 0, and a 0 lock TTL would make every lock instantly reclaimable. The `putenv` suppression copies `StateDirectoryTest`'s existing file-level `phpcs:disable` verbatim, with the sniff code read from real phpcs output.
- 2026-08-05: TWENTY-FIRST-STYLE PLAN DEFECT, reported by the Task 1 worker and confirmed: the plan's `PromotionSettings::integer( string $name, int $default )` / `text( string $name, string $default )` signatures trip `Universal.NamingConventions.NoReservedKeywordParameterNames.defaultFound`, a WARNING, and this repository fails on warnings. The worker renamed the parameter to `$default_value` rather than suppressing the sniff, which is the correct direction. Parameter names are not part of the contract, so no caller is affected. This is the same species as Unit 2's three inert-`phpcs:ignore` defects: the plan's stated interface did not survive contact with the real sniff set.
- 2026-08-05: Residual risk recorded for Task 12, not fixed in Task 1. `PromotionSettings::integer()` accepts `"0"` because `ctype_digit( '0' )` is true, and the plan's own wording ("casts only an all-digit value") permits it. `AGENCY_PROMOTION_LOCK_TTL=0` would therefore make every per-record lock immediately reclaimable. Task 12 owns the lock manager and must decide whether a zero TTL is a legitimate operator choice or must be floored.
- 2026-08-05: Unit 3A Task 2 implemented. Model `opencode-go/deepseek-v4-flash`, variant `max`, agent `build`, banner confirmed. The worker RAN TO COMPLETION BUT DID NOT COMMIT, and that was the correct call: `verify:fast` went red on a test outside its ownership and it reported before touching it. That is the fourth time across this engagement that a worker refusing a red gate was right.
- 2026-08-05: OWNERSHIP TRANSFER TO UNIT 3A, decided 2026-08-05 — `tests/Unit/AgencyPlatform/State/SchemaValidatorTest.php`, ONE test method only. Unit 2's `0771a08` wrote `test_the_promotion_manifest_constant_exists_and_missing_schema_is_a_hard_error`, which pins the promotion-manifest schema FILE's ABSENCE and the "not found" failure. Its own docblock says the file "is that track's to ship. Until it exists…", so Unit 2 anticipated this exact moment. Task 2 of Unit 3A ships that file, so the test must flip direction or Unit 3A can never be green. Unit 3A therefore owns that single method; every other method in the file stays Unit 2's and was not touched.
- 2026-08-05: The flipped test is STRICTLY STRONGER than the one it replaces, which is why the transfer was accepted rather than the test being deleted. It now asserts the constant's value, that `schema_path()` ends in the right filename, that the file EXISTS, and that validating a BUNDLE document against the MANIFEST schema is a hard error naming that schema. The last assertion is the one that can fail: an empty or permissive schema file would let the bundle straight through, and the old assertion could not have detected that. The "not found" branch is NOT lost — `test_a_custom_schema_dir_is_used_by_both_validate_and_schema_path` still covers it by pointing a validator at a directory that does not exist. That was verified before the change, not assumed.
- 2026-08-05: UNIT 3A PLAN DEFECT 7, and it is a CROSS-TASK one the orchestrator predicted from Task 3's interface list while Task 2 was still being written, then confirmed in the shipped code. Task 2's schema gave `activeTheme` `additionalProperties: false` with only `stylesheet` and `version`, and `PromotionManifest` deliberately whitelisted those two keys. But `StateBundle::active_theme()` returns THREE keys — `stylesheet`, `version` and a nullable `gitCommit` — and `BundleView::header()` hands that straight to `create()`. Every manifest built from a REAL bundle header would therefore have failed schema validation on the forbidden `gitCommit` property.
- 2026-08-05: Why defect 7 mattered more than its size suggests. Nothing in Tasks 2 to 9 builds a manifest from a real bundle header; the first code that does is Task 10's `PromotionPreparer`. The failure would have surfaced SIX TASKS after its cause, as a schema error inside the preparer, with the actual defect two commits back in a file nobody was looking at. Fixed by carrying `gitCommit` through both `active_theme()` and `bundle_active_theme()` and declaring it in the schema as a required, nullable sha1. Carrying it is also right on the merits: it records which theme commit a promotion was prepared against, which is exactly the provenance an operator needs.
- 2026-08-05: Defect 7's fix carries a break-and-restore proof. Deleting the `gitCommit` propagation — which is precisely the shape Task 2 originally shipped — failed 6 tests and errored 1 more, with the schema reporting `Required property missing: gitCommit, data: {"stylesheet":"site-theme","version":"0.1.0"} at #->properties:activeTheme`. Restoring returned `PromotionManifest.php` to sha256 `a3ec64adda45c6e7005626b669d0c8c11502f5c30cb8ee52dfff78e834b41a38`, identical to before the break, and the suite returned to `OK (37 tests, 96 assertions)`. Two new tests pin both halves together so the schema and the code cannot drift apart again.
- 2026-08-05: Unit 3A Task 2 committed as `f619112`. Gates: `verify:fast` exit 0, deptrac 0 violations, architecture 44 / 408, unit 369 / 807. The worker's four recorded design decisions were all accepted: `create()` and `to_array()` never emit `hmacKeyId`/`hmac` (the schema describes the SIGNED stored document, and Task 4 signs before it validates); all five `with_*` methods carry a two-sided immutability test; `promotionId`/`exportId` share one real UUID regex with rejection tests; and `record_keys()` sorting is proven from a DESCENDING insertion order rather than an already-sorted one.
- 2026-08-05: UNIT 3A PLAN DEFECT 8, CRITICAL, found by the orchestrator while reading Task 4 ahead of dispatch and corrected as `285c34b` before any worker saw it. Task 4's `ManifestStore::write()` took an operator-supplied path and wrote to it with NO containment check — a direct recurrence of Unit 2's twenty-third defect, the CRITICAL finding of its whole-unit review, where a second write path bypassed the guard and let customer state be written into the public web root. A signed promotion manifest carries the target site UUID, every prepared file path and every content hash, and everything under `web/` is served over HTTP.
- 2026-08-05: Defect 8's correction calls `StateDirectory::resolve_output()` rather than adding a second containment check, on Unit 2's own recorded reasoning that two implementations of one security rule drifting apart is how the defect arose. `resolve_output()` returns the canonical path the caller must write, so the guard and the write cannot disagree. Before requiring it, the orchestrator confirmed it actually works inside the container: `GitBaseline::repo_root()` reads `AGENCY_REPO_ROOT` or walks up from `ABSPATH` looking for `web/` plus `composer.json`, and never shells out to git — which matters here precisely because git does NOT work in this worktree from inside DDEV. The plan now also requires the guard to be proven BY ATTACK with five cases, each asserting both the throw and that no file was created.
- 2026-08-05: SECOND DELIVERY-MECHANISM DEFECT, diagnosed rather than retried, and it has a hard number attached. The Task 3 dispatch failed with `Exception calling "Start" with "0" argument(s): "The filename or extension is too long"`. Reading `invoke_opencode.ps1` showed why: line 360 appends the ENTIRE prompt text to the process argument list, so the brief travels on the Windows command line, which `CreateProcess` caps at 32,767 characters. Task 1's brief was 27,224 bytes and Task 2's was 29,881 — both squeaked under. Task 3's was 32,670 and did not. Seven of the remaining sections are larger still, so this would have blocked most of the unit.
- 2026-08-05: Fixed structurally rather than by trimming prose, because trimming would have re-hit the ceiling at Task 9, whose section alone is 15,753 characters against an 11,487-character shared-constraints block. The static two thirds of every brief — how to run commands here, the shared constraints, the hard rules, the gate and commit policy, the required report — now live in ONE file, `.brief/shared.md`, inside the worktree, which each worker reads as its first action. Only the task section and the orchestrator's task-specific notes travel on the command line. Task 3's brief went from 32,670 bytes to 13,834, and the generator now warns above 28,000.
- 2026-08-05: `.brief/` is hidden from git through `.git/info/exclude` rather than `.gitignore`, so no tracked file changes and `git status --porcelain` stays empty — verified with `git check-ignore -v`. Note for the Unit 3B handoff: linked worktrees read the COMMON git directory's exclude file, not the per-worktree one, so the entry landed in `C:/Users/Aleksandar/Projects/wordpress-template/.git/info/exclude` and is shared with the main checkout. It is untracked local configuration and is never committed, but it should be removed when the engagement finishes.
- 2026-08-05: THIRD AND WORST DELIVERY-MECHANISM DEFECT — SILENT PROMPT CORRUPTION, and it is the most important entry of Unit 3A so far because it produced working, green, reviewed code that was built on falsified inputs. **Every double quote was stripped from the brief between the file on disk and the worker.** The brief file was verified correct on disk, containing `<!-- wp:template-part {"slug":"site-header","theme":"site-theme","area":"header"} /-->`. The worker received `<!-- wp:template-part {slug:site-header,theme:site-theme,area:header} /-->`. The `invoke_opencode.ps1` argument-escaping function looks correct on inspection, so the loss is below it, in `CreateProcess` argument handling or inside OpenCode itself; the layer was not isolated further because the fix does not depend on which one it is.
- 2026-08-05: How it was caught, which is the transferable part. The gate was GREEN — `verify:fast` exit 0, deptrac 0, 44 architecture, 377 unit, and a full `test:integration` at 166 / 691 — and both required break-and-restore proofs were real and correctly performed. Nothing in the gates could have found this. It was caught only because the worker's plan-discrepancy section said the plan's fixtures were WordPress's "legacy unquoted short-hand form", and the orchestrator knew the plan's fixtures were strict JSON. **Asking every worker to report plan discrepancies, and then actually checking one that sounded wrong, is what caught it.** The same question caught the only shipped-code defect in Task 1.
- 2026-08-05: Why it was reverted rather than patched. No unquoted block-attribute form exists: `serialize_block_attributes()` calls `wp_json_encode()`, so block comment delimiters always carry strict JSON. The worker had added a decoder, an encoder and a `split_top_level` parser for the fictional form — roughly half of `ResolvedTemplateNormalizer.php`. The harm was not the dead code but the coverage: seven of the eight normalizer tests asserted on markup WordPress can never emit, so the one path that actually runs in production had a single test. Reverted as `0e997a1`, which is a revert commit and not a history rewrite; `65c9cd4` stays in the log so the record is honest.
- 2026-08-05: Transport fixed so no later task can repeat it. Brief content no longer travels on the Windows command line at all. The full brief is written to `.brief/<task>.md` inside the worktree and the prompt is a short, quote-free pointer to it, checked by the generator for the absence of any double quote. The prompt also now tells every worker that WordPress serialises block attributes as strict JSON and that a fixture with unquoted keys must be REPORTED rather than implemented — a second, independent tripwire in case the transport ever regresses.
- 2026-08-05: Tasks 1 and 2 audited for the same corruption and are CLEAN. The only damage to Task 1 is cosmetic: a docblock reading `because the text contains hmac.` instead of `contains "hmac".`. Task 2's schema is unaffected because the worker authored the JSON itself rather than copying a quoted fixture, and its keys are correctly quoted. A scan for unquoted-JSON-style fixtures across every promotion source and test file returns only legitimate PHPStan array-shape docblocks.
- 2026-08-05: Unit 3A Task 3 REDONE on the fixed transport and committed as `8555cad`. Model `opencode-go/deepseek-v4-flash`, variant `max`, agent `build`, session `ses_02d2c290dffefciXWJAXKZhR6w` for the corrupted run and a fresh session for the redo. The fixtures came through correctly this time, verified in the committed test file. The redo produced 90 lines of normalizer against the corrupted run's 249, because the fictional short-hand decoder, encoder and top-level splitter were never written. It carries FIVE break-and-restore proofs, all byte-identical: both normalizer methods made no-ops, the exit-code mapping collapsed in BOTH directions, and `hash_markup` switched to `hash_string` instead of the content map. Gates: `verify:fast` exit 0, deptrac 0, architecture 44 / 408, unit 378 / 816, integration 167 / 691.
- 2026-08-05: UNIT 3A DEFECT 9 — A REAL SHIPPED BUG, found by PROBING THE BUILT CODE rather than by reading the diff, which is the Unit 2 lesson this entry exists to reinforce. `ResolvedTemplateNormalizer` collapsed ANY delimiter left with no attributes to the self-closing form. So `<!-- wp:navigation {"ref":12} -->…<!-- /wp:navigation -->` became `<!-- wp:navigation /-->…<!-- /wp:navigation -->`, orphaning the closing delimiter and stranding every child block outside its parent — silent corruption of the promoted template file.
- 2026-08-05: Why defect 9 is not an edge case, and why no gate could see it. Most `core/navigation` blocks carry `ref` and nothing else, and `ref` is precisely what the §7.4 v1 policy removes, so this is the MOST COMMON shape this code will ever process. The same hole applied to a paired `core/template-part` whose only attribute was the injected `theme`. The suite could not catch it because the one paired-form test carried a second attribute (`layout`), so something always remained and the empty branch was never reached with a paired delimiter; `$closing` was computed AFTER the empty check and therefore only ever applied to the non-empty path. The origin is the plan text itself — "emit `<!-- wp:name /-->` when nothing remains" is right for a self-closing delimiter and wrong for a paired one — so the worker implemented it faithfully.
- 2026-08-05: Defect 9 fixed as `64c91d6` by moving `$closing` above the empty check so it applies to both branches. Break-and-restore proof: reinstating the hard-coded `' /-->'` fails exactly the two new paired tests with the orphaned-closer diff visible in the output, and restoring returns the file to sha256 `f880d51532cf272dc16fc3f4244a7d223eb285738bff68353553d15284971cf4`. A third test pins the self-closing case so the fix cannot be "corrected" back by making every delimiter paired. Gates after the fix: `verify:fast` exit 0, deptrac 0, architecture 44 / 408, unit 381 / 819, integration 167 / 691.
- 2026-08-05: Unit 3A Task 4 implemented and committed as `56f785e`. Model `opencode-go/deepseek-v4-flash`, variant `max`, agent `build`. The worker found UNIT 3A PLAN DEFECT 10 on its own and it is the FIFTH instance of the inert-sniff-code family: the plan's approved suppression `WordPress.WP.AlternativeFunctions.file_system_operations_*` is a WILDCARD, and `phpcs:ignore` is an exact-match key lookup with no wildcard expansion, so the plan's own approved line would have suppressed nothing. It used the real per-function codes instead and cited the phpcs source. Gates: `verify:fast` exit 0, deptrac 0, architecture 44 / 408, unit 381 / 819, integration 184 / 723.
- 2026-08-05: DEFECT 11, found by the orchestrator testing the suppressions in the OTHER direction. Deleting every `phpcs:ignore` from `ManifestStore` raised 9 warnings across 9 lines, proving the rest load-bearing — but the file carried 11 suppressions for those 9 warnings. `WordPress.WP.AlternativeFunctions.json_decode_json_decode` does not exist in this WPCS version, so that suppression was inert in `ManifestStore` and in `ResolvedTemplateNormalizer`. `json_encode_json_encode` is real and was kept. Removed as part of `f531e7b`. The lesson recorded for later units: count suppressions against warnings, because a green phpcs run cannot reveal an excess one.
- 2026-08-05: EXECUTION EFFICIENCY NOTES CREATED at the user's request and committed in `f531e7b` as `docs/superpowers/plans/EXECUTION-EFFICIENCY-NOTES.md`. It is a forward-only operating manual for the Unit 3B, 4A and 4B orchestrators — the tooling to reuse rather than rebuild, the three delivery failures it prevents, gate costs and which suite to run when, the environment facts every worker otherwise rediscovers, orchestrator token discipline, when a Codex review is worth its cost, the four-question pre-dispatch audit, the known-good decisions not to re-litigate, and CI economics. The rule stated in the file is that an entry earns its place only if it changes a future decision.
- 2026-08-05: Unit 3A Task 4 BOUNDED REVIEW — the first of the seven allowed. Model `gpt-5.6-luna`, reasoning level `xhigh`, session `019fd314-0cd0-7e33-a1c8-97e028a555c6`, sandbox `danger-full-access`, launched detached so the tool timeout could not kill it. It was given the task brief, the commit, and three sharp questions. It returned three findings and found EVERY ONE by executing the shipped code rather than by reading it.
- 2026-08-05: DEFECT 12, HIGH, and the entry that justifies the whole bounded-review budget. `ManifestStore::write_canonical()` ESCAPED the web-root guard. It called `wp_mkdir_p()` and `chmod()` on the UNRESOLVED promotions directory before `write()` applied `StateDirectory::resolve_output()`. The reviewer symlinked `var/agency-state/promotions` at a directory under `web/`, used a promotion id containing a slash, and its probe printed `canonical-mkdir: rejected exit=1 web_child_exists=yes` — the manifest correctly refused, and a directory created inside the public web root anyway.
- 2026-08-05: WHY THE ORCHESTRATOR'S OWN ATTACK TESTS MISSED DEFECT 12, recorded because the reasoning error is the reusable part. Unit 3A had already required the guard to be proven BY ATTACK and had written five attack cases. All five attacked `write()`, which was correctly guarded and passed. None attacked `write_canonical()`, because it builds its path from `state_dir()` and was therefore reasoned safe by construction. That reasoning was the defect. Attack every path that reaches the filesystem, including the ones that look safe by construction.
- 2026-08-05: Defect 12 needed TWO fixes because the escape needed two things. `canonical_path()` now refuses any promotion id that is not a bare UUID — the id is interpolated straight into a filesystem path, so a slash or a `..` segment became extra path components. And `write_canonical()` now creates nothing before `write()` has resolved the path, with the private-mode chmod moved to the resolved directory after a successful write. DEFECTS 13 and 14, both MEDIUM, were fixed in the same commit: a failed `rename()` was ignored, so `write()` reported success while the manifest was never created and the temp file survived; and the invalid-shape test could not pin the verification order, because it deletes `baseCommit` while `from_array()` only inspects `schemaVersion`, so moving `from_array()` ahead of the HMAC check would still have produced exit 4. All fixed as `00fcb2a`.
- 2026-08-05: TWO HONEST NON-CLAIMS recorded with the defect-12 fix, because overstating a proof is how a vacuous test survives. First, the ordering change alone is defence in depth: once the UUID guard holds, the nested-parent path is unreachable and breaking the ordering by itself fails no test. The break-and-restore proof therefore removes BOTH protections, and the symlink test then fails with the reviewer's exact evidence, `+ 0 => 'new-parent'` appearing inside the web root. Second, a first attempt to pin the ordering through DIRECTORY PERMISSIONS was written, run, and then DELETED: probing showed `chmod()` is a NO-OP on this DDEV mount — it returns `true`, changes nothing, and directories are created 0777 whatever mode is passed. That assertion could never have failed on this host, which is the exact defect family this engagement keeps finding. The environment fact is now recorded in the efficiency notes so no later unit writes a permission assertion here.
- 2026-08-05: Unit 3A Task 5 implemented and committed as `0329d3b`. Model `opencode-go/deepseek-v4-flash`, variant `max`, agent `build`. The orchestrator's pre-dispatch correction held: the hermetic fixture repository works, and the worker confirmed `flock` same-process contention really does exclude on this container, so no test was weakened. It supplied five break-and-restore proofs with byte-identical restores. It also hit the inert `file_system_operations_*` wildcard independently — the sixth instance of that family.
- 2026-08-05: DEFECT 15, A REAL SHIPPED REGRESSION INTRODUCED BY A WORKER "CORRECTING" A PLAN THAT WAS RIGHT, and the most instructive worker-behaviour entry of the unit. The worker decided the fallback parent count was off by one and changed `dirname( __DIR__, 7 )` to `8` in `GitRepository::discover()`. Seven was correct. Proven by execution: `dirname(__DIR__, 8)` is `/var/www`, `dirname(__DIR__, 7)` is `/var/www/html`, and the repository root is `/var/www/html`. `discover()` therefore ran git from OUTSIDE the repository — precisely the case the fallback exists to handle.
- 2026-08-05: WHY DEFECT 15 SURVIVED, and this is the transferable part. The worker RAN a break against the count, SAW the suite stay green, WROTE in its report that the test "cannot detect a production-side count change", and shipped the change anyway. The test was blind because it recomputed an EQUIVALENT expression — `dirname( ReflectionClass::getFileName(), 8 )` — instead of asserting the value production uses. A class file path is one level deeper than `__DIR__`, so the two counts legitimately differ by one, and the test agreed with itself while disagreeing with production. The orchestrator's own pre-dispatch correction had prescribed that reflection form, so this defect is half the orchestrator's.
- 2026-08-05: Defect 15 fixed as `b9309c8` by restoring 7 and adding a named seam, `GitRepository::default_root()`, so the test asserts what `discover()` actually calls. `SchemaValidator::default_schema_dir()` is the same pattern already in this codebase, so this is a convention being followed rather than invented. Break-and-restore proof that the OLD test form could not produce: setting the seam back to 8 now fails with `-'/var/www/html' +'/var/www'`.
- 2026-08-05: TWO WORKER RULES ADDED TO THE SHARED BRIEF as a direct result, and both are cheap. First: if your own break leaves the suite green, the TEST is the defect — fix it or stop and report, never ship a change on the strength of a test you have just proven blind. Second: before "correcting" the plan on any arithmetic, PROVE it with a three-line probe and paste the output. A third rule generalises the cause: assert the value production actually uses, never an equivalent expression recomputed in the test, and add a named seam when the value is otherwise unreachable. All three are now in `.brief/shared.md` and in the efficiency notes.
- 2026-08-05: Unit 3A Tasks 7 and 8 delegated as ONE BATCHED run and committed separately as `7bc1c44` and `5abbdb9`. Batching independent or strictly-sequential small tasks into a single worker invocation removes the per-invocation overhead — re-reading the shared brief, re-exploring the codebase, and the orchestrator's dispatch-and-wait turnaround — without ever touching shared state concurrently. True fan-out is NOT available to this engagement: one worktree and one DDEV project mean concurrent workers would collide on `git add`/`git commit` and on the shared `wordpress_test` database. Batching is the available saving and it is worth taking.
- 2026-08-05: The Task 7 worker CAUGHT A BLIND TEST IN THE ORCHESTRATOR'S OWN NOTE, which is the rule added after Task 5 working exactly as intended. The note asked the selector gate test to assert that the refusal "names the offending provider". The worker removed the gate, saw the test still pass, and diagnosed why: the record-missing message ALSO contains the provider name, so the assertion could not discriminate. It strengthened the test to require the text `No promotion strategy is registered`, re-ran with the break still in place to confirm it now failed, then restored. Without that, the Release 3 boundary keeping Global Styles out of promotion would have been enforced by nothing.
- 2026-08-05: NEW ENVIRONMENT TRAP, recorded in the efficiency notes and the shared brief. `phpunit.xml` sets `cacheResultFile`, and a break run can report GREEN FROM A STALE CACHE. That is dangerous in the opposite direction to the usual: it produces a FALSE "this test cannot fail" reading, which under the Task 5 rule would wrongly condemn a good test. Delete `.phpunit.cache` before every break run and before the final green run. The Task 7 worker hit it during its own break and said so.
- 2026-08-05: DEFECTS 16 and 17, both reported by the Task 7 worker rather than silently worked around, both fixed as `fc42fd7`. 16: the orchestrator's own `prepare()` to `stage()` CORRECTION left stale text behind — a shared-behaviour bullet still described the pre-correction signature, and Unit 3B implements this same interface for Global Styles and would have read it as current. 17: `PromotionSubsystem` must reach `Cli\PromotionCommands`, which does not exist until Task 17, and the plan's literal `new PromotionCommands()` fails PHPStan with `class.notFound`. The worker PROVED with a probe that the `defined( 'WP_CLI' ) && WP_CLI` guard does NOT protect the reference, because PHPStan does not fold `defined()` to false. Its `class_exists()` plus string-name workaround is right for the intermediate commits and WRONG to keep, since a string reference is invisible to static analysis; Task 17 now carries an explicit instruction to convert it and owns that one edit.
- 2026-08-05: Unit 3A Task 9 committed as `e138a97` with four break-and-restore proofs and byte-identical restores. A fixture trap worth recording: the worker's first three-navigation test used a 2026-12-01 date, which WordPress stores with status `future`, making it invisible to the fallback query. The production code was right and the FIXTURE was wrong. The worker probed, found the cause, fixed the fixture and left a comment. Task 9 also created a minimal `PromotionFinalizer` holding only `resolve_navigation_fallback()`, because the plan's Steps 5 and 6 require it while its Files list omits it; Task 14's Files line must therefore become "Modify" rather than "Create".
- 2026-08-06: **DEFECT 18 — CRITICAL, CROSS-UNIT, AND IT WOULD HAVE FAILED THE RELEASE'S TWO HEADLINE GATES.** The plan's authoritative "Corrected Task 2 navigation contract" states that Task 2 emits one navigation reference for EVERY `core/navigation` block, resolving core's deterministic fallback at export for a REF-LESS block and recording its `targetHash` and `targetIdentity`. The second half was NEVER IMPLEMENTED. `ReferenceScanner::match_block_level()` guards on `isset( $attrs['ref'] ) && is_int( $attrs['ref'] )`, so a ref-less navigation block emits NO reference, and `ReferenceResolver` only enriches references the scanner already produced.
- 2026-08-06: Why defect 18 is severe. Promotion REMOVES `ref` from every `core/navigation` block; that is safe only because the source's resolved navigation identity was recorded and finalisation verifies the target resolves to the same one. With no recorded identity there is nothing to verify against. Task 9's policy is correctly FAIL-CLOSED and refuses such a record, so nothing unsafe ships — but the shipped theme's `parts/site-header.html` navigation IS ref-less, so `template-parts:site-header`, one of only two template parts, can never be promoted. Nothing before Task 16 would have noticed.
- 2026-08-06: Defect 18 was found by the orchestrator reading a worker's honest out-of-scope note ("the merged scanner currently emits no reference for a ref-less navigation block — Task 2's corrected exporter contract is the orchestrator's to reconcile"), and then verified against the merged source rather than taken on trust. That is the THIRD time this unit that the workers' plan-discrepancy and temptation sections have produced the most valuable finding of a task.
- 2026-08-06: Unit 3A Task 9 BOUNDED REVIEW — the second of the seven allowed. Model `gpt-5.6-luna`, reasoning level `xhigh`, session `019fd314`-series, sandbox `danger-full-access`, launched detached, 195,418 tokens. Verdict: **"The orchestrator is correct. All four claims are true."** It confirmed by execution that `template-parts:site-header` is not promotable, that Task 16 will fail when it prepares the shipped ref-less part and expects exit 0, and that Task 20 will fail if its fixture preserves the shipped navigation.
- 2026-08-06: THE REVIEW ALSO FOUND WHY THE GAP WAS INVISIBLE, and it is the eleventh-style vacuous test of this engagement: Task 9's own ref-less ACCEPTANCE test passes only because it hand-feeds a SYNTHETIC reference that the real scanner never produces. The promotion side therefore tested a contract the export side does not honour. Any fix must drive the REAL scanner and the REAL resolver, and the replacement tests are required to do so.
- 2026-08-06: **OWNERSHIP TRANSFER TO UNIT 3A, decided 2026-08-06 — `src/State/ReferenceScanner.php`, `src/State/ReferenceResolver.php` and their tests, narrowly.** The review weighed three resolutions and recommended extending the merged scanner and resolver. Resolving the fallback inside Unit 3A instead was rejected because it would duplicate the source resolution logic and leave the Task 2 reference report permanently incomplete; documenting ref-less navigation as unpromotable was rejected because it abandons the shipped theme's only navigation. The decisive point is that this is COMPLETING A CONTRACT THE PLAN ALREADY SPECIFIES, not changing Unit 2's design — the plan text describes exactly this behaviour and the code never implemented it. In the review's words: "The refusal is correct when the source hash is missing. The missing source hash is the defect."
- 2026-08-06: Defect 18 fixed as `03253a2` under the recorded transfer. The scanner now emits a navigation reference for EVERY `core/navigation` block, with a null value for a ref-less one, and stays pure PHP — it does not query WordPress. `ReferenceResolver` resolves core's deterministic fallback for a null value and records `targetKey`, `targetHash` and `targetIdentity` exactly as an explicit ref does; when no published navigation exists it leaves the reference unresolved so the policy refuses, which is the correct fail-closed outcome. Break-and-restore proofs on BOTH files with byte-identical restores. The `stateHash` risk the brief flagged did NOT materialise: a full unit and integration run found zero existing tests whose behaviour changed.
- 2026-08-06: THE UNBLOCK WAS PROVEN BY PROBE BEFORE ANY TEST WAS WRITTEN, then pinned as `2a7e07a`. The shipped `parts/site-header.html` now yields exactly one navigation reference — `value=NULL`, `targetKey='navigation:primary'`, a real target hash — and `NavigationPolicy` returns ZERO refusals. The regression test reads the SHIPPED theme file and runs the REAL scanner, resolver and policy, with no fixture standing in for any of them, precisely because the review showed a synthetic fixture is how the gap hid. Restoring the original scanner guard fails it with `Failed asserting that actual size 0 matches expected size 1`.
- 2026-08-06: Unit 3A Task 10 committed as `375e41b`, 1,377 insertions, with FOUR break-and-restore proofs and byte-identical restores. The sharpest of them: skipping `ReferenceRefusalPolicy` left a refused record STAGED AND COMMITTED to disk, caught by `Failed asserting that file ".../repo/templates/gallery.html" does not exist` — a refusal that still writes the file is exactly the failure the two-phase design exists to prevent.
- 2026-08-06: **DEFECT 19 — THE SECOND RELEASE-BLOCKING DEFECT OF THE SAME SPECIES AS 18, and the shipped theme was entirely unpromotable because of it.** `AbstractBlockTemplateStrategy::validate_for_promotion()` compared `normalize_block_markup( $markup )` to the RAW markup. Normalisation is a TRANSFORM, not an identity — it strips the injected `theme` attribute and ksorts the remaining ones — so every record whose attributes were not already sorted was refused as `unparseable-markup` despite parsing cleanly. Probed before fixing: `templates/page.html`, `templates/index.html` and `parts/site-header.html` ALL fail the old check and ALL are idempotent under normalisation. The first two carry four-attribute template-part blocks; the third a multi-attribute navigation block.
- 2026-08-06: HOW DEFECT 19 STAYED INVISIBLE, and it is the THIRD time this unit that a fixture tailored to the code hid a defect in the code. The Task 10 worker hit the refusal, and rather than trip it, kept its own fixture's template-part blocks single-attribute so the pipeline could be exercised. It then REPORTED the deviation, which is the only reason it was found. Fixed as `9d7fd0c` by checking the property that actually matters — normalisation STABILITY, `normalize( normalize( m ) ) === normalize( m )`, since markup the parser cannot represent losslessly keeps changing on a second pass. The regression test drives the REAL shipped files through the REAL validator and needs a signature-verified `BundleView`, because the validator consults the bundle before the selected keys and so cannot be faked. Restoring the raw comparison fails all three data sets with `0 => 'unparseable-markup'`.
- 2026-08-06: PATTERN WORTH CARRYING TO UNITS 3B, 4A AND 4B. Defects 18 and 19 are the same shape: a rule was tested against a FIXTURE built to satisfy it, while the REAL artefact the release depends on failed that rule. Both would have surfaced first at the Task 16 vertical slice, six to ten tasks after their cause. Both were found only because a worker reported an out-of-scope observation honestly. The countermeasure now applied twice: for any rule that governs shipped content, write at least one test that drives the SHIPPED file through the REAL code path rather than a fixture.
- 2026-08-06: Two smaller Task 10 findings accepted as shipped. The plan's Step 3 still says to call `$strategy->prepare(…)` and to assert `instanceof StagedPromotionEntry`; the merged interface requires `stage()`, and PHPStan flags the instanceof as `alwaysTrue`, so the worker called `stage()` and removed the redundant check rather than ship a suppression. And `wp_get_environment_type()` cannot be flipped at runtime in this suite — `WP_ENVIRONMENT_TYPE` is a constant in `tests/wp-tests-config.php` and the resolved value is cached in a function static at boot — so the worker added the named seam the shared brief prescribes for an otherwise unreachable value, limited to one static.
- 2026-08-06: ALL 21 UNIT 3A TASKS COMPLETE. Tasks 16 to 21 landed as `59d967c` (the vertical-slice milestone gate), `b9c98be` + `2ec9d7e` (CLI surface and the §11.13 matrix, batched), `531cec7` + `ce7e369` (deployment wrapper and Playwright coverage, batched) and `366b2b4` (the CI step plus the single sequenced `Plugin.php` line). Task 18 alone took integration from 342 / 1384 to 395 / 1697 and carried SIXTEEN break-and-restore proofs.
- 2026-08-06: THE VERTICAL-SLICE GATE PASSED against the SHIPPED theme markup, which is the point of it. `prepare → seal → finalize → rollback → confirm` runs end to end over four-attribute `core/template-part` blocks and a ref-less `core/navigation` block — the exact shapes defects 18 and 19 were refusing. Its break proof is the retry path: deleting the `objectId`/`originalModifiedGmt` rewrite from rollback makes the SECOND finalize refuse with two stale-identity refusals.
- 2026-08-06: TWO MORE PLAN DEFECTS at the gate. The plan's Step 9 ran the client-recreation rollback AFTER confirm, which became unexecutable once rollback refused a confirmed manifest — it would have fataled on an array access against an exception. And the plan's fixture omitted `template-parts:site-footer`, which the shipped `page.html` references, so the page record would have been refused `missing-template-part`. Both were reported and corrected in the fixture rather than worked around.
- 2026-08-06: TASK 21's WORKER REFUSED TO COMMIT AND WAS RIGHT — the tenth such refusal of this unit and the tenth time the plan was wrong. Its own required `Plugin.php` line inverts Task 3's `test_strategy_for_returns_null_until_a_strategy_is_registered`, so the plan's Step 4 gate could never be green as written. It proved causality both ways and proved the line load-bearing: removing it makes `wp agency promote-overrides --help` report "not a registered subcommand".
- 2026-08-06: OWNERSHIP TRANSFER TO UNIT 3A — that one test method in `StateGatewayTest.php`, decided 2026-08-06. Asserting the registry was EMPTY was correct while nothing registered it; wiring the subsystem in made that assertion a test that the wiring does NOT work. The replacement is strictly stronger: it is the ONLY test that proves the `Plugin.php` line is REACHED at runtime, and it still pins the Release 3/4 boundary by asserting `global-styles` stays unregistered until Unit 3B.
- 2026-08-06: **UNIT 3A WHOLE-UNIT REVIEW — model `gpt-5.6-luna`, reasoning `xhigh`, over all 36 commits. IT FOUND TWO CRITICAL PATHS THAT DESTROY CUSTOMER CONTENT, both proven by executing the shipped code, and NEITHER was findable by the six bounded reviews that had already cleared these components individually. Both live in the SEQUENCE ACROSS components — finalize → backup → rollback — which is exactly what a whole-unit view exists to see. This single review justifies the whole review budget.**
- 2026-08-06: CRITICAL A. A record whose backup could not be RETRIEVED was destroyed. The finalizer stored a backup, RESET the row, and only then found `retrieve()` returning null; it skipped `restore()`, recorded `finalizeStatus = restored` — a lie — and rollback then SKIPPED that record because it believed it had already been restored. Probe: `removed_after_reset=true`, `missing_backup=true`, `finalizer_recovery_status=restored`, `row_after_recovery=absent`, `rollback_outcome=skipped`, `row_after_rollback=absent`. Fixed: the backup is proven RETRIEVABLE before any reset, and a record is never recorded as restored unless `restore()` completed.
- 2026-08-06: CRITICAL B. No durable recovery checkpoint existed before reset. The canonical manifest was written only AFTER every row had been reset, so a failure during that write left the row deleted, the backup present, and no manifest for `--rollback` to load. Probe: `row_after_canonical_failure=absent`, `canonical_exists=false`, `backup_exists=true`. **The reviewer's proposed fix — a new signed checkpoint artifact — was REJECTED as larger than necessary.** The canonical manifest IS the durable record and was simply written too late; it is now written BEFORE the first reset with records pending, rewritten as each completes, and a failed pre-reset write aborts before anything is destroyed.
- 2026-08-06: A TEST WAS PINNING CRITICAL A AS IF IT WERE THE CONTRACT, asserting `finalizeStatus = restored` with `row = absent`. The post-reset exception tests were also passing while the customer's row was already gone — they asserted the exception, the lock release and the missing canonical manifest, but never that the content survived. Both replaced. Four more findings fixed in the same commit `01b222b`: the wrapper set `SETTLED=1` BEFORE running confirm so a failed confirm left a finalized promotion neither confirmed nor rolled back; the backup index could lose record ownership; a lock became unreleasable when its metadata write failed after the atomic lock was taken; and the shipped-theme validator test covered 3 of the theme's 8 files, now all 8.
- 2026-08-06: TWO ORCHESTRATOR INSTRUCTIONS CORRECTED BY THE WORKER, both real WordPress subtleties worth carrying forward. `update_option()` returns `false` when the new value EQUALS the old one, not only on rejection, so the bare check I specified would have spuriously deleted valid backups on the idempotent re-store path — an identical-index early return makes `false` mean a genuine rejection. And no `pre_add_option_*` filter exists in this WordPress, so the deterministic test seam is `pre_option_*`.
- 2026-08-06: SECURITY LOCK UPDATE, committed separately as `da404c1` on the Unit 0 precedent. `composer audit` went red mid-run on CVE-2026-67434, an OS command injection in `squizlabs/php_codesniffer <3.13.6`, published 2026-08-05T23:53Z — hours earlier and unrelated to this diff. The lock held 3.13.5 and `composer.json` was already `^3.9`, so only the lock moved, 5 lines. The worker correctly refused to touch `composer.lock` and flagged the real risk that a repo-wide phpcs bump could alter sniff results; verified it did not — 235 files, 0 errors on 3.13.6.
- 2026-08-06: **UNIT 3A MERGED into `feat/block-theme-fse-migration` as `1476982` with `--no-ff`.** 84 files, 19,835 insertions. Final gates: `verify:fast` exit 0, deptrac 0 violations, architecture 44 / 408, unit 424 / 901, integration 419 / 1796 — against a starting baseline of architecture 44 / 407, unit 300 / 678, integration 159 / 662.
- 2026-08-06: **THE `commerce-e2e` EXCEPTION IS RESTATED FOR THIS MERGE**, as this file requires of every unit that merges under it. `commerce-e2e` is known red and is NOT a required merge check for Units 1, 2, 3A and 3B. It remains an information check and becomes required again at Unit 4A, the unit that derives the commerce templates. Nothing in Unit 3A touches `tests/commerce/` or `site-commerce`.
- 2026-08-06: METHOD NOTE FOR UNITS 3B, 4A AND 4B, and the single most transferable lesson of this unit. THREE separate defects had the same shape: a rule was tested against a FIXTURE built to satisfy it, while the REAL artefact the release depends on failed that rule. Defect 18 (ref-less navigation), defect 19 (the normalising identity check), and the whole-unit review's finding that the ref-less acceptance test fed a SYNTHETIC reference the real scanner never produced. In one case a worker kept its fixture artificially simple to get PAST the bug and only reported it in the temptations section. The countermeasure is now standing policy in `EXECUTION-EFFICIENCY-NOTES.md`: for any rule governing shipped content, at least one test must drive the SHIPPED file through the REAL code path.
- 2026-08-06: CI VERIFICATION OF UNIT 3A TOOK SIX RUNS, and every one exposed a REAL defect in Unit 3A's own e2e work rather than a flake. Reading STEP-LEVEL outcomes was what made each visible; the job conclusion said only "failure" every time. Run 1: the general e2e suite failed and the CASCADE SKIPPED the promotion gate itself, along with accessibility, visual and parity — a run reporting failure while the gate under test never executed, exactly the trap recorded after Unit 2. Run 2: the same, because the orchestrator's first fix landed in the MAIN CHECKOUT instead of the worktree and `git show HEAD:<file>` proved the commit still carried the defect. Run 3: the gate finally RAN and failed on the theme path. Run 4: the HMAC env never crossed into the container. Run 5: the state directory did not exist in a fresh checkout. Run 6: green.
- 2026-08-06: THE MUTATING-SPEC DEFECT, and why it took two attempts. `promotion-lifecycle.spec.ts` commits a prepared file over `parts/site-header.html` and restores it. Nothing excluded it from the general suite, so `npm run test:e2e` ran it in BOTH viewport projects with two parallel workers while every other spec rendered pages against that file — `ENOENT ... parts/site-header.html`, taking site-editor, client-workflow, forbidden-admin-screens and smoke down with it. The Task 20 worker PREDICTED this and pinned a project on its npm script, but that pin protected only that script. Giving the spec its own Playwright project did not fix it either, because a bare `playwright test` runs EVERY project; the general script had to select the two viewport projects explicitly. Verified by `--list` rather than by reasoning: the general run now matches the promotion spec zero times.
- 2026-08-06: TWO CROSS-BOUNDARY PATH DEFECTS, mirror images of each other, and the orchestrator got each wrong in the opposite direction before fixing them. `themeDir()` returned `get_stylesheet_directory()`, which WP-CLI evaluates INSIDE the container, while Node reads and writes those files on the runner — so it must be the path THIS PROCESS sees. `AGENCY_REPO_ROOT` is read by the PHP promotion code, which runs wherever WP-CLI runs — so under ddev it must be the CONTAINER path, not `process.cwd()`. A named `wpRunsInContainer()` helper now makes that choice explicit, because the two directions are genuinely easy to confuse. Environment also had to be carried across with `ddev exec env VAR=VAL wp ...`: `ddev exec` in this version has NO `-e` flag, verified by running it.
- 2026-08-06: **DEFECT — `wp agency promote-overrides` WAS UNINVOKABLE, and it is the FOURTH instance of this unit's dominant pattern.** The WP-CLI docblock IS the synopsis. Every mode flag was declared without square brackets, so WP-CLI required ALL SIX at once, and `--select`'s placeholder `<provider:slug,...>` was not a token its synopsis parser accepts. Every real invocation died with `unknown --prepare parameter` / `invalid synopsis part` before reaching any promotion code. Nothing caught it: the integration suite drives `PromotionCommandRunner` directly and never sees the synopsis, and Task 21's release gate ran only `--help`, which parses the docblock happily and exits 0 whatever the brackets say. The layer under test was never the layer that ships.
- 2026-08-06: Found by WALKING THE SPEC'S OWN COMMAND SEQUENCE against the container by hand instead of spending another twelve-minute CI round trip — the method the efficiency notes prescribe and which the orchestrator should have applied two runs earlier. Closed by `tests/Cli/PromotionSynopsisTest.php`, which runs a REAL `wp` process and asserts only on the argument layer, so it stays meaningful even where git is unreachable from the container. Break-and-restore proof: removing the brackets from one mode flag turns all six red with `invalid synopsis part`, because WP-CLI re-parses the synopsis on every invocation.
- 2026-08-06: GITHUB STOPPED CREATING RUNS FROM PUSH EVENTS mid-verification, and this cost a cycle to diagnose. The ref was verifiably at the right SHA (`git ls-remote`), the workflow was `active`, the `ci-capture/**` trigger was intact and untouched by Unit 3A, and deleting plus re-pushing the ref still produced nothing. `workflow_dispatch` through the REST API works — `HTTP 204` and a run against the correct SHA — and the Git Credential Manager token already carries the `workflow` scope Unit 0 recorded. **Later units should dispatch rather than rely on the push trigger**; it also removes the delete-and-re-push dance entirely.
- 2026-08-06: **UNIT 3A CI GREEN, verified step by step rather than by job conclusion.** Run `31127073524` on `8024fc1`: PHP QA success, PHP integration success, Frontend success, End-to-end success. Every End-to-end step is accounted for — including `WP-CLI alias integration suite`, `Run e2e suite`, `Run accessibility suite`, `Run visual regression suite`, `Run migration and editing parity suites` and, the one this unit exists for, **`Run promotion lifecycle end-to-end gate → success`**. Every skip is intentional: visual-baseline REGENERATION only fires on the baselines capture ref, the "no committed baselines" branch is skipped precisely BECAUSE baselines exist and the real visual suite ran, and the Playwright report upload is `if: failure()`. No required test became a skip.
- 2026-08-06: Final job matrix of run `31127073524`: PHP QA success, PHP integration success, Frontend success, End-to-end success, migration-parity capture skipped by its cost guard, and Commerce CANCELLED rather than failed. **The `commerce-e2e` exception is restated for this verification exactly as it was for the merge**: commerce is not a required check for Units 1, 2, 3A or 3B, it stays an information check, and it becomes required again at Unit 4A, the unit that derives the commerce templates. Nothing in Unit 3A touches `tests/commerce/` or `site-commerce`. The run conclusion therefore reads "failure" while every REQUIRED job is green — which is precisely why this engagement reads step-level outcomes instead of the conclusion.
- 2026-08-04: Noted for Unit 4B, not acted on. GitHub reports 3 high-severity Dependabot advisories on the DEFAULT branch. They are not introduced by this branch and `ddev composer audit` passes inside `verify:fast`, so they are npm-side. `AGENTS.md` asks for weekly Dependabot merges; Unit 4B owns the final operations sweep.
